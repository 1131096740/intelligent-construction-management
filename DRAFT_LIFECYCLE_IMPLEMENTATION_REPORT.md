# 建工智管全系统草稿生命周期治理实施报告

> 报告状态：Task 17 自动化验证已完成；真实岗位 UAT、生产只读盘点和生产备份隔离迁移仍是发布阻断
> 规格基线：`docs/superpowers/specs/2026-07-19-draft-lifecycle-governance-design.md`
> 实施计划：`docs/superpowers/plans/2026-07-19-draft-lifecycle-governance-implementation.md`
> 当前已核对代码范围：Task 0–17；本报告不代表 Task 18、生产发布或真实岗位 UAT 已经完成

## 1. 当前结论

Task 0–16 已将方案 C 落入共享契约、三个前向迁移、各领域服务、Web 公共交互和主要工作台。当前实现的核心原则是：让员工结束无效工作，但不把企业审批、文件、金额和责任事实从账本中抹掉。

截至本初稿形成时，可以确认：

- 没有建设业务根记录的通用物理删除能力。
- “放弃填写、删除草稿、放弃申请、正式撤回/作废/异常终止、技术临时清理”已经按不同事实强度分层。
- 合同、结算、付款、项目支出、零星采购父子流程与四类模板均由各自领域服务判断动作，不由前端仅凭状态猜测。
- Web 已建立正式台账、我的草稿、退回待修改和已结束的分离语义；首页草稿不再冒充审批待办。
- M70–M72 仅增加可空生命周期事实、状态值、约束和索引；未回填或改写既有业务记录。
- Task 17 已重新执行三包全量测试、类型/静态检查、构建、空 PostgreSQL 16 的 72 个迁移、本地回滚不变量验证和六档浏览器验证。
- 空库验证真实发现并修复 M70–M72 大小写表名 `regclass` 引用以及 M72 `indexdef` 规范输出匹配两项迁移阻断，并新增回归测试锁定 PostgreSQL 实际行为。
- 真实岗位 UAT、当前生产只读 A/B/C/D 盘点和生产备份 69→72 隔离迁移尚未获得本轮执行证据，不能由开发者或本地合成数据代替。

因此，本报告结论为“代码与本地自动化候选已验证，业务发布仍为 No-Go”，不能宣称真实 UAT 已签认或允许直接上线。

## 2. 范围与非范围

### 2.1 已实施范围

- 新合同与合同变更草稿。
- 历史合同接管及批次接管。
- 合同税务事实修订。
- 结算工作台草稿和乙方签署证据。
- 普通付款退回待修改后的申请放弃。
- 报销与旧项目支出的本地填写、撤回和既有作废语义。
- 零星采购父申请、自动 A5 付款草稿、收货草稿重置与父子安全级联。
- 合同业务模板、合同版式、标准条款和结算模板的草稿版本废弃。
- 正式台账、我的草稿、退回待修改和已结束视图，以及首页独立草稿队列。
- 统一敏感动作、未保存离开保护、只读存量盘点脚本与数据库约束验证。

### 2.2 明确未实施或未改变

- 不改变审批节点、审批人解析、自审规则、委托、转交、OR 签、归档和生效逻辑。
- 不改变合同金额上限、结算占额、付款额度、项目资金池、供应商余额和元分转换算法。
- 不修改文件上传 API、文件类型/大小/权限、COS 私有读写或短时下载 URL。
- 不给 `super_admin`、主管或其他岗位新增通用业务删除权。
- 不自动删除 30 天或 90 天以上的业务草稿。
- 不批量治理生产存量，不执行 COS 业务对象物理删除。
- 不把项目支出改造成持久化草稿，也不把旧 `spot_purchase` 支出迁入新零星采购状态机。

## 3. A/B/C/D 盘点规则

只读盘点脚本使用 A/B/C/D 识别候选，分类是风险提示，不是自动写入指令。

| 分类 | 业务含义 | 可用处置 | 关键边界 |
| --- | --- | --- | --- |
| A | 可以安全放弃的纯净持久化草稿 | 当前责任人二次确认后“删除草稿” | 必须从未提交、无审批/签署/归档/实付/下游引用，实际仍由领域事务复核 |
| B | 需责任人确认的审批型草稿 | 填写原因后“放弃申请” | 终止活跃流程并保留审批、文件、来源和责任历史 |
| C | 已存在下游正式事实的业务记录 | 只能走既有撤回、作废或异常终止；禁止删除 | 实付、有效收货、退款、签署、归档、发布或正式引用均失败关闭 |
| D | 技术临时数据 | 只按独立保留策略取消、过期或清理 | 不与业务草稿治理混用，清理前仍须复核统一文件绑定 |

另有“本地未保存填写”这一客户端类别：用户选择“放弃填写”时不创建业务记录，不进入 A/B/C/D 持久化盘点。

## 4. 统一生命周期和台账口径

### 4.1 用户动作

- **放弃填写**：只清空或关闭尚未保存的表单，不产生后端业务写入。
- **删除草稿**：仅针对纯净持久化草稿；后台标记放弃并默认隐藏，不物理删除。
- **放弃申请**：针对曾提交、撤回、退回或带审批/文件事实的草稿；强制填写原因。
- **撤回、作废、异常终止**：继续使用领域既有正式状态机和权限。
- **废弃草稿版本**：只针对从未提交且未被正式引用的模板草稿版本。
- **重置未提交收货**：创建新的空修订，保留收货根单、旧修订和锁定证据。

### 4.2 四类读取视图

- **正式台账**：正式记录和当前有效流程；不含纯净草稿、退回待修改和已放弃记录。
- **我的草稿**：只含当前用户负责、从未提交的纯净草稿。
- **退回待修改**：只含曾进入审批、当前等待原申请人继续办理的记录。
- **已结束**：含已放弃、已撤回、已驳回、已作废和异常终止记录。

正式摘要、金额统计和待我审批均排除纯净草稿、退回待修改和已放弃记录。首页另设中性的“我的草稿”队列，不重复进入待我处理、阻塞事项或审批中心。

## 5. 各领域实施结果

### 5.1 合同、变更、接管与税务修订

- 新合同从未提交且无业务证据时，当前经办人可删除草稿。
- 合同变更统一使用放弃变更；放弃派生版本不会修改原有效合同、付款条款、累计金额、历史结算或付款。
- 一旦存在审批、正式文件、授权、用印、归档或下游事实，只能放弃申请或执行既有正式动作。
- 历史接管支持单条放弃和批次 preview/apply；apply 使用稳定 hash 与事务内复核，任一行漂移或阻断则整批零写入。
- 接管批次收据、fingerprint、资料、更正和审计永久保留，避免出现幽灵合同或重复接管。
- 税务修订放弃不更新当前有效合同版本的发票类型、税率、含税计价或合同清单事实。

### 5.2 结算

- 无乙方签署或冻结完整结算单的纯净草稿可以删除。
- 已生成冻结完整结算单、上传乙方签署 PDF 或曾提交审批的草稿只能放弃申请。
- 放弃后活动签署绑定失效，但文件、页数、声明和历史证据保留。
- 放弃未形成正式 `Settlement` 的最终结算草稿，会释放活跃最终结算草稿唯一性和发起阻断；不改合同或结算金额事实。
- 已形成正式结算的记录不能通过草稿接口结束。

### 5.3 普通付款、报销与项目支出

- 普通付款仍保持“提交即创建审批申请”，未提交页面只有本地放弃填写，不伪造持久化付款草稿。
- 只有最新付款审批实例确实退回原申请人、当前记录仍为可编辑草稿时，才返回“放弃付款申请”。
- 放弃付款按更新时间 CAS，并复核审批、实付、凭证、入账、PDF、业务归档和融资额度事实。
- 已释放额度不重复释放；异常残余占用只允许在同一后端事务中释放一次。
- 项目支出不新增草稿删除 API：未提交表单放弃填写，审批中由申请人撤回，既有授权岗位按原规则作废；部分实付或已实付不提供普通作废/删除。
- 新付款拒绝状态已统一写为 Schema 合法的 `approval_rejected`，读取兼容历史 `rejected`。

### 5.4 零星采购父子流程

- 父采购根据跨版本提交、审批和执行证据区分删除纯净草稿与放弃申请。
- 父流程只允许级联关闭未提交付款草稿和从未提交收货；任何正式付款、实付历史、有效收货、差异、退款或归档均失败关闭。
- 自动 A5 付款草稿可由当前经办人放弃；附件、来源和正式历史保留。
- 放弃后父采购回到“待创建付款申请”，且只在没有活动付款并存在合法来源时返回唯一重建动作。
- 重建生成新草稿身份并记录来源类型/来源付款 ID；新版无价 A4 重建为 0 元空白 A5，不预填付款对象、账户或渠道，也不替代后端金额算法。
- 收货草稿没有删除入口；重置使用当前修订号 CAS，保留根单、旧修订和锁定照片证据，未绑定业务的对象也不在业务事务中直接物理删除 COS。
- 失效付款草稿不进入默认台账或正式金额摘要，仅在显式结束筛选中可读。

### 5.5 四类模板

- 合同业务模板、合同版式、标准条款和结算模板分别由原领域服务实现废弃，不建设通用模板删除引擎。
- 只有既有模板维护岗位可以废弃从未提交且未被场景、合同、生成文件、结算草稿、正式结算或导入事实引用的草稿版本。
- `super_admin` 的技术只读能力不自动转换为模板废弃权；结算模板仍只允许既有合同主管维护边界。
- 发布、停用、撤销、正式引用和历史快照永久保留；废弃只失效草稿预览并清当前预览指针，不物理删除源文件或预览文件。

## 6. 数据库迁移

### M70 — `20260719210000_contract_settlement_draft_lifecycle`

- 为合同版本、历史接管、税务修订和结算草稿增加可空的放弃时间、操作人和原因。
- 四类状态约束只增加 `abandoned`，并增加状态/更新时间索引。
- 迁移先核验既有状态约束、结算提交事实约束和潜在同名索引。
- 无业务 `INSERT`、`UPDATE`、`DELETE`，无数据回填、级联删除、删表或删列。

### M71 — `20260719211000_payment_spot_draft_lifecycle`

- 为普通付款、零星采购父单和版本增加可空放弃事实。
- 为零星采购付款增加可空来源类型和 `ON DELETE/UPDATE RESTRICT` 自引用来源链。
- 为收货草稿增加可空失效事实；状态约束仅增加 `abandoned` / `invalidated`。
- 不触碰实付、退款、供应商余额、凭证、资金账本或文件对象，不回填来源。

### M72 — `20260719212000_template_draft_lifecycle`

- 为四类模板版本增加可空废弃时间、操作人和原因。
- 状态约束只增加 `discarded`，并补充状态/更新时间索引。
- 业务模板根沿用既有 `discarded`，其余模板根不新增通用终态。
- 不回填、不改写已提交、发布、停用、撤销或正式引用版本。

### Task 17 迁移证据

- 主执行者运行 `pnpm --filter @jiangkong/api verify:draft-lifecycle:local`，在仅绑定 `127.0.0.1` 的一次性 PostgreSQL 16 容器中从空库顺序应用全部 72 个迁移；`Prisma migrate status` 返回 `Database schema is up to date!`。
- 验证器确认 M70–M72 的新增列、状态约束、结束事实约束、来源外键与索引全部存在，缺少必要操作事实的结束记录为 0。
- 本地 seed 后在事务中对合同、付款和模板执行结束状态合成探针并强制回滚；前后正式摘要完全相同：Contract 1、Settlement 1（金额摘要 70,400,000 分）、PaymentRequest 1（64,000,000 分）、PaymentExecution 1（12,800,000 分）、FileObject 12，AuditLog、SpotProcurement、SpotProcurementReceipt、SpotProcurementRefund 均为 0。
- 验证器默认拒绝远程主机、非固定数据库名和非 PostgreSQL；显式隔离恢复模式只允许本机 `jiangkong_restore_*` 数据库运行只读核验，并拒绝回滚写探针。
- 容器、临时目录和 seed 文件均在成功或失败路径清理；仓库工作区未残留 `storage/` 测试产物。
- TODO(授权后)：生产备份隔离恢复库从 69 到 72 的验证。本轮未取得当前生产备份使用授权，不得把历史恢复演练冒充本候选证据。

## 7. 权限、金额、文件和审计不变量

### 7.1 权限

- 草稿动作由服务端返回 `availableActions/blockedReasons`，前端不根据角色名或状态自行开放。
- 删除纯净草稿只允许当前责任人；责任转交后原创建人不能继续删除。
- 主管不能静默删除下属草稿，须先按既有规则完成责任转交。
- `super_admin` 不获得业务删除、放弃、审批、作废或模板维护权限。
- 正式作废、异常终止和模板维护继续使用领域既有权限。

### 7.2 金额、额度与唯一性

- 前端不重算或释放合同金额、结算占额、付款额度、项目资金池或供应商余额。
- 所有合法释放与状态转换在同一后端事务内完成，并通过 CAS、锁和事实复核防止重复释放。
- 合同变更放弃不影响原有效版本及累计金额。
- 结算草稿放弃只释放活跃草稿/最终结算阻断，不改变合同上限和正式结算事实。
- 普通付款、零星付款和父采购在存在实付、退款、收货或残余不可安全释放的占用时失败关闭。
- 业务编号、正式合同编号和来源关系不因放弃而回收复用。

### 7.3 文件

- 不修改既有上传 API、类型、大小、权限和私有下载链路。
- 审批、签署、用印、归档、实付凭证、收货照片和下载审计一旦成为业务证据，只能失效、替换或追加版本，不物理删除。
- 业务事务不直接删除 COS 对象；未绑定临时文件只能进入独立技术保留/清理策略。
- 乙方签署 PDF、正式合同、结算审批单、付款凭证和实际付款证据继续按档案策略保留。

### 7.4 审计

- 放弃、失效、废弃、重置、金额释放和批次处理均保留操作人、时间、原因或动作类型。
- 已发生的审批实例、审批动作、批次收据、来源链、文件绑定和下载事实不因草稿终止而删除。
- 重复请求保持幂等；并发冲突返回稳定业务结果，不以覆盖旧记录换取成功。

## 8. Task 0–16 提交与主要模块

| Task | 提交 | 主要模块/文件组 | 已落地结果 |
| --- | --- | --- | --- |
| 0 | `58ccb409`, `ca4581f7` | 基线合并、`PROGRESS.md` | 纳入最新主线 A5 草稿入口并建立唯一实施分支 |
| 1 | `2f95d3c1` | `packages/shared-domain/src/draft-lifecycle.*`、只读盘点脚本 | 五类生命周期、四视图、七动作键和默认只读 A/B/C/D 盘点 |
| 2 | `efe96389` | M70、Prisma Schema、核心约束/保留测试 | 合同/接管/税务/结算生命周期字段与约束 |
| 3 | `61325a3e` | Contract Controller/Service/Read/Workbench、DTO | 新合同删除草稿与合同变更放弃 |
| 4 | `1de24acb` | ContractTakeover、ContractTaxFacts、DTO | 接管单条/批次原子放弃与税务修订放弃 |
| 5 | `c9592caf` | SettlementDraft Controller/Service、DTO | 结算草稿删除/申请放弃、签署证据留存 |
| 6 | `ea782939` | M71、Prisma Schema、零星采购 Schema 回归 | 付款/零星采购/付款来源链/收货失效事实 |
| 7 | `634658a2` | Payment Service/Read/Controller、ProjectExpense 测试 | 退回付款申请放弃与项目支出边界 |
| 8 | `a0e54756` | SpotProcurement Application/Closure/Read/Controller | 父采购安全放弃和未提交子流程级联 |
| 9 | `8418936f` | SpotProcurement Payment/Application/Read/Controller | A5 草稿放弃、来源链和安全重新创建 |
| 10 | `f3896bd5` | SpotProcurement Receipt/Read/Controller | 未提交收货重置、旧修订和照片证据保留 |
| 11 | `16c021ac` | M72、Prisma Schema、模板约束测试 | 四类模板草稿版本生命周期字段与约束 |
| 12 | `b29415ac` | ContractTemplate/Layout/Clause、SettlementTemplate | 四类模板版本废弃 API、引用保护与审计 |
| 13 | `1d5a226f` | `BusinessDraftAction`、未保存保护、UI 检查 | 统一敏感动作和离开保护，禁止原生 confirm/prompt |
| 14 | `000713c3` | 合同/接管/税务/结算 API 客户端、页面、读服务 | 四视图、服务端动作、批次 preview/apply、工作台 CAS |
| 15 | `ec2c8c99` | 付款/项目支出 API、页面、读服务和路由 | 付款/费用台账分离、本地放弃填写、正式动作复用 |
| 16 | `f4ad4ddf` | 零星采购、模板、首页、读服务及 Playwright | 父子草稿、四类模板、首页草稿队列和正式统计收口 |

具体文件以各提交 `git show --name-only <SHA>` 为准；Task 17/18 如产生新的提交，应在最终报告中追加而不是改写本表的历史事实。

## 9. 已知测试证据（Task 0–16）

下表只记录各任务在提交前已写入 `PROGRESS.md` 的证据。不同 Task 的定向集可能重叠，不能相加为一个新的“总测试数”，也不能替代 Task 17 最终门禁。

| Task | 已记录证据 |
| --- | --- |
| 0 | Shared 102/102；API 3,922/3,937（15 项既有跳过）；Web 787/787；API/Web typecheck、lint、Web `check:ui`、`git diff --check` |
| 1 | Shared 定向 4/4；API 盘点脚本安全测试 3/3；Shared typecheck；脚本语法；`git diff --check` |
| 2 | Prisma format/validate/generate；核心 Schema 与业务记录保留 28/28；`git diff --check` |
| 3 | 合同定向 4 套 352/352；业务记录保留 13/13；API typecheck/lint、业务错误检查、`git diff --check` |
| 4 | 接管/税务定向 4 套 243/243；API typecheck/lint、业务错误检查、业务记录保留、`git diff --check` |
| 5 | 结算定向 5 套 314/314；业务记录保留 13/13；API typecheck/lint、业务错误检查、`git diff --check` |
| 6 | Prisma format/validate/generate；Schema 与业务记录保留 4 套 62/62；`git diff --check` |
| 7 | 付款/项目支出定向 4 套 362/362；API typecheck/lint、业务错误检查、`git diff --check` |
| 8 | 零星采购全模块 22 套 433/433；直接相关 4 套 64/64；Shared 4/4；API typecheck/lint、业务错误检查、`git diff --check` |
| 9 | 零星付款定向 5 套 155/155；API typecheck/lint、业务错误检查、`git diff --check` |
| 10 | 收货/读取/父放弃 4 套 105/105；API typecheck/lint、业务错误检查、`git diff --check` |
| 11 | Prisma format/validate/generate；M72 定向 12/12；`git diff --check` |
| 12 | 模板定向 5 套 183/183；API typecheck/lint、业务错误检查、`git diff --check` |
| 13 | 公共动作、敏感对话框和离开保护 3 文件 10/10；Web typecheck/lint、`check:ui`、`git diff --check` |
| 14 | Web 全量 100 文件 818/818；合同/结算 API 3 套 73/73；Shared 106/106；三包 typecheck/lint、`check:ui`、业务错误检查、`git diff --check` |
| 15 | Web 全量 100 文件 829/829；付款/项目支出 API 5 套 469/469；Web/API typecheck/lint、`check:ui`、业务错误检查、`git diff --check` |
| 16 | Web 全量 100 文件 839/839；首页 API 22/22；零星采购读取 22/22；模板定向 97/97；零星采购 Playwright 2/2；Web/API typecheck/lint、Web E2E typecheck、`check:ui`、业务错误检查、`git diff --check` |

### Task 17 最终门禁

- Shared 全量：12 文件、106/106 通过。
- API 全量：184 套通过、4 套按既有环境条件跳过；4112 项通过、15 项跳过、0 失败。
- Web 全量：100 文件、839/839 通过。
- Shared/API/Web typecheck 与 lint：全部通过。
- API/Web build：全部通过；Web 仅保留既有大 chunk 警告，无构建失败。
- API `check:business-errors`：扫描 326 个生产 TypeScript 文件通过；Web `check:ui`、Web `typecheck:e2e`：通过。
- P0 E2E：57 通过、2 项按既有条件跳过、0 失败；首轮暴露的 7 个旧 mock/旧文案/隐藏 portal 选择器问题均按当前服务端分页生命周期读模型和可见 TDesign 控件修正，没有删除或跳过有效测试。
- `git diff --check`：通过。

15 个计划场景由以下证据共同覆盖：

| 场景 | 主要自动证据 |
| --- | --- |
| 1–3 新合同、撤回后放弃、变更不影响原合同 | `contract.service.spec.ts`、`contract-read.service.spec.ts`、`contract-workbench.service.spec.ts`、`draft-lifecycle-governance.e2e.ts` |
| 4 接管批次 preview/apply 与收据 | `contract-takeover.service.spec.ts`、`contract-takeover.controller.spec.ts` |
| 5 税务修订不改生效事实 | `contract-tax-facts.service.spec.ts` |
| 6 结算释放阻断且保留签署证据 | `settlement-draft.service.spec.ts`、`draft-lifecycle-business-record-preservation.spec.ts` |
| 7 付款放弃与额度单次释放 | `payment-request.service.spec.ts`、`payment-read.service.spec.ts`、`draft-lifecycle-governance.e2e.ts` |
| 8 项目支出不伪造草稿删除 | `project-expense.service.spec.ts`、`flow-detail-responsive.e2e.ts` |
| 9–12 零星采购父子级联、阻断、A5 重建、收货重置 | `spot-procurement-application.service.spec.ts`、`spot-procurement-closure.service.spec.ts`、`spot-procurement-payment.service.spec.ts`、`spot-procurement-receipt.service.spec.ts`、`spot-procurement-workbenches.e2e.ts` |
| 13 模板废弃与正式引用保护 | 四类模板 service/controller 测试、`draft-lifecycle-governance.e2e.ts` |
| 14 三类并发只能成功一个 | 各领域 CAS/事务并发测试及本地数据库强制回滚探针 |
| 15 四视图、统计、首页口径 | 合同/结算/付款 read service 测试、`me.service.spec.ts`、Web 配置测试、`draft-lifecycle-governance.e2e.ts` |

## 10. 浏览器和截图证据

Task 17 必须验证以下桌面尺寸：1512×982、1440×900、1280×800、1180×820、1024×768、900×768。

- 当前会话未提供内置浏览器控制工具，按计划允许的回退路径使用仓库 Playwright；全部数据通过 `page.route` 稳定 mock 隔离，没有连接或修改生产数据。
- 既有响应式 P0 测试覆盖合同、结算、付款、零星采购父单/A5 付款/收货和模板的正常、草稿、阻断、空态、失败及禁用状态；六档窗口均验证页面无整体横溢出且无父子嵌套横向滚动。
- 草稿生命周期专项截图根目录为 `apps/web-admin/test-results/`（测试产物被 Git 忽略），包括：
  - `draft-lifecycle-payment-ended-1512x982.png`
  - `draft-lifecycle-payment-ended-1440x900.png`
  - `draft-lifecycle-payment-ended-1280x800.png`
  - `draft-lifecycle-payment-ended-1180x820.png`
  - `draft-lifecycle-payment-ended-1024x768.png`
  - `draft-lifecycle-payment-ended-900x768.png`
  - `draft-lifecycle-contract-ended-900x768.png`
  - `draft-lifecycle-template-discarded-900x768.png`
- 六张付款截图验证已放弃记录仅在已结束视图出现且统计口径稳定；合同截图验证只读历史、原因和无工作台入口；模板截图验证废弃后不再暴露危险动作。P0 全量另生成既有合同、结算、付款、组织与响应式截图。

## 11. 真实岗位 UAT

真实 UAT 必须由对应岗位人员本人登录完成，不能由开发者代登录、伪造审批或直接写数据库代替。

建议最小签认覆盖：

1. 合同员：新合同纯净草稿删除、提交后退回再放弃、变更放弃不影响有效合同。
2. 合同主管/财务复核岗位：接管批次、税务修订、模板引用阻断和历史只读证据。
3. 结算经办人：纯净结算删除、乙方签署后放弃及最终结算阻断释放。
4. 普通付款申请人：退回待修改放弃、额度不重复释放。
5. 物资员/零星采购经办人：父采购放弃、A5 草稿放弃与重建、收货重置。
6. 项目支出申请人及既有作废角色：本地放弃填写、撤回、作废与实付后阻断。

当前状态：**TODO(真实 UAT)：尚无本轮真实岗位签认，不得代签。**

## 12. 生产证据边界

- Task 1 的盘点脚本具备只读事务和固定 SQL 白名单，但本轮未获得当前生产只读盘点授权，未刷新生产 A/B/C/D 聚合清单。
- 本轮未获得当前生产备份用于本候选隔离恢复的授权，未执行从生产备份 69→72 的候选迁移验证。
- 历史生产部署、备份或恢复收据只能说明当时版本的事实，不能冒充当前草稿治理候选证据。
- Task 17 允许本地空库与合成数据验证；任何生产访问、生产数据治理、推送、部署和生产迁移均不在当前文档子任务授权内。

## 13. 已知问题与后续观察

1. 付款/项目支出服务端读取目前先形成完整可见集再内存分页，口径正确；数据量显著增长前应改为数据库分组统计与分页，不能以当前实现长期替代容量治理。
2. 结算与合同的服务端分类包含审批、文件和授权等批量事实读取，应在真实数据规模 UAT 中观察查询耗时。
3. 模板引用保护当前以准确性优先保留少量按版本只读查询；版本量明显增长后再单独批量化。
4. 长期未处理草稿只标记和筛选，不自动删除；存量治理必须基于只读盘点另行批准。
5. Task 17 的本地空库 72 迁移、自动场景和浏览器矩阵已通过；真实岗位 UAT、当前生产只读盘点和生产备份隔离迁移仍是发布阻断。

## 14. 发布与回滚边界

### 14.1 发布门禁

- Task 17 全量回归、72 迁移、浏览器和 UAT 证据完成后，Task 18 阶段 A 才能形成精确候选 SHA。
- Task 18 阶段 A 必须停止并等待用户明确批准该精确 SHA。
- 未获批准前不得推送、合并 `main`、部署、执行生产迁移或修改生产数据。
- 生产存量批量治理与代码发布分离，不能把“上线”解释为自动放弃历史草稿。

### 14.2 回滚原则

- M70–M72 是前向、可空字段和状态/索引扩展，不使用破坏性 down migration。
- 应用回滚前必须确认旧代码不会因遇到 `abandoned`、`invalidated`、`discarded` 而错误开放动作；不兼容时先关闭新动作入口并保持新代码。
- 已形成的放弃、失效、废弃、来源链和审计事实永久保留，不回写为原状态。
- 代码回滚不能删除迁移新增列、业务文件、审批记录、金额释放记录或审计记录。

## 15. Task 17 完成时必须回填

- 最终验证提交 SHA 与执行分支。
- `verify-draft-lifecycle.cjs` 和 live verification 的场景覆盖及结果。
- 空 PostgreSQL 16 的 72 个迁移证据。
- 本地 A/B/C/D 合成数据前后计数。
- 全量门禁命令、测试数、跳过项和失败修复。
- 六种视口的截图路径与逐项结论。
- 真实岗位 UAT 人员、时间、场景和签认，或明确列为未完成发布阻断。
- 若后续获得授权，生产只读盘点和生产备份隔离迁移的独立收据；未授权则保持“未执行”。

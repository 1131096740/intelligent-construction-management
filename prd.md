# 建工智管工作台实施包 1–5 完成与全员上线 PRD

> 版本：v1.1
>
> 日期：2026-08-03
>
> 状态：执行中；阶段 A 已建立唯一五包上线候选，待启动阶段 B
>
> 适用范围：合同工作台实施包 1–5 的全部任务、全员上线和上线后关闭旧能力
>
> 产品负责人：系统负责人
>
> 执行原则：实际代码、Schema、迁移、测试、Git、部署和生产只读证据优先于进度文档

## 1. 执行结论

建工智管采用“实施包 1–5 全部完成、同日全员上线、关键风险先闭环、上线后快速反馈修正”的推进方式：

1. 所有 11 名员工在同一上线日获得 Web 系统使用权限，不执行跨数日的分批放量。
2. 全员开放前只保留 30–60 分钟的四类真实岗位技术烟测，用于确认登录、权限、核心读取、受控写入、文件和审计链正常。
3. 金额、权限、跨项目隔离、重复付款、审计、私有文件、迁移和备份问题不得依赖员工反馈发现，必须在开放前通过自动化和受控验证关闭。
4. 员工反馈主要用于发现操作体验、字段文案、页面布局、业务步骤和非关键兼容问题。
5. 首份真实合同、首份结算、首次付款申请、首次实付、首次财务入账和首次历史接管由项目负责人监督完成。
6. 实施包 1–4 已通过的代码切片不重复开发，但其尚缺的真实岗位、浏览器、金额和生产业务验收必须补齐，不能仅凭历史局部测试判定完成。
7. Release C1、旧接口物理删除、原生小程序物理清理等动作在上线观察后执行，不阻塞首次全员开放；未执行前实施包 5 和五包整体不得标记为全部完成。
8. 执行采用“一条代码写入主线、冻结 SHA 后多条只读验证线并行”的组织方式；先修复测试、CI、部署 SHA、readiness 等发布门禁，再处理真正缺失的业务行为和证据，避免在不可重复的发布基础上反复返工。

当前代码和生产环境具备继续收口的基础，但现有 `main` 和审计候选都不能直接作为最终发布版本。必须形成新的干净候选 SHA，并重新通过本文规定的门禁。

## 2. 背景与目标

### 2.1 背景

五个实施包已完成大量代码、Schema、迁移和自动化工作，但“实现完成”“局部测试通过”“生产已运行”与“实施包完成”仍是不同状态。目前主要差距集中在：

- 实施包 5 Task 11 的整站能力矩阵尚未收口；
- 当前全量测试命令存在测试收集和构建顺序问题，不能提供确定性全绿回执；
- 浏览器总门、部署工作流、迁移恢复、幂等、依赖安全和生产就绪检查仍有缺口；
- 实施包 1–4 缺少足够的真实岗位和真实业务长链证据，因此仍属于待验收完成；
- Task 12–15、Release C1/C2 和小程序退役仍处于不同程度的未完成或未授权状态。

### 2.2 产品目标

- 完成实施包 1–5 的全部既定任务，并明确区分“首次上线必需”和“上线观察后才能完成”。
- 形成一个可重复、可审计、绑定精确 SHA 的发布候选。
- 在满足资金、权限、数据、文件、迁移和备份安全底线后，同日向全部员工开放。
- 用 3–5 条真实合同业务链验证系统，随后关闭旧调用代码和原生小程序运行时。
- 最终形成可供后续版本复用的上线流程、回滚流程、监控指标和五包逐项验收证据。

### 2.3 非目标

本 PRD 不新增 OCR、完整 OA、发票管理大模块、考勤、人事、安全管理、大屏或其他一期范围外模块；也不授权生产业务数据修改、物理删除、推送、合并或部署。

## 3. 已确认且不得再次悬置的产品决策

### 3.1 生产草稿修订号

- 接受生产现有 `revision 13` 作为新基线。
- 不为了回到 revision 12 改写生产草稿、技术保存回执或审计历史。
- 新候选验证、部署前指纹和后续 CAS 均从 revision 13 开始计算。

### 3.2 原始“约 5 元差异”Excel

- 原始约 75 万元、出现约 5 元差异的部门 Excel 已无法找到。
- 该原文件不再作为首次上线硬阻塞，也不得继续被登记为“等待用户提供”。
- 不得使用另一份真实绿化报价文件冒充该原文件；该报价文件只能作为独立真实业务回归样本。
- 必须依据已确认的问题重建固定、脱敏、可重复的最小回归样本，稳定覆盖：
  - `9`、`9%`、`0.09` 等税率表达归一化；
  - 单合同多税率和不同发票类型；
  - 含税单价录入与不含税单价录入两种方向；
  - 中间精度、逐行取分和汇总顺序导致的差异；
  - Excel 值与系统值不一致时精确定位到行、列和两侧值。

### 3.3 全项目金额计算契约

该契约适用于所有已开放的合同、结算、付款、实付、财务、费用、零星采购、垫资、历史接管及挂靠资金链：

1. 金额计算使用 Decimal、整数分或等价的精确十进制实现，禁止使用 JavaScript 浮点数作为账务结果。
2. 页面和正式金额默认展示两位小数；内部中间值保留满足计算所需的精度。
3. 有明细行的业务按“逐行计算、每行四舍五入到分、再汇总”执行，不先汇总后统一取整。
4. 每一清单行只选择含税单价或不含税单价中的一侧作为录入权威，另一侧由系统按该行税率反算。
5. 合同允许行级多税率和不同发票类型；合同头部税率是默认值或摘要，不强制所有行一致。
6. 系统只校验税率、发票类型和数字格式等基础合法性，不内置复杂税务组合判断；具体税点由合同和财务专业人员确认。
7. 官方 Excel 模板与后端使用相同公式、相同舍入位置和相同汇总顺序。
8. 导入差异必须说明具体行、列、Excel 值和系统值，不得静默改数。
9. 历史正式业务数据不自动重算或覆盖；发现历史差异时只能通过备份、审计和单独授权的更正流程处理。

### 3.4 全员上线方式

- “全量上线”指全部 11 名员工同日获得系统入口和岗位权限，不等于跳过技术门禁。
- 不做跨数日 canary；部署完成后先做 30–60 分钟岗位烟测，随后立即向全部员工开放。
- 高风险首单由负责人在场监督，不限制其他员工正常登录、查询和低风险业务使用。

### 3.5 本 PRD 对旧计划的覆盖关系

以下是产品负责人在旧五包计划形成后的新决定，以本 PRD 为准：

- 旧计划中“必须取得原约 5 元差异 Excel 才能发布”的条款，改为第 3.2 节的可复现样本验收；
- 旧计划中的单一税率一致性仅适用于明确选择单一税率的合同，不能阻止已确认的行级多税率合同；
- 首次全员开放可以早于 C1/C2/M2/M3，但“实施包 1–5 全部完成”必须闭合五个包的所有 Task，包括另行授权后的 C2 和 M3；
- 当前 `AGENTS.md` 中的岗位和审批规则优先于旧计划或旧规格中的历史角色描述。

## 4. 当前事实基线

以下为 2026-08-03 编写本 PRD 时的只读审计基线。所有 SHA、迁移数、依赖和生产计数都会变化，正式发布前必须重新取证。

| 范围 | 当前事实 | 结论 |
| --- | --- | --- |
| `main` / 生产运行基线 | `5234fd37bc5c320922f73323af77b20317fcf5f7` | 当前稳定基线，不包含后续五包收口候选 |
| 五包源工作树 | `6838eb17…`，相对远端领先 58 个提交，并含暂存、未暂存和用户文件 | 必须保护，禁止 reset/stash/clean；不得直接发布 |
| 干净审计候选 | `4c3ca0f3cde24c2476ddee44e4be413d11f9b3c2` | 已包含结算审批撤回 oracle 修复，但不是最终发布候选 |
| 生产用户/项目 | 11 个启用用户、2 个启用项目 | 采用同日全员开放 |
| 生产核心业务量 | 2 份合同版本均为草稿；revision 分别为 1、13；结算、付款申请、实付和财务记录均为 0；历史接管草稿 1 份 | 首次高风险真实写入尚未发生，适合受控首单 |
| 数据库迁移 | 生产 109 条；当前候选基线 116 条 | 新候选必须重新完成迁移和恢复演练 |
| 数据库约束 | 约 170 个未验证约束；“未验证”不等于已发现违反 | 关键表必须在恢复库验证，并形成生产分批验证方案 |
| 备份 | 最近自然 Cron 备份、校验、`pg_restore --list`、异机回执和权限证据通过 | 发布时仍需新备份和恢复证据 |
| Task 11 能力矩阵 | 398 routes、388 wrappers/408 bindings、59 actions/84 bindings、20 unresolved actions、37 orphan wrappers、3 duplicate mutation routes、247 uncovered mutation pairs、310 blockers | 整站严格完成门仍未通过 |
| 全量测试 | Shared 全绿；Web 1748 项断言通过但错误收集 Playwright 文件导致退出 1；API 5616 项断言通过但 verifier 依赖 `dist` 导致 runner suite 失败 | 必须修复测试入口，不能以“断言大部分通过”替代总门全绿 |
| 浏览器门 | 历史精确候选共 16 项，7 通过、9 失败 | 旧证据不可复用，新 SHA 必须重跑 |
| CI/CD | 只有手动生产部署工作流；workflow 与部署脚本的 SHA 确认参数不一致；无 PR/push CI | 上线前必须修复确定性部署链 |
| 安全 | HSTS、登录/刷新限流、私有数据库已具备；缺 CSP，API 暴露 `X-Powered-By`；依赖审计有 5 high、5 moderate、1 low | 可达高危必须清零或关闭受影响能力 |
| 健康检查 | 当前 `/health` 主要返回静态 `ok` | 不能证明数据库、对象存储或任务依赖可用 |

## 5. 五个实施包完成度

| 实施包 | 实现状态 | 上线证据状态 | 当前完成判定与本 PRD 处置 |
| --- | --- | --- | --- |
| 1 核心契约、草稿聚合与模板治理 | Task 1–14 已进入五包代码线 | 聚合读写、租约、保存守恒有自动化；真实岗位长链仍需重跑 | **部分完成**；不重做已通过功能，但必须补齐本包总门和真实验收 |
| 2 清单金额与统一资金规则 | 主要实现和多个真实/合成样本已存在 | 原 75 万元文件缺失；后续会话已批准重建样本并冻结全项目金额契约 | **部分完成**；按第 3.2、3.3 节补齐固定回归和全金额面盘点 |
| 3 响应式工作台、签名与页面入口 | 桌面、移动 Web 和签名能力主要已实现 | Release B 仅有最小烟测，真实岗位浏览器长链不足 | **部分完成**；新候选重跑浏览器总门和真实岗位验收 |
| 4 历史合同与挂靠持续接管 | Schema、服务、页面和测试主要完成 | 尚无真实历史接管、挂靠、结算、付款长链 | **部分完成**；用真实链完成资金不重复和审计验收 |
| 5 迁移、能力收口与旧运行时清理 | Task 1–7 的基础和历史 Release A/B 已有；Task 10/11 部分；Task 8、12–15 未完全收口；Task 9/C2 未授权 | 当前新候选没有完整发布证据 | **部分完成**；按第 7–9 节分成上线前、上线日、观察后执行，全部 Task 闭合后才完成 |

### 5.1 实施包 1–4 必须补齐的完成条件

#### 实施包 1：核心契约、草稿聚合与模板治理

- 逐项复核 Task 1–14 的代码、Schema、迁移、API、页面和测试仍存在于最终候选；
- 证明精确 `contractVersionId`、全局草稿修订、编辑租约、统一保存、预览解耦、提交时编号和模板治理没有旧入口旁路；
- 覆盖空草稿、继续办理、内容相同保存、并发保存、租约接管、失败恢复、提交冻结和正式记录保护；
- 使用真实合同经办和主管完成一条“新建草稿→离开→继续办理→保存→预览→提交准备”的浏览器链；
- 最终候选的本包定向门、全量门、数据库门和浏览器门全部退出 0。

#### 实施包 2：清单金额与统一资金规则

- 完成第 3.2、3.3 节的重建 Excel 样本和全项目金额契约；
- 覆盖手工录入、官方 Excel 下载/导入、系统复算、合同文档、结算、付款、实付、财务、费用、零采、垫资、历史和挂靠资金面；
- 证明税率归一化、多税率、含税/不含税双向录入、逐行取分再汇总、上限/余额、反向更正和重复请求一致；
- 所有启用金额写入口不得使用浮点账务结果，不得静默改数，不得产生重复资金事实；
- 最终候选的金额、并发、数据库和文档回归全部退出 0。

#### 实施包 3：响应式工作台、签名与页面入口

- 逐项复核既定工作台章节、唯一保存入口、问题定位、本地恢复、离开保护、签名、二维码和移动 Web；
- Chromium 桌面与 WebKit 手机覆盖真实页面、真实权限、上传/下载、签名和审批入口；
- 页面动作只按服务端能力与状态开放，不以本地角色判断绕过后端；
- 双击、网络重试、409 冲突、保存失败和预览失败均不丢数据、不重复写入；
- 使用真实岗位完成合同、结算和付款三类关键页面验收。

#### 实施包 4：历史合同与挂靠持续接管

- 逐项复核双部门独立修订、独立确认、撤回、双确认幂等激活和激活后更正；
- 用真实项目完成至少一条历史接管，并继续办理结算、付款申请、实付和财务记录；
- 验证历史实付、未付余额、预付款、异常超付、挂靠拨款/扣款和外部事实不重复计入我方现金或成本；
- 验证主体、项目、文件、金额、审批和审计在跨项目及并发场景下失败关闭；
- 真实链未完成前，实施包 4 不得标记完成。

### 5.2 实施包 5 Task 1–15 当前判定

| Task | 当前判定 | 首次全员上线要求 |
| --- | --- | --- |
| 1 能力基线 | 已有基础 | 对最终 SHA 重生全部 manifests |
| 2 只读预检 | 工具已具备 | 对最新生产快照重新运行 |
| 3 受控转换器 | 工具已具备，历史 transition 已执行 | revision 13 重新绑定；无待转换则零写通过 |
| 4 保留策略 | temporary-only 已运行 | 保持现有边界；业务 purge 不启用 |
| 5 Release A | 历史已完成 | 只作为历史证据，不复用授权 |
| 6 ready 草稿迁移/旧写冻结 | 历史已执行 | 重核 revision 13、旧写 410 和双写为零 |
| 7 Release B | 生产已运行旧基线 | 新五包候选仍需重新发布；同日全员开放 |
| 8 Release C1 | 未完成 | 不阻塞首次上线；观察后执行 |
| 9 Release C2 | 未授权 | 不阻塞；必须另行授权物理清理 |
| 10 合同工作台生产验收 | 部分 | 上线前技术门 + 上线后首单完成 |
| 11 整站能力矩阵 | 部分，严格门 blocked | 上线前关闭所有 P0；最终完成前严格门为 0 blocker |
| 12 旧接口退出 | 未进入 | 观察零调用后逐组执行，不阻塞首次上线 |
| 13 模板/场景/类型/岗位迁移 | 工具/模型部分存在 | 上线前生成报告；有冲突的依赖写入口保持关闭 |
| 14 小程序 M0–M3 | 未完成 | 首次上线不要求物理删除；上线后 M0→M1→M2；M3 另授权 |
| 15 扩展生产验收 | 未完成 | 上线后用 3–5 条真实链闭合 |

## 6. 上线范围

### 6.1 首次上线包含

- 响应式 Web 登录和全部 11 名员工的真实岗位权限；
- 项目、合同、合同草稿、清单、条款、文档、审批、用印和归档；
- 结算、付款申请、付款审批、实付、凭证和财务记录；
- 历史合同双部门接管；
- 已纳入当前一期的项目费用、零星采购、项目资金/垫资和挂靠事实；
- 私有文件、短时下载、敏感动作二次确认和审计查询；
- 监控、备份、恢复和停写/回滚能力。

任何不能通过本文 P0 门禁的单项写能力必须在发布时隐藏、只读或关闭，不能带病开放；这不影响全部员工进入同一个 Web 系统。

### 6.2 首次上线日不要求完成、但五包最终完成仍要求闭合

- Task 8 / Release C1 的旧调用代码删除；
- Task 12 的全部旧接口和封装物理删除；
- Task 9 / Release C2 的旧表、checkpoint 或业务数据物理清理；
- Task 14 M2 的原生小程序代码删除和 M3 数据字段清理；
- 所有存量历史合同一次性全部接管；
- OCR、完整 OA、考勤、人事、安全、大屏等一期外能力；
- 纯 UI、美观、提示语或低频便利性问题全部清零。

上述项目只是不阻塞“首次向 11 名员工开放”。除一期范围外功能外，其余属于五包既定 Task 的内容必须在观察窗口和授权条件满足后继续执行；Task 9/C2、Task 12、Task 14 M2/M3 等未完成时，五包整体状态保持“部分完成”。

## 7. 上线前强制需求

### 7.1 RC-01：形成干净、唯一、可追溯的发布候选

- 以干净候选线继续收口，不直接在脏的五包源工作树发布。
- 保留源工作树及用户公安备案页脚、图片和报告，不 reset、stash、clean 或夹带提交。
- 所有纳入发布的提交必须可追溯到五包基线，并通过 ancestry、`git cherry`、worktree 和远端分支审计。
- 最终只允许一个 `TARGET_SHA`；本地、远端、构建产物、部署输入和服务器 checkout 必须一致。
- `4c3ca0f3…` 只作为当前审计起点，任何后续修改都会产生新的候选并令旧证据失效。

验收：工作树干净、无意外文件、`git diff --check` 通过、候选 SHA 在全部回执中一致。

### 7.2 RC-02：完成全项目金额契约验证

- 将第 3.3 节转换为共享计算契约和固定测试矩阵。
- 重建“约 5 元差异”最小样本，不包含真实相对方、材料名称或其他敏感信息。
- 保留现有真实绿化报价样本和 75 万元合成舍入样本，但分别标注来源和用途。
- 盘点所有启用写入口的金额存储、计算、比较、上限、余额、反向、更正和幂等逻辑。
- 有行明细的模块必须证明逐行取分再汇总；仅存整数分的模块必须证明不经浮点转换。
- 付款执行、凭证上传、普通财务入账、PDF 归档及其他重试入口必须具有请求幂等或等价的数据库唯一约束。

验收：固定样本在系统、官方 Excel、导入预览、保存读回和生成文档中精确到分一致；重复请求不产生第二条资金或财务事实。

### 7.3 RC-03：收口发布关键能力矩阵

对最终 SHA 重新生成 Nest 路由、Web wrapper、页面动作、route usage 和整站矩阵，并完成以下分级：

- P0：上线可见且会修改金额、状态、权限、文件、审批或业务事实的动作；
- P1：低频管理写动作，失效时可通过临时隐藏或只读隔离；
- P2：纯治理覆盖、内部工具或不影响当日业务的技术债。

首次上线硬条件：

- 未分类路由为 0；
- 可见按钮指向不存在路由为 0；
- P0 动作的 unresolved binding、重复 mutation、无消费者写入口和权限绕过为 0；
- P0 动作至少有后端权限/状态测试和真实页面动态证据；
- 所有残余 P1/P2 有负责人、风险、关闭方式和计划日期。

实施包 5 Task 11 的最终完成条件仍是严格 `require-ready` 为 0 blocker；首次全员上线不得把“P0 已清零”误写成“Task 11 全部完成”。

### 7.4 RC-04：修复确定性测试和 CI

- Web 单测不得收集 Playwright E2E 文件；E2E 使用独立配置和命令。
- API verifier 不得依赖偶然存在的旧 `dist`；测试前显式构建所需包或改用可追溯源码入口。
- 最终候选必须让 Shared、API、Web 的标准全量测试命令全部退出 0。
- 运行 Shared/API/Web typecheck、lint、Web `check:ui`、API/Web production build、业务错误检查和 Prisma validate/generate。
- GitHub 增加 PR/push 验证，至少执行全量测试、类型、lint、build、UI 检查、迁移静态门和能力矩阵发布门。
- 修复部署 workflow 与部署脚本的 `TARGET_SHA` / `CANDIDATE_SHA_CONFIRMATION` 契约，Verify 失败时不得推进生产 checkout。

验收：空缓存或明确缓存状态下，CI 对同一 SHA 可重复全绿；故意传入不同 SHA 时部署在改变生产前失败关闭。

### 7.5 RC-05：迁移、数据库与恢复门

- 使用 PostgreSQL 16 从空库完整应用最终迁移，再次 deploy 必须零待办。
- 使用最新自然生产备份的隔离恢复库应用最终迁移和只读预检。
- 验证 7 条候选新增迁移及后续新增迁移的幂等、约束、索引、触发器和文件绑定。
- 对关键业务表的未验证 CHECK/FK 执行恢复库校验，输出生产分批验证顺序、锁风险和失败回退方式。
- 运行付款、实付、财务、接管、额度和审批的双连接并发、重复请求和中段故障回滚测试。
- 生产只执行前向迁移；不得把数据库回退建立在运行 down migration 上。

验收：空库、恢复库、二次 deploy、并发、回滚、记录数/金额/文件/审计守恒全部通过。

### 7.6 RC-06：浏览器和真实岗位技术门

最终 SHA 至少覆盖：

- Chromium 1366×768；
- WebKit 390×844；
- 登录、导航、工作台恢复、保存、模板/Excel、审批、归档、结算、付款、实付、凭证、下载和审计；
- 同步双击只发送一次写请求；
- 403、409、400、503 等错误稳定且不泄露资源存在性；
- 页面无意外 console error、未处理 promise、404 资源和重复 POST；
- 移动端登录、签名、上传、下载和核心办理可用。

验收：最终定义的浏览器清单全部通过，不允许沿用旧 SHA 的 7/16 结果。

### 7.7 RC-07：安全、依赖和就绪检查

- 修补所有可达的 high 漏洞；无法修补时必须关闭受影响入口并形成负责人签字的风险接受，不得只记录数量。
- 移除 `X-Powered-By`，补齐适合当前 Web 的安全响应头。
- CSP 先在生产等价环境验证；如直接强制会破坏功能，上线日可使用 Report-Only，并在观察无违规后转为强制策略。
- 复核登录、刷新、敏感动作、上传和下载限流；不得泄露 token、对象 key、数据库错误或内部枚举。
- 增加 readiness 检查，至少覆盖 API 进程、数据库连接和必要依赖；静态 `{status: "ok"}` 只能作为 liveness。
- 复核 PostgreSQL 仅本机监听、COS 私有、短时 URL、下载审计和二次确认。

验收：可达 high 为 0；未登录受保护 API 为 401；越权访问为统一 403；私有文件不存在公开直链。

### 7.8 RC-08：生产数据和岗位准备

- revision 13 被固定写入发布前快照，不进行纠正写入。
- 复核 11 个启用账号的全局岗位、项目岗位和审批替代关系；`super_admin` 不参与业务审批。
- 至少准备以下真实岗位：合同经办、合同主管、项目经理、财务经办/主管、董事长或总经理 OR-sign。
- 复核 2 个项目的成员关系、唯一挂靠企业、模板/场景映射和可办理合同类型。
- Task 13 报告如有冲突，可以保留只读兼容，但所有依赖冲突分类的写入口必须关闭并显示明确原因。
- 发布前记录合同、版本、revision、审批、结算、付款、实付、财务、接管、文件和审计的只读计数及摘要指纹。

验收：不存在跨项目岗位泄漏、业务审批使用 `super_admin`、未知模板写入或未说明的自动分类。

### 7.9 RC-09：备份、监控和停写能力

- 保留自然 Cron 备份证据；发布前另生成受控发布备份并验证 checksum、`pg_restore --list` 和异机回执。
- 配置 5xx、数据库错误、迁移失败、任务队列失败、COS 上传失败、审计缺失和磁盘容量告警。
- 保留合同/历史接管写冻结、全站维护和高风险模块单独停写能力。
- 建立上线观察看板：登录成功率、5xx、P95、慢 SQL、队列、COS、审计、重复幂等冲突、付款/余额不变量。

验收：发布负责人能在 5 分钟内关闭高风险写入口，同时保留安全的登录和只读查询。

## 8. 可执行阶段计划

本计划按最短可靠关键路径执行。实施期间只允许一条代码写入主线负责修改、提交和更新 `PROGRESS.md`；只有在候选 SHA 冻结后，代码质量、数据库、浏览器、运维安全等只读验证线才能并行。任一验证失败都返回唯一写入主线修复；产生新 SHA 后，旧 SHA 的受影响证据失效。

### 阶段 A：唯一候选收口

> 2026-08-03 执行记录：已从 `4c3ca0f3…` 建立 `codex/five-package-go-live`，可追溯纳入本 PRD，完成工作树处置、首次上线范围冻结和 75 Task 证据映射。收口证据见 `docs/progress/2026-08-03-five-package-stage-a-candidate-baseline.md` 与 `docs/progress/2026-08-03-five-package-75-task-evidence-matrix.md`。此状态只表示可进入阶段 B，不表示 Go-Live Ready。

1. 从干净审计候选 `4c3ca0f3…` 建立新的上线集成分支和独立工作树。
2. 纳入本 PRD 和对应进度提交，解决 `PROGRESS.md` 的真实内容冲突，不直接合并脏的五包源工作树。
3. 保全并逐项审查五包源工作树中的暂存、未暂存和未跟踪资料；公安备案页脚、图片、报告等只在确认属于上线范围后选择性移植。
4. 建立实施包 1–5 共 75 个 Task 的“任务—代码—迁移—测试—动态证据—真实验收”映射；同一份最终 SHA 证据可以证明多个 Task，但不得复用旧 SHA 的发布授权或动态回执。
5. 冻结首次上线范围，不再纳入本 PRD 以外的新模块。

退出条件：形成唯一、干净、可追溯的集成候选；五包源工作树保持原样受保护；所有待纳入内容都有明确来源和处置决定。

### 阶段 B：发布门禁基础设施先行

在继续业务补漏前先建立可重复的验证和部署基础：

1. 在干净候选工作树按锁文件完成 frozen dependency install，记录 Node、pnpm、锁文件和缓存状态。
2. 修复 Web 单测误收集 Playwright E2E 文件的问题，使单测和 E2E 使用独立配置与命令。
3. 修复 API verifier 对偶然存在的旧 `dist` 的依赖，在命令入口显式构建所需包或使用可追溯源码入口。
4. 增加 PR/push CI，至少覆盖全量测试、typecheck、lint、build、Prisma、Web `check:ui`、业务错误和发布关键能力矩阵。
5. 统一 GitHub workflow、服务器部署入口和部署脚本的 `TARGET_SHA` / `CANDIDATE_SHA_CONFIRMATION` 契约；错误 SHA 必须在生产 checkout、构建、迁移和服务重启前失败关闭。
6. 将静态 `/health` 保留为 liveness，新增至少检查 API 进程、数据库和必要依赖的 readiness。
7. 移除 `X-Powered-By`；在生产等价环境验证 CSP Report-Only，并完成可达 high 依赖漏洞分诊。

退出条件：标准门禁在明确依赖和缓存状态下可以重复执行；CI 对同一 SHA 可重复得到一致结果；错误 SHA 和缺失确认参数不会改变生产。

### 阶段 C：能力矩阵风险分级

对阶段 B 的精确 SHA 重新生成全部 machine-readable manifest 和整站能力矩阵。310 项历史 blocker 不能逐项等同于业务缺陷，必须按可达性、动作风险和共享根因重新分级：

- P0：11 名员工在首次上线范围内可进入、可见且会修改金额、状态、权限、文件、审批、审计或其他业务事实的动作；
- P1：低频管理写动作，失效时可以通过隐藏、只读或模块停写隔离；
- P2：纯治理覆盖、退出候选、内部工具、test-only 或不影响当日业务的技术债。

分级时合并共享根因，不把 unresolved action、orphan wrapper、duplicate mutation 和 uncovered mutation pair 的重叠计数重复当作开发任务。每一项 P1/P2 必须记录上线可见性、风险、临时关闭方式、负责人和最终收口阶段。

退出条件：未分类路由为 0；形成唯一的 P0 修复队列和 P1/P2 处置表；不能通过 P0 门的写能力已有隐藏、只读或停写方案。

### 阶段 D：风险优先补齐业务缺口

只修复行为确实缺失、测试已经失效或最终候选证据不足的切片，已实现且仍通过的 Task 登记为“复核通过”，不重复改写。按以下依赖顺序执行：

1. 全项目金额契约、重建 Excel 固定样本、付款/实付/财务幂等、余额和上限守恒；
2. P0 权限、状态、审批、文件、审计、并发和失败回滚；
3. 实施包 1 的草稿聚合、模板治理和正式记录保护复核；
4. 实施包 2 的所有启用金额面、并发和文档一致性复核；
5. 实施包 3 的唯一入口、响应式 Web、签名、上传下载和错误恢复复核；
6. 实施包 4 的双部门接管、挂靠、结算、付款及资金不重复复核；
7. 发布前可以确定性验证的 Task 10–15 和能力矩阵 P0 项。

每个修复必须先有能够暴露问题的聚焦测试，再做最小实现，并运行风险相称的扩展回归、形成独立提交和更新 `PROGRESS.md`。必须依赖生产真实观察的项目只能登记为待生产验收，不能留下已知实现或确定性门禁缺口。

退出条件：实施包 1–4 的代码、Schema、迁移和确定性门禁全部通过；上线范围内 P0 blocker 为 0；剩余项目仅为冻结 SHA 总门或必须依赖上线后的真实验收。

### 阶段 E：冻结 SHA 并行总验证

冻结唯一候选 SHA 后，以下四条只读验证线可以并行执行，期间不得修改代码或生成新的候选提交：

| 验证线 | 必须完成的证据 |
| --- | --- |
| 代码质量线 | Shared/API/Web 全量测试、typecheck、lint、build、Prisma、Web `check:ui`、业务错误和全部能力 manifest/matrix |
| 数据库线 | PostgreSQL 16 空库全迁移、最新自然生产备份隔离恢复、二次 deploy、关键约束、资金/审批/接管并发和中段失败回滚 |
| 浏览器线 | Chromium 1366×768、WebKit 390×844、四类岗位、合同/结算/付款/实付/凭证/文件/签名及 403/409/400/503 负向路径 |
| 运维安全线 | 依赖漏洞可达性、安全响应头、readiness、备份三件套、异机回执、监控告警、全站维护、模块停写和回滚能力 |

任一验证失败都必须返回阶段 D 的唯一写入主线。修复产生新 SHA 后先执行聚焦回归，再重新运行受影响验证线和最终发布总门；不得把不同 SHA 的回执拼成一个 Go/No-Go 包。

退出条件：RC-01 至 RC-09 全部满足，形成绑定同一精确 SHA 的 Go/No-Go 包和生产授权材料。

### 阶段 F：生产等价上线演练

在生产等价环境使用阶段 E 的同一精确 SHA 完整演练一次：

- 从最新备份恢复到 PostgreSQL 16；
- 迁移、构建、部署、readiness 和应用回滚；
- 四类岗位浏览器技术烟测；
- 全站维护、合同/接管停写和恢复；
- 监控、告警、日志和证据归档。

退出条件：同一 SHA 演练成功，演练中未临时修改服务器代码、构建产物或数据库；回滚后代码、迁移状态和业务指纹均可核对。

### 阶段 G：同日全员上线

1. 经单独授权后将最终候选以可追溯方式推送、合入远端 `main`，再次核对 40 位目标 SHA。
2. 完成发布备份、恢复可读性和上线前只读指纹。
3. 进入短维护，执行最终前向迁移和精确 SHA 部署。
4. 运行内部/公网 liveness、readiness、401/403、Nginx、服务、迁移和版本一致性检查。
5. 使用四类真实岗位完成 30–60 分钟烟测：
   - 合同经办：登录、工作台读取/保存、Excel 预检；
   - 合同主管/项目经理：权限、审批读取和受控动作；
   - 财务经办/主管：结算/付款读取、越权负向、凭证入口；
   - 董事长或总经理：OR-sign 待办、签名和终审入口。
6. 烟测通过后，同日开放全部 11 个账号。
7. 发布公告写明反馈方式、严重性、首单监督和停写联系人；前 4 小时集中观察登录、5xx、慢请求、金额、权限、审计、COS 和幂等冲突。

退出条件：全部员工可登录；关键岗位权限正确；上线后 4 小时没有停止上线级事件；首日问题均已分级并分配负责人。

### 阶段 H：真实业务观察与五包最终收口

先在上线后 0–3 个工作日完成：

- 负责人监督第一条合同完整链；
- 监督第一条历史接管后结算/付款链；
- 监督第一次实付、凭证和财务入账；
- 每日检查金额守恒、重复记录、权限、审计、队列、COS、磁盘和备份；
- 完成至少 3–5 条真实合同或等价真实业务链；
- 快速修复 S2 体验问题；所有代码修复仍需新 SHA、聚焦验证和可回退发布。

真实业务观察通过后，严格按顺序完成五包封板：

1. 用真实链补齐实施包 1–4 最后一列生产验收，并分别签署完成结论；
2. Task 10/15：补齐实施包 5 真实生产验收和精确收据；
3. Task 11：把严格能力矩阵降到 0 blocker；
4. Task 8 / Release C1：取得批准时间窗的静态、构建和生产零调用证据；
5. Task 12：按接口组逐个删除旧调用代码，每组独立提交和回退点；
6. Task 13：以报告 SHA、数据库指纹和逐记录 revision 执行获授权迁移；
7. Task 14：M0 盘点，M1 关闭入口，M2 删除专属运行时；
8. Task 9 / Release C2 与 M3：在单独物理清理授权后执行并验收。

退出条件：实施包 1–5 每个 Task 均有完成证据；发布记录分别声明 B、C1、C2、M1、M2、M3 和数据迁移状态。任何未授权的 C2/M3 会使五包整体保持“部分完成”，不得用一个“全部完成”掩盖延期项。

## 9. 上线问题分级与反馈机制

### 9.1 必须立即停写或进入维护

出现以下任意一项即停止相关写入；影响范围不明时全站维护：

- 跨项目或跨岗位数据泄漏；
- 金额、税额、余额、额度或付款上限计算错误；
- 重复付款、重复实付、重复财务记录或幂等失效；
- 审计缺失、可覆盖或与业务事实不一致；
- 私有文件公开、越权下载或短时 URL 边界失效；
- 迁移失败、Schema/应用不兼容或业务数据守恒失败；
- 新旧入口双写或同一动作产生两套事实。

### 9.2 可保持在线并快速修复

- 文案、布局、颜色、间距和提示不清；
- 不影响结果的操作步骤繁琐；
- 低频筛选、排序、导出和显示问题；
- 有明确替代路径且不影响金额、权限、审计和数据完整性的局部问题。

### 9.3 员工反馈最小信息

- 发生时间；
- 登录岗位和项目；
- 页面/操作；
- 业务记录编号或脱敏 ID；
- 预期结果和实际结果；
- 截图或错误文案；
- 是否重复发生。

员工不得在普通反馈渠道发送密码、token、完整身份证、付款凭证原件或 COS 地址。

## 10. 回滚与恢复原则

- 发布前保留代码回滚点、API/Web 产物和发布备份。
- 前向迁移成功后优先回滚应用代码或关闭写入口，不自动运行 down migration。
- 已产生真实业务写入后，不得为了回滚代码直接恢复整库覆盖员工数据。
- 金额、权限或双写事故先停写并保全证据，再决定前向修复、逐笔更正或在明确授权下恢复。
- 回滚后必须复核 SHA、服务、迁移状态、只读指纹、token/租约、队列、文件和审计。

## 11. 验收标准

### 11.1 Go-Live Ready

同时满足以下条件才允许全员开放：

- RC-01 至 RC-09 全部通过；
- 最终 SHA 的标准全量测试、构建、迁移和浏览器门全部退出 0；
- P0 能力矩阵 blocker 为 0；
- 可达 high 依赖漏洞为 0；
- 最新备份可校验、可列目录、具备异机回执并完成恢复演练；
- revision 13、生产计数和业务指纹已冻结；
- 四类岗位烟测通过；
- 监控、停写、维护和回滚负责人已明确。

### 11.2 全员上线完成

- 11 个账号均可登录；
- 岗位菜单、项目范围和敏感动作权限正确；
- 上线后 4 小时无停止上线级事件；
- 首日问题均完成分级和负责人分配。

“全员上线完成”只是运营里程碑，不等于五个实施包已经完成。

### 11.3 实施包 1–5 全部完成

- 实施包 1：Task 1–14 的最终 SHA 静态、数据库、浏览器和真实岗位证据全部通过；
- 实施包 2：Task 1–13、重建 Excel、统一金额契约、所有启用金额面和真实/合成样本全部精确到分通过；
- 实施包 3：Task 1–17、桌面、移动、签名、文件、恢复、入口和真实岗位验收全部通过；
- 实施包 4：Task 1–16、真实历史接管、挂靠、结算、付款、实付、财务和审计链全部通过；
- 实施包 5：Task 1–15 均达到原计划完成定义，严格能力矩阵为 0 blocker；
- 完成至少 3–5 条真实业务链和至少一条历史接管后结算/付款链；
- C1/Task 12 的旧调用退出具备静态、构建和生产零调用证据；
- C2 物理清理和小程序 M3 在独立授权后完成备份、恢复、清理和后置验收；
- `PROGRESS.md`、五包逐项矩阵和最终发布收据一致，不存在“代码完成但验收待补”的条目。

在 C2 或 M3 尚未获得独立授权时，可以保持系统正式运行，但五包整体状态必须继续标记为“部分完成/等待清理授权”。

## 12. 风险登记

| 风险 | 等级 | 控制措施 |
| --- | --- | --- |
| 将员工反馈当成金额/权限测试 | P0 | 上线前自动化、恢复库、浏览器和真实岗位门 |
| 脏工作树或错误 SHA 被发布 | P0 | 干净候选、精确 SHA、workflow/script 双重确认 |
| 测试断言通过但命令退出 1 | P0 | 修复收集/构建入口，以标准命令退出码为准 |
| 未验证约束在生产验证时锁表或失败 | P0 | 恢复库测时、分批验证、维护窗和前向回退方案 |
| 付款/财务重试产生重复事实 | P0 | 请求幂等、唯一约束、并发与故障注入测试 |
| 310 个能力矩阵 blocker 被当成同一种问题 | P1 | 分 P0/P1/P2；首次上线清 P0，最终 Task 11 清零 |
| 原 75 万元 Excel 丢失导致长期阻塞 | 已关闭 | 使用已确认业务规则重建固定样本，不伪装其他文件 |
| revision 12/13 反复决策 | 已关闭 | 接受 revision 13，不改写审计历史 |
| C1/C2/M2/M3 被提前执行 | P0 | 生产零调用、观察期和独立授权门 |
| 全员上线后反馈量集中 | P1 | 严重性分级、首单监督、4 小时集中响应 |

## 13. 必须交付的证据

- 最终发布候选与 Git 拓扑审计；
- 全项目金额契约、重建 Excel 样本和差异报告；
- 五份 machine-readable 能力 manifest 与分类后的矩阵；
- Shared/API/Web 全量测试、类型、lint、build、UI 和业务错误回执；
- PostgreSQL 16 空库、恢复库、二次迁移、并发和回滚回执；
- Chromium/WebKit trace、截图和请求账本；
- 依赖漏洞可达性和修复报告；
- 备份三件套、恢复演练和异机回执；
- 发布日四类岗位烟测、全员开放和 4 小时观察记录；
- 3–5 条真实业务链、历史接管链和首笔资金链验收记录；
- B/C1/C2、M1/M2/M3 和生产数据迁移的独立状态声明。

## 14. 授权边界

本 PRD 的批准仅表示同意产品目标、上线方式、金额口径和执行顺序。以下动作执行前仍需以最终精确 SHA、范围、测试、备份和回滚方案单独确认：

- push、合并 `main` 和生产部署；
- 生产 Schema 迁移或约束验证；
- 生产业务数据 transition、Task 13 apply 或历史更正；
- retention 业务 purge；
- Release C2 旧表/字段/业务数据物理清理；
- 小程序 M3 的 `wxOpenid` 等生产数据清理。

## 15. 事实来源

- [PROGRESS.md](PROGRESS.md)
- [五包实施总计划](docs/superpowers/plans/2026-07-28-contract-workbench-implementation-index.md)
- [实施包 1：核心契约、草稿聚合与模板治理](docs/superpowers/plans/2026-07-28-contract-draft-aggregate-persistence-plan.md)
- [实施包 2：清单金额与统一资金规则](docs/superpowers/plans/2026-07-28-contract-bill-tax-money-plan.md)
- [实施包 3：响应式工作台、签名与页面入口](docs/superpowers/plans/2026-07-28-contract-workbench-frontend-plan.md)
- [实施包 4：历史合同与挂靠持续接管](docs/superpowers/plans/2026-07-28-historical-contract-takeover-plan.md)
- [实施包 5 切换计划](docs/superpowers/plans/2026-07-28-contract-workbench-cutover-plan.md)
- [合同工作台与历史接管规格](docs/superpowers/specs/2026-07-28-contract-workbench-and-historical-takeover-redesign.md)
- [Release A 本地候选审计](docs/progress/2026-07-29-contract-workbench-release-a-local-candidate.md)
- [Release B 修复候选记录](docs/progress/2026-07-30-contract-workbench-release-b-remediation-candidate.md)
- 2026-08-02 五包停机交接、干净候选能力矩阵和 2026-08-03 全项目只读体检证据

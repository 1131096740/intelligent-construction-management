# 建工智管 UI P1.2 合同管理样板迁移实施计划

> 版本：P1.2
> 日期：2026-07-14
> 状态：实施前基线
> 适用范围：合同台账、合同详情及其直接测试与 UI 治理规则

## 1. 实施目标与边界

本轮沿用已经通过 P0/P0.5 和 P1.1 验收的企业级 UI 样板，只迁移合同台账和合同详情。目标是统一页面结构、状态语义、敏感动作和信息层级，不重新设计合同业务，不修改合同工作台、合同变更工作台、历史合同接管或后端事实。

必须保持：

- Vue 3 + TypeScript + TDesign Vue Next，TDesign 是唯一基础组件库。
- `/合同管理`、`/contracts/:contractId`、`/contracts/new`、`/contracts/:contractId/workbench`、`/contract-takeovers` 等现有路由及跳转关系。
- `GET /contracts`、`GET /contracts/:contractId`、草稿列表、变更资格读取和所有现有合同操作接口。
- 服务端对可见项目、岗位权限、审批节点、董事长/总经理或签、用印、归档、合同变更和后续结算准入的判断。
- 合同审批、用印、归档确认后生效；合同条款生效后不可直接编辑；变更/补充协议形成新版本后再走审批、用印和归档。
- 合同金额、结算金额、付款金额和版本替代效果直接展示后端事实，不在前端重算。
- 历史结算与付款继续引用原合同版本和付款条款版本。
- 文件上传、私有下载、二次密码、下载原因、短时效链接和审计链路。

明确不做：

- 不修改 `ContractWorkbenchPage.vue` 及 `workbench/` 下的合同正文、计价、清单、条款、自动保存和文档画布。
- 不修改合同变更工作台、历史合同接管、合同模板/场景/合作单位页面。
- 不修改结算、付款、项目经营、组织权限或其他范围外页面。
- 不新增后端分页，不用前端切片伪装服务端分页。
- 不新增图表、经营大屏、动画、渐变、玻璃拟态或装饰性阴影。
- 不修改后端 API、数据库、路由、权限、业务状态、审批规则和金额逻辑。

## 2. 当前页面结构与主要问题

### 2.1 合同台账 `ContractListPage.vue`

当前结构：

1. 页面顶部直接显示五项合同状态摘要，没有标准页面标题和说明。
2. “合同台账 / 我的草稿 / 已作废草稿”三个页签，默认进入“我的草稿”。
3. 合同台账页签内使用 `BusinessTableToolbar`，但又重复放置“历史合同接管 / 新建合同”。
4. 项目、合同状态、归档状态、付款条款版本和关键词全部使用文本输入。
5. 查询按钮重新读取完整合同列表，本地筛选与数据刷新语义混在一起。
6. 错误使用自定义消息条；列设置整行常驻；表格置于额外 Card。
7. 草稿和作废草稿失败时只显示技术错误文本，没有说明影响和下一步。

主要问题：

- 页面没有标准标题，摘要在所有页签持续展示，切到草稿时仍像是在解释草稿数据，信息语义不准确。
- 新建合同和历史接管同时出现在局部工具栏，缺少全页唯一主任务。
- 加载失败时摘要保留 0，容易把“读取失败”误判为“没有合同”。
- 配置已经声明四个字段为 select，但模板仍统一渲染 `t-input`，结构化筛选没有真正落地。
- 常驻列设置和外层表格 Card 增加边框层级，与付款/结算台账样板不一致。
- 当前接口没有服务端分页契约，页面没有明确说明本次响应范围。

### 2.2 合同详情 `ContractDetailPage.vue`

当前结构：

1. 自定义页头，刷新为主按钮，变更/补充协议和审计并列。
2. 自定义错误/加载 Card。
3. 六项元信息面板和五项流程摘要重复展示状态、金额、责任人和下一步。
4. 业务链路、合同版本历史、流程动作、归档资料、审批时间线连续平铺。
5. 基础信息、生效流程、付款条款、资金时间轴、结算台账和付款台账继续向下平铺。
6. 归档上传仍是浏览器原生文件控件。
7. 审批、归档确认和下载仍调用浏览器 `confirm` / `prompt`。

主要问题：

- 当前状态、合同金额、当前处理人、下一步在元信息和摘要中重复，页头没有形成单一事实区。
- 刷新被强调为主动作，真正的后端 `primaryAction` 没有进入页头任务层级。
- 合同版本、付款条款、流程办理、凭证资料、结算付款和审计全部争夺首屏注意力。
- 加载状态与真实详情结构不一致；读取失败没有统一说明当前数据不能用于业务判断。
- 原生上传、确认框和输入框破坏 TDesign 与敏感操作的一致性，也不利于失败时保留输入。
- 多层 Card、动作组和边框造成容器过多；局部 CSS 仍有硬编码颜色、字号和圆角。

## 3. 本轮准确文件清单

### 3.1 新增

- `UI_P1_2_CONTRACT_PLAN.md`：本实施计划。
- `UI_P1_2_CONTRACT_REPORT.md`：实施结果、差异、测试、截图和遗留问题。
- `apps/web-admin/e2e/ui-p1-contract-visual.e2e.ts`：合同台账与详情的稳定视觉/状态回归。

### 3.2 修改

- `apps/web-admin/src/pages/contracts/ContractListPage.vue`
- `apps/web-admin/src/pages/contracts/contract-list.config.ts`
- `apps/web-admin/src/pages/contracts/contract-list.config.test.ts`
- `apps/web-admin/src/pages/contracts/ContractDetailPage.vue`
- `apps/web-admin/src/pages/contracts/contract-detail.config.ts`
- `apps/web-admin/src/pages/contracts/contract-detail.config.test.ts`
- `apps/web-admin/scripts/check-ui-rules.mjs`
- `apps/web-admin/e2e/p0-browser-smoke.e2e.ts`
- `PROGRESS.md`

### 3.3 原则上不修改

- `apps/web-admin/src/app/design-tokens.css`：现有 P0 token 已覆盖本轮需求；只有发现两处以上真实缺口才允许补充。
- `apps/web-admin/src/components/`：直接复用现有公共业务组件，不新增无业务意义包装，也不修改组件契约。
- `apps/web-admin/src/api/`：现有接口已满足本轮展示和办理，不改变请求地址、参数或返回结构。

## 4. 复用组件与职责映射

| 组件 | 合同台账 | 合同详情 | 职责 |
| --- | --- | --- | --- |
| `BusinessPageHeader` | 使用 | - | 标准页面标题、说明和全页主/次入口 |
| `BusinessStatusSummary` | 使用，`metrics` | - | 白底统计单元格、失败占位和细分隔线 |
| `BusinessTableToolbar` | 使用，`plain` | - | 结构化筛选、刷新和列设置 |
| `BusinessFeedback` | 使用 | 使用 | 读取失败、动作结果、影响和下一步 |
| `EmptyBusinessState` | 三个页签使用 | 按数据区使用 | 空态和单一下一步入口 |
| `BusinessDetailHeader` | - | 使用 | 编号、标题、状态、合同金额、责任人、当前节点、下一步和主动作 |
| `BusinessActionPanel` | - | 使用 | 后端动作、禁用原因和办理要求 |
| `EvidenceFileCards` | - | 使用 | 合同归档资料和下载能力 |
| `ApprovalTimeline` | - | 使用 | 审批与办理时间线 |
| `SensitiveActionDialog` | - | 使用 | 审批、归档确认、撤回、转审、委托和敏感下载二次确认 |

归档上传直接使用 TDesign Upload。合同变更/补充协议继续使用现有 TDesign Dialog 和原有资格复核，不新建组件。

## 5. Design Token 新旧映射

本轮以删除页面局部硬编码和重复样式为主，不改变全局品牌方向。

| 当前写法 | 目标 token/组件语义 |
| --- | --- |
| 自定义 `summary-strip` | `BusinessStatusSummary appearance="metrics"` |
| 自定义错误/成功消息条 | `BusinessFeedback` |
| 自定义页头 | `BusinessPageHeader` / `BusinessDetailHeader` |
| 硬编码颜色 | `--jg-color-*` 或 TDesign 语义状态 |
| 自定义 3px 圆角 | `--jg-radius-control` / `--jg-radius-panel` |
| 11px/12px 关键正文 | 12px 辅助信息、13px 摘要标签、14px 正文 |
| 常驻列设置边框条 | TDesign Popup + CheckboxGroup |
| 连续 Card 堆叠 | 标题、间距、浅色分区、必要边框和页签 |
| 自定义加载文本 Card | 与真实详情结构一致的静态骨架 |

## 6. 不得改变的业务逻辑与风险点

### 6.1 合同台账

- `loadContractLedger()` 仍只调用现有 `fetchContractLedger()`，不增加分页参数。
- `loadMyDrafts()` / `loadVoidedDrafts()` 继续调用原草稿接口和 scope。
- 默认页签仍为“我的草稿”，不改变合同经办人的日常入口。
- 项目 query 预填、按用户保存列偏好、详情/工作台/新建/历史接管路由保持不变。
- 五项统计仍来自响应 `summary`；筛选只作用于当前已加载记录。
- 失败时不得显示 0，也不得清空后宣称没有记录。

### 6.2 合同详情

- `availableActions`、`primaryAction` 和禁用原因是动作权限唯一来源，前端不得按角色或状态自行推断。
- `contractVersionId`、业务合同编号和合同记录 ID 不能混用。
- 董事长/总经理或签、自审确认、编号规则、用印、归档确认、当前密码、下载原因和文件权限必须完整保留。
- 文件上传仍先调用 `uploadPrivateFile`，再调用原 `uploadContractArchiveFile`，不修改上传 API。
- 合同变更资格读取、提交前重新复核、基版/金额/版本一致性检查和路由竞争保护保持原样。
- 合同变更归档确认时，替代前后金额和“历史结算付款不改写”提示必须保留。
- 结算与付款摘要、时间轴和台账只整理层级，不重新计算任何金额。
- 接口失败保留当前意见、密码、目标人员、下载原因或文件选择；只有成功后清理对应输入。

主要风险是合同详情动作数量和版本规则多。控制方法是先为标准页头投影、页签、基础信息去重和筛选选项补纯函数测试；模板移动时保留原 API 函数和 payload；结构测试禁止原生文件输入、`confirm`、`prompt` 回归；P0 E2E 继续验证审批、归档文件、版本金额和后续资金事实。

## 7. 页面修改前后结构

### 7.1 合同台账

修改前：

```text
自定义统计条（所有页签常驻）
  -> 页签
       合同台账：局部标题 + 两个入口 + 文本筛选 + 消息条 + 常驻列设置 + Card 表格
       我的草稿：消息条 + Card 表格
       已作废草稿：消息条 + Card 表格
```

修改后：

```text
BusinessPageHeader
  主动作：新建合同
  次级：历史合同接管
  -> 页签（保持默认“我的草稿”）
       合同台账：
         BusinessStatusSummary（仅此页签；失败显示 —）
         BusinessTableToolbar（结构化筛选、刷新、列设置）
         数据区（范围说明 / 错误重试 / 加载 / 表格 / 空态）
       我的草稿：
         数据区（错误重试 / 加载 / 表格 / 空态）
       已作废草稿：
         数据区（错误重试 / 加载 / 表格 / 空态）
```

项目、合同状态、归档状态和付款条款版本选项从当前已加载记录生成；关键词继续使用输入框。本地筛选即时生效，“刷新数据”才重新读取接口。列设置进入 TDesign Popup，不常驻占据整行。

### 7.2 合同详情

修改前：

```text
自定义页头
  -> 元信息面板
  -> 重复流程摘要
  -> 业务链路
  -> 版本历史
  -> 全部流程动作
  -> 归档资料
  -> 审批时间线
  -> 基础信息 / 生效流程 / 付款条款 / 结算付款连续平铺
```

修改后：

```text
BusinessDetailHeader
  编号 + 标题 + 状态 Tag
  合同金额 + 责任部门/处理人 + 当前节点 + 下一步
  当前用户可执行的后端 primaryAction（如存在）
  次级：发起变更/补充协议、刷新、审计
  -> tabs
       概览：基础信息、生效步骤、当前结算阻断说明
       流程办理：审批、辅助办理、用印、归档文件生成
       版本与条款：合同版本历史、付款条款版本
       凭证资料：TDesign 上传、归档确认、敏感下载、证据文件
       结算与付款：后端金额摘要、时间轴、结算台账、付款台账
       关联与审计：业务链路、审批时间线
  -> SensitiveActionDialog
  -> 原合同变更/补充协议 TDesign Dialog
```

页头主按钮只定位到对应办理页签，不绕过权限、密码或二次确认。没有可执行主动作时只显示后端下一步事实。加载使用静态骨架，详情未就绪时刷新和审计等依赖操作禁用。

## 8. 反馈、权限与状态语义

- 合同台账失败：统计显示“—”；明确读取失败不代表没有合同记录；保留重试。
- 草稿失败：说明当前草稿列表不可用于判断，用户可重试，已存在草稿不会因此丢失。
- 详情失败：区分无权限和普通读取失败，说明当前事实不可用于审批、用印、归档或结算判断。
- 加载：按钮禁用，显示静态占位，不旋转、不闪烁、不呼吸。
- 空态：每个页签最多一个主入口，不重复新建动作。
- 动作禁用：展示后端原因，不伪造可点击按钮。
- 动作失败：对话框保持打开并保留输入；说明发生了什么和下一步。
- 动作成功：关闭对应对话框、刷新后端事实并显示统一成功反馈。
- 状态必须有中文文字或 Tag，颜色不是唯一表达。

## 9. 验收标准

### 9.1 结构与视觉

- 两页不包含硬编码颜色、装饰阴影、渐变、动画或过渡。
- 页面标题不使用 Card；合同台账摘要为白底单元格与细分隔线。
- 普通正文 14px，辅助信息不低于 12px，关键标签不使用 11px。
- 金额列右对齐，操作列固定右侧，行内只有一个主要查看/进入动作。
- 合同详情关键事实原则上只出现一次；版本、条款、流程、凭证、资金和审计按页签组织。
- 所有原生文件上传、`confirm`、`prompt` 从合同详情移除。
- 1024px 桌面视口主内容不溢出页面；宽表允许在数据区内部横向滚动。

### 9.2 行为与业务

- 默认“我的草稿”、三页签加载逻辑、统计口径、本地筛选、列偏好和路由预填不变。
- 详情所有动作仍按后端动作元数据控制，禁用原因可见。
- 归档文件类型、大小、权限、提交顺序和 API 不变。
- 合同变更资格、基版复核、金额方向和版本一致性保护不变。
- 失败保留输入，成功刷新事实并按动作清理。
- 未生效合同不得从前端伪造结算入口；金额不在前端重算。

### 9.3 自动化与截图

完成后必须通过：

- 合同定向 Vitest。
- Web 全量 Vitest。
- `pnpm typecheck`。
- `pnpm lint`。
- `pnpm check:ui`，且两页纳入样板视觉和业务文案治理。
- 现有 P0 E2E。
- 新增合同 P1.2 Playwright，覆盖台账正常/失败/空态、草稿、详情概览/流程/版本/凭证/资金/加载，以及可稳定复现的禁用状态。

截图输出到仓库外：

- 1440×900：合同台账正常、失败、空态、我的草稿；合同详情概览、流程、版本与条款、凭证、结算与付款、加载。
- 1280×800：合同台账、我的草稿、合同详情。
- 1024×768：合同台账、我的草稿、合同详情。

## 10. 回滚方案

1. P0/P0.5 基线为 `e74444f8`，P1.1 结算迁移为 `30fb6234`，均与本轮分离。
2. 本计划先独立提交，合同 P1.2 实现再形成单独聚焦提交，不推送、不部署。
3. 回滚只反向应用本轮合同实现提交，不回退 P0/P1.1 公共组件、token 和结算页面。
4. 若视觉门禁阻断，修正合同页本身，不新增宽泛 allowlist 或原生文件输入例外。
5. 若动作回归，优先恢复原动作入口和 payload，同时保留已验证的标准页头、页签和反馈结构；禁止通过修改 API、权限或业务状态规避前端问题。

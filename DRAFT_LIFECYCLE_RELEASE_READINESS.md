# 建工智管全系统草稿生命周期治理发布准备审计

> 审计日期：2026-07-20
>
> 阶段：Task 18 阶段 C（获批候选已部署，生产全面试运行已开始）
>
> 执行分支：`codex/draft-lifecycle-governance`
>
> 运行时代码证据 SHA：`fae3200f7ab0f198f870aa699f813a92a886d1d1`
>
> 获批发布候选 SHA：`59166b42349edb5f7745dcdf914571a8bcbf9940`
>
> 阶段 B/C 证据文档提交 SHA：以本轮本地文档提交后的分支 `HEAD` 为准；该提交不改变已部署候选的运行时代码树，且未经新的精确 SHA 授权不得推送或部署
>
> 审计基线：`origin/main` = `c72c312978ddf77feff657a315a70cd1dd8cc882`

## 1. 发布结论

**Task 18 阶段 A、B 技术门禁和阶段 C 生产部署均已完成，系统已进入生产全面试运行。**

运行时代码已经通过 Task 17 的完整本地自动化、空 PostgreSQL 16 的 72 个迁移、回滚不变量探针和六档浏览器验证。阶段 B 又完成当前生产只读盘点、自然备份 COS 独立回读和候选绑定的 69→72 隔离迁移。用户随后明确选择向全部现有真实用户开放生产全面试运行，不再把真实岗位 UAT 作为部署前置；以下业务证据改为试运行期间的最终验收门禁，不能用自动化或开发者代签：

1. 对应岗位人员本人完成核心业务 UAT 并签认。

用户于 2026-07-20 再次明确授权把 `59166b42349edb5f7745dcdf914571a8bcbf9940` 作为生产全面试运行版本。Actions `29720027166` 已完成全部验证、发布前本地与异机备份、Web/API 部署和 M70–M72；**没有自动处理、批量清理或修改任何草稿。**

## 2. Git 与候选拓扑

审计开始时：

- 当前分支：`codex/draft-lifecycle-governance`。
- 运行时代码证据 HEAD：`fae3200f7ab0f198f870aa699f813a92a886d1d1`。
- `origin/main`：`c72c312978ddf77feff657a315a70cd1dd8cc882`。
- 相对 `origin/main`：主线独有 0 个提交，候选独有 22 个提交。
- 候选相对基线共 176 个变更路径；范围包括规格/计划/进度、共享域、M70–M72、目标领域 API、Web 页面与测试。
- 主工作树在审计开始时洁净。
- 另有独立 worktree `/Users/leoyang/.codex/worktrees/5023adf0-0a37-4255-83be-2a7884f9d19c/建工智管`，分支 `codex/spot-procurement`，HEAD `3ca9d812eee063982357b71f7d2ca28cb892e1c3`；本任务未修改该 worktree。

阶段 A 最终文档提交形成并获批候选 `59166b42349edb5f7745dcdf914571a8bcbf9940`。本轮阶段 B 收据将形成新的本地仅文档提交；必须单独报告其精确 SHA，且未经新的明确授权不得把该文档 SHA 推送或当作运行部署目标。

## 3. 数据库变化

相对 `origin/main` 只有三条新增迁移：

1. M70 `20260719210000_contract_settlement_draft_lifecycle`
2. M71 `20260719211000_payment_spot_draft_lifecycle`
3. M72 `20260719212000_template_draft_lifecycle`

审计结果：

- 只增加可空的放弃、失效、废弃、来源链字段，扩充状态约束并增加索引。
- 来源付款自关联使用 `ON DELETE RESTRICT ON UPDATE RESTRICT`。
- 没有业务 `INSERT`、`UPDATE`、`DELETE`，没有存量回填。
- 没有 `DROP TABLE`、`DROP COLUMN`、`TRUNCATE` 或级联删除。
- 为扩展允许状态而替换既有状态约束；迁移内先核验预期结构，避免在未知 Schema 上静默执行。
- 本地空库已经从 M1 顺序应用到 M72，`Prisma migrate status` 为最新；M70–M72 的字段、约束、索引和异常事实检查通过。
- 阶段 B 预部署时生产只读核验为 `69|0|0`，候选绑定隔离库已从 69 正确前向迁移至 72；阶段 C 部署后生产已为 `72|0|0`，M70–M72 均已执行成功。

## 4. 受保护业务事实审计

主执行者完成全部相对主线 diff 审计后，又进行一轮独立只读复核；复核未发现阻断 Task 18 文档提交的 P0 或 P1 问题，并把“项目经营路由入口”与“收货照片技术绑定删除”列为需要持续验证的 P2 发布说明。本节已纳入这两项边界。

### 4.1 HTTP 接口和业务根记录

- 新增终止动作全部使用语义化 `POST`：`abandonment`、`discard`、`draft-reset` 或批次 preview/apply。
- 没有新增 HTTP `DELETE` 业务根接口。
- 合同、结算、付款、采购、模板、审批、归档、实付、退款和审计的业务根记录均以状态转换和审计留痕结束，不物理删除。

### 4.2 唯一技术绑定删除例外

新增代码中存在一处 Prisma `delete`：重置**从未提交**的收货草稿时，删除 `SpotProcurementReceiptPhoto` 技术绑定行。该动作不是删除收货根单、收货修订或业务文件：

- 仅当前经办人/有效受托人在当前修订号 CAS 下可执行。
- 先检查未提交、未锁定、未复用及无其他业务文件绑定。
- 收货根单和旧修订保留，并创建新的空修订。
- 原始图和水印对应 `FileObject` 只转为 `quarantined`，不删除记录。
- 业务事务不调用 COS 删除；对象由独立技术保留策略处理。
- 每个文件和整次重置均写审计。

因此，“业务事实不硬删除”成立；不能把该技术绑定清理误写成“候选中不存在任何 SQL/Prisma delete”。

### 4.3 审批与状态机

- 相对 `origin/main` 没有修改 `services/api/src/approval`。
- 没有调整审批节点、审批人解析、自审、委托、转交、OR 签、归档确认或生效条件。
- 新动作只在领域服务确认当前责任人、当前状态、最新审批实例和阻断事实后开放。
- 已提交、签署、归档、实付或被正式引用的记录失败关闭。

### 4.4 金额、额度和元分转换

- Prisma 金额字段类型、默认值和元分表达未改变；Schema 中金额字段的差异仅为 `prisma format` 对齐。
- 没有新增前端金额计算或元分转换算法。
- 合同上限、结算占额、付款额度、项目资金池、供应商余额和实付算法未被替换。
- 合法释放只在后端事务内执行，并由锁、CAS、执行/退款/收货/归档事实复核保证恰好一次。
- Task 17 回滚探针精确比较合同、结算、付款、实付、文件对象的选定生命周期字段和金额摘要，强制回滚前后相同。

### 4.5 权限与路由

- 没有修改后端通用授权模块，也没有给 `super_admin` 增加业务结束权。
- 删除/放弃/废弃动作继续由领域服务核对当前责任人和既有岗位能力；前端只消费服务端 `availableActions`。
- Web 的“项目经营”路由外壳新增 `employee`、`material_staff`，目的是让已经拥有 `project_expense.create` 的人员进入同一页面发起项目支出；页面内部继续分别校验项目资金读取、项目支出台账读取和创建能力，后端权限没有扩大，员工仍不能读取资金总览。
- 该路由变化属于已有业务能力的入口可达性修正，不是新增资金读取权限；真实普通岗位 UAT 仍须验证不能越权看到资金和他人台账。

### 4.6 文件与 COS

- 没有修改上传 API、文件类型、大小、权限、私有桶或短时下载链路。
- 相对 `origin/main` 没有修改通用文件服务。
- 业务事务不物理删除 COS 对象。
- 审批、签署、用印、归档、实付凭证、收货锁定照片和下载审计继续保留。

## 5. Task 17 最终自动化证据

- Shared：12 文件，106/106 通过。
- API：184 套通过、4 套按既有环境条件跳过；4112 项通过、15 项跳过、0 失败。
- Web：100 文件，839/839 通过。
- Shared/API/Web typecheck 与 lint：通过。
- API/Web build：通过；Web 只有既有大 chunk 警告。
- API `check:business-errors`、Web `check:ui`、Web `typecheck:e2e`、`git diff --check`：通过。
- P0 E2E：57 通过、2 项按既有条件跳过、0 失败。
- 本地 PostgreSQL 16：全部 72 个迁移、Schema 最新、M70–M72 约束/索引/异常事实、强制回滚探针和清理均通过。
- 浏览器：1512×982、1440×900、1280×800、1180×820、1024×768、900×768 六档验证，无页面整体横溢出或嵌套横滚。
- 8 张专项截图位于被 Git 忽略的 `apps/web-admin/test-results/**/draft-lifecycle-*.png`。

上述测试精确绑定运行时代码证据 SHA `fae3200f7ab0f198f870aa699f813a92a886d1d1`。Task 18 只修改发布文档，因此文档提交后运行时代码树不变。

## 6. 当前生产盘点摘要

阶段 C 部署后已在固定 SQL 白名单和只读事务中重新形成当前生产 A/B/C/D 数量：

| 项目 | 数量 / 状态 |
| --- | ---: |
| 生产纯净草稿 A | 7 |
| 生产审批型草稿 B | 0 |
| 存在正式事实 C | 0 |
| 技术临时数据 D | 0 |
| 自动批量治理 | 未执行 |

7 条 A 类记录由合同草稿 5 条、零星付款草稿 1 条、零星收货草稿 1 条组成，均未处理。第 11.2 节保留部署前 6 条盘点，第 11.5 节记录部署后新增草稿与当前盘点。

## 7. 未解决问题和试运行验收项

### 最终业务验收

1. 生产部署已经完成；真实岗位 UAT 尚需由本人在全面试运行中完成并签认，不能由开发者或自动化代签。

### 上线后容量观察项

1. 付款/项目支出读取目前先形成完整可见集再内存分页，真实数据量增长前应改为数据库分组统计与分页。
2. 合同/结算分类会批量读取审批、文件和授权事实，需要在真实数据规模记录耗时。
3. 模板正式引用保护采用准确性优先的少量按版本只读查询，版本量增长后再批量化。
4. 长期草稿不自动结束，任何生产存量治理必须另行预览、授权和留存收据。

## 8. 实际发布顺序

以下受控顺序已经完成第 1–7 步，第 8 步进入生产全面试运行：

1. 推送候选分支并按主线策略快进/合并 `main`。
2. 重新核对目标 SHA、Actions 固定 SHA、工作树和迁移清单。
3. 生成发布前本地及异机数据库备份，验证 checksum、COS 收据和恢复可用性。
4. 从该生产备份创建隔离库，精确应用 M70–M72，复跑迁移和正式事实不变量。
5. 使用只读事务输出生产 A/B/C/D 盘点；不自动放弃任何记录。
6. 用户明确批准把真实岗位 UAT 调整为生产全面试运行期间的最终验收后，部署 Web/API 并执行生产迁移。
7. 核对运行 SHA、72 个迁移、API/Nginx/PostgreSQL/Cron、内外健康检查、COS 私有下载与审计。
8. 由真实账号执行一条最小链路；生产存量批量治理仍保持独立授权。

## 9. 回滚原则

- M70–M72 不做破坏性 down migration。
- 发布前必须备份并完成候选绑定的隔离恢复；失败时不进入部署。
- 若应用发布后需回退，先判断旧代码能否安全识别 `abandoned`、`invalidated`、`discarded`；不能识别时保持新代码并临时关闭新动作入口。
- 已产生的结束状态、来源链、金额释放、文件隔离和审计事实永久保留，不回写旧状态、不删列、不删除文件或审批记录。
- 生产存量治理与代码发布分离；回滚应用也不会自动恢复或删除任何已结束草稿。

## 10. 阶段 A 停止点

阶段 A 已按计划停止，并由用户随后精确批准候选 SHA。阶段 B 的预部署收据和阶段 C 的生产部署收据见下节；阶段 A 当时的停止条件已被后续明确授权取代。

## 11. 阶段 B 预部署执行收据

### 11.1 Git 和阶段 B 生产基线（历史）

- 批准 SHA：`59166b42349edb5f7745dcdf914571a8bcbf9940`。
- 候选分支与 `main` 已通过一次 `--atomic` 推送精确指向批准 SHA；推送后本地与 `origin/main` 为 `0|0`。
- 阶段 B 当时未触发 `Deploy Production` workflow。该历史基线随后已被第 11.5 节的阶段 C 部署取代。
- 生产实时运行 SHA：`797a8920b1ee67e13493edf15b4b008e820a83d9`。
- 生产数据库：`69|0|0`；最新迁移 `20260719100000_unified_file_business_binding_guard`。
- API、Nginx、PostgreSQL、Cron 均 active；内网 health 正常，公网 Web/API 均为 200。
- 根分区使用率 17%，可用内存约 2.88 GiB，NTP 同步正常。

### 11.2 生产只读 A/B/C/D 盘点

盘点使用候选自带的固定 SQL 白名单脚本，在生产只读事务中执行；输出仅含脱敏 ID，证据保存在服务器 root `600` 目录。

| 领域 | 总数 | A | B | C | D |
| --- | ---: | ---: | ---: | ---: | ---: |
| 合同草稿 | 4 | 4 | 0 | 0 | 0 |
| 零星付款草稿 | 1 | 1 | 0 | 0 | 0 |
| 零星收货草稿 | 1 | 1 | 0 | 0 | 0 |
| 接管、税务修订、结算、普通付款、项目支出、零星采购父单、模板版本和技术临时数据 | 0 | 0 | 0 | 0 | 0 |

盘点证据 SHA-256：`10767c7fb74796caa1c80cdb7eecaa1f957ec8e42f406f4e958d3c5322bf0861`。上述 6 条 A 类记录没有被自动放弃、迁移或清理。

### 11.3 备份、COS 回读和候选绑定恢复

- 使用 2026-07-20 03:00:01 自然生成的生产备份；本地 dump、checksum 和 `.offsite.json` 均为 root `600`。
- 备份大小 634,520 字节，`pg_restore --list` 为 1,091 项，SHA-256 为 `3a109565165f80bd9d5a508a9bb3e08da739809d960b6f27eb182406911eb8e9`。
- 备份监控 timer active/enabled，最近执行 `Result=success`、`ExecMainStatus=0`，持续确认 03:00 异机备份有效。
- 使用专用备份 CAM 从 COS 独立下载 dump 和 checksum；下载 SHA、checksum、结构清单和与本机副本逐字节比对全部通过。
- 恢复到隔离库 `jiangkong_restore_20260720_030001_59166b42`，原始 69 个迁移成功恢复，候选精确应用 M70–M72 到 72，`Prisma migrate status` 为最新。
- 候选恢复后核心计数：User 11、Project 1、Contract 4、ContractTakeover 1、Settlement 0、PaymentRequest 0、ProjectExpenseRequest 0、FileObject 15、AuditLog 204。
- 恢复证据 SHA-256：`60bf11e9f67037326e202a6b0b9383dc68316fdba083abdc729a83ce761f6d24`。
- 候选只读 Schema/约束/索引及正式事实核验通过，证据 SHA-256：`622e90042b5306532f4a8fc1897b90874aa63e09e8ce6a05e90851227ce13652`。
- 迁移前后 A/B/C/D 业务事实逐项相同，仅迁移数从 69 变为 72；迁移后盘点证据 SHA-256：`0048aa4f974ff0ff88f1e0dbf1d3df88cdef8b05d501436ea5b4fe3cc6d2c046`。

### 11.4 失败关闭与清理

- 第一次演练在恢复前因 `postgres` 无法穿越 `/home/ubuntu` 候选目录而失败，没有放宽目录权限。
- 第二次演练已恢复隔离备份，但在候选迁移前发现 Corepack 试图选择 pnpm 11；隔离库随即删除并重建为空库，再使用独立的 pnpm 9.15.9 缓存完整重跑。
- 最终成功后已删除隔离数据库、COS 下载副本、两个候选 worktree和临时 postgres pnpm 环境；仅保留 root `700/600` 的四份脱敏证据。
- 清理后隔离数据库计数为 0；生产仍是 `797a8920…` / `69|0|0`，服务与 health 正常。

### 11.5 阶段 C 生产部署与验证

- 用户明确授权以获批 SHA 直接进入生产全面试运行，允许部署 Web/API 和执行 M70–M72，不授权自动处理既有草稿或批量修改业务数据。
- Actions `29720027166` 精确绑定 `59166b42349edb5f7745dcdf914571a8bcbf9940`，2026-07-20 13:49:25（Asia/Shanghai）开始，14:01:42 完成，结论 `success`。
- Verify build 用时 10 分 23 秒：目标提交、依赖、Prisma、typecheck、Web E2E typecheck、lint、业务错误检查、部署安全自测、全量测试、API/Web build、`check:ui` 和生产构建 P0 E2E 全部通过。
- Deploy to server 用时 1 分 48 秒；发布前备份 `jiangkong-20260720-140133.dump` 为 636,948 字节、1,091 项，checksum 和异机收据通过。
- 生产运行 SHA 精确为 `59166b42349edb5f7745dcdf914571a8bcbf9940`，tracked 修改 0；数据库 `72|0|0`，M70、M71、M72 均完成。
- API、Nginx、PostgreSQL、Cron 均 active；内部 health 正常，公网 Web/API 200，HTTP 正确 301 跳转 HTTPS，未认证项目读取为 401，空登录请求为 400；发布后 15 分钟 API warning/error 为 0。
- 发布后只读盘点证据 SHA-256 为 `dfaa0f05f4dd3a1bf41ff26badd4a0eb7d07ca884696352ff01e34d212502e38`：合同 A 类 5 条、零星付款 A 类 1 条、零星收货 A 类 1 条，其余 A/B/C/D 为 0。
- 新增的第 5 条合同草稿创建于 2026-07-20 13:48:57，即工作流触发前约 28 秒，责任人启用、状态 `draft`；它不是迁移或部署创建。部署前原 6 条和该新增草稿均未被自动处理。

### 11.6 当前试运行边界

全部现有真实用户可以开始使用真实业务。前 3–5 个工作日作为生产观察期；真实岗位签认从“部署阻断”调整为“全面试运行最终验收”。首份合同、首份结算和首笔付款需加强人工复核；出现越权、金额错误、付款绕过或文件泄露时立即暂停对应业务。7 条 A 类草稿的删除、放弃或批量治理仍需由业务责任人按规则操作或另行授权。

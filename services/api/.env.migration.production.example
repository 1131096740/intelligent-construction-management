# Deploy-only environment file. Store as /etc/jiangkong/db-migration.env with
# root:root ownership and mode 0600. It must never be loaded by the API service
# or any systemd timer.
DATABASE_MIGRATION_URL=postgresql://jiangkong_owner:replace-with-owner-password@127.0.0.1:5432/jiangkong

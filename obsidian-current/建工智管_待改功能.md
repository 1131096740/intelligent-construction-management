# 建工智管 - 待改功能

同步时间：2026-07-15。本页只记录当前仍需处理的事项；已经完成的历史任务由 `PROGRESS.md` 和日期型状态报告保留，不再把大量完成项混入当前待办。

## P0：正式扩大使用前必须关闭

- [ ] 完成约 20 份真实历史合同的 A/B/C 分级、资料、余额来源、复核和接管确认。
- [ ] 使用真实岗位账号跑通 3-5 条合同、归档、结算、付款、实付、凭证和审计长链路。
- [ ] 完成合同母版 DOCX 逐页验收和真实业务场景/模板映射签认。
- [ ] 完成普通员工、项目岗位、部门主管、财务和管理层的跨项目权限 UAT。
- [ ] 对付款实付、项目费用实付和财务入账进行重复点击、网络重试和并发幂等测试；必要时增加后端幂等约束。
- [ ] 对数据库 `NOT VALID` 约束涉及的历史数据执行只读一致性检查，再决定受控验证迁移。
- [ ] 对 `pnpm audit --prod` 的 5 个中等级别公告逐项记录是否可达、处理方式和回归结果。
- [ ] 发布流程增加数据库备份成功门禁、目标 SHA 人工批准、深度健康检查和失败回滚演练。
- [ ] 深度健康检查覆盖 PostgreSQL、COS、API、磁盘和关键后台任务，不再只返回静态 `status=ok`。
- [ ] 固定下一次 UI 发布候选范围；当前响应式候选相对公网生产涉及 126 个文件，必须分阶段验收。
- [ ] 业务与技术共同形成明确的 Go / No-Go 结论；不额外开发签字审批模块。

## P1：受控试运行中收口

- [ ] 建立请求关联 ID、结构化请求日志、关键业务指标、前端异常上报和告警责任人。
- [ ] 完成 access/refresh token 存储和 CSP 的认证安全专项，不在普通 UI 任务中顺带重写认证。
- [ ] 优化 COS 大文件上传内存占用，评估 multipart/断点续传和轻量服务器并发容量。
- [ ] 将文件下载改为不在 URL 暴露下载原因和业务信息的短时效一次性票据。
- [ ] 将少量剩余原生按钮、输入框、文件控件和浏览器 `confirm` 纳入触达页面治理。
- [ ] 补充真实组件行为测试；现有部分 Vitest 仍偏结构/源码断言。
- [ ] 度量并优化 Web 主包体，优先评估 TDesign 全量注册和路由级拆包。
- [ ] 对超过 2,000 行的核心 Service/Page 按业务边界渐进拆分；试运行前不做全站重写。
- [ ] 建立数据库备份保留策略、异地副本和周期性恢复演练记录。
- [ ] 完成 HTTPS 证书自动续期的真实日志验证。

## 已批准、需单独执行的移动端收口

- [ ] 第一阶段：删除 `apps/miniprogram`、`POST /auth/wx-login`、微信供应商会话和有效密钥配置要求；保留手机号登录、共享后端 API、手机 Web、审批和手写签名二维码。
- [ ] 第一阶段生产验收后，只读核验 `wxOpenid` 非空数量。
- [ ] 第二阶段：只有非空数量为 0、最新备份和临时恢复演练通过并获得单独授权后，才执行字段/索引删除迁移。
- [ ] 完成手机 Web 的真实业务路径、触控、软键盘、文件上传和窄屏可用性验证。

## P2：首个真实项目稳定后再评估

- [ ] 合同到期、付款到账期和已批待付提醒。
- [ ] 用户上传原始归档件水印。
- [ ] 历史合同单价搜索和历史市场价辅助。
- [ ] OCR / Excel 辅助导入；只能辅助录入，不作为业务事实来源。
- [ ] 完整 OA、考勤、人事、安全、开票和经营驾驶舱。

## 明确不在当前阶段做

- 不引入第二套 UI 组件库、低代码运行时或通用工作流平台。
- 不进行全站机械改色、全量重构或数据库模型重写。
- 不让前端替代后端计算金额、权限或业务状态。
- 不因文档同步删除历史状态报告、迁移或 Git 历史。

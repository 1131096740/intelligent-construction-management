# 建工智管 - 数据库

当前版本采用 PostgreSQL + Prisma。旧版微信云数据库集合设计仅作为历史参考。

同步时间：2026-07-03。当前 `services/api/prisma/schema.prisma` 共 52 个 Prisma model。模型已覆盖合同工作台、项目经营、预付款、合同级付款、实付分摊/抵扣台账和项目支出；历史合同接管模型/余额初始化模型仍待补。

## 核心模型

### 组织、权限、项目

| Prisma model | 说明 |
| --- | --- |
| `User` | 用户账号、手机号、微信绑定、密码、个人签名 |
| `CompanyEntity` | 我方公司主体字典 |
| `RefreshToken` | 登录刷新会话 |
| `Department` | 部门 |
| `Position` | 岗位 |
| `UserPosition` | 用户岗位，一人可多岗 |
| `Project` | 项目 |
| `ProjectMember` | 项目成员和项目岗位 |

权限策略目前主要由 `packages/shared-domain/src/permissions.ts` 维护，后端 Guard 强制执行；不是单独依赖 `roles`、`permissions` 两张表。

### 合同主链路

| Prisma model | 说明 |
| --- | --- |
| `Contract` | 合同主档 |
| `ContractVersion` | 合同版本，支持原合同、变更、补充协议 |
| `PaymentTermsVersion` | 付款条款版本 |
| `PaymentTermsStage` | 多阶段付款条款 |
| `ContractArchiveFile` | 合同归档件 |

### 企业级合同工作台

| Prisma model | 说明 |
| --- | --- |
| `ContractBusinessTemplate` | 业务模板主档 |
| `ContractBusinessTemplateVersion` | 业务模板版本和字段/清单/条款/附件 schema |
| `StandardClause` | 标准条款主档 |
| `StandardClauseVersion` | 标准条款版本 |
| `BusinessParty` | 合作单位主档 |
| `BusinessPartyVersion` | 合作单位版本档案 |
| `ContractPartySnapshot` | 合同多方快照 |
| `ContractLayoutTemplate` | DOCX 版式模板主档 |
| `ContractLayoutTemplateVersion` | DOCX 版式模板版本 |
| `ContractLayoutPreviewJob` | 版式预览任务 |
| `ContractDraftCheckpoint` | 合同草稿检查点/自动保存 |
| `ContractNumberRule` | 合同编号规则 |
| `ContractBill` | 合同清单 |
| `ContractBillRow` | 合同清单行 |
| `ContractBillImport` | Excel 清单导入预览/应用记录 |
| `ContractGeneratedDocument` | 合同 DOCX/PDF 生成任务和结果 |
| `ContractOfflineRevision` | 线下 Word 修订稿上传记录 |

### 结算

| Prisma model | 说明 |
| --- | --- |
| `Settlement` | 结算单，绑定合同版本和付款条款版本 |
| `SettlementArchiveFile` | 结算归档件 |

### 付款

| Prisma model | 说明 |
| --- | --- |
| `PaymentRequest` | 付款申请 |
| `PaymentExecution` | 实际付款记录，支持分批付款 |
| `FinanceRecord` | 财务流水 |

付款凭证目前作为私有文件对象关联到实际付款记录，不是独立 `payment_vouchers` 模型。

### 审批

| Prisma model | 说明 |
| --- | --- |
| `ApprovalFlow` | 审批流定义 |
| `ApprovalFlowNode` | 审批节点定义 |
| `ApprovalInstance` | 审批实例，提交后冻结 |
| `ApprovalActionLog` | 审批动作历史 |
| `ApprovalDelegation` | 审批委托台账 |

### 文件、归档、PDF、审计

| Prisma model | 说明 |
| --- | --- |
| `FileObject` | 私有文件元数据，本地/COS 均走后端 |
| `ArchiveRecord` | 资料库归档索引 |
| `PdfDocument` | 自动生成 PDF 留档 |
| `AuditLog` | 审计日志 |

## 关键关联

结算单必须绑定：

- `contract_id`
- `contract_version_id`
- `payment_terms_version_id`

付款申请必须绑定：

- `settlement_id`
- `contract_id`
- `contract_version_id`
- `payment_terms_version_id`

实际付款必须绑定：

- `payment_request_id`
- `settlement_id`

合同生成文档、线下修订稿、清单、合作单位快照都必须绑定到明确的合同版本，避免历史单据被后续模板或条款修改影响。

## 状态设计

合同版本状态：

- `draft`
- `in_approval`
- `approval_rejected`
- `approved_pending_seal`
- `in_seal`
- `seal_approved_pending_archive`
- `pending_archive_confirm`
- `effective`
- `voided`

结算状态：

- `draft`
- `in_approval`
- `approval_pending`
- `approval_rejected`
- `withdrawn`
- `approved_pending_archive`
- `pending_archive_confirm`
- `effective`
- `partially_paid`
- `paid`
- `voided`

付款状态：

- `draft`
- `in_approval`
- `approval_rejected`
- `approved_pending_payment`
- `partially_paid`
- `paid`
- `voided`

## 金额控制

付款申请金额必须满足：

```text
本次申请金额
  ≤ 结算单可付金额
    - 已审批待付款金额
    - 已实际付款金额
```

该校验必须在后端事务中完成。API 响应中的 BigInt 金额字段已统一 JSON 序列化为十进制字符串，避免生产返回 500。

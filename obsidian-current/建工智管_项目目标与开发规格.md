# 建工智管 - 项目目标与开发规格

日期：2026-06-22

## 文档定位

本文是后续使用 superpowers / Codex 继续开发建工智管时的入口规格文档。

进入本项目开发时，优先阅读顺序：

1. `AGENTS.md`
2. `obsidian-current/建工智管_项目目标与开发规格.md`
3. `obsidian-current/建工智管_第一阶段MVP_产品与架构设计.md`
4. `docs/superpowers/plans/2026-06-19-jiangkong-phase1-mvp.md`

旧版 6 类审批、8 个角色、微信云数据库、41 个云函数只作为历史资料，不作为当前开发依据。

## 项目总目标

建工智管要建设成建筑企业项目经营管理系统，而不是单一微信小程序。

第一阶段目标是把企业最核心、风险最高的合同付款链路做成可审计、可追溯、可落地的闭环：

```text
合同编制
  -> 合同付款条款结构化录入
  -> 合同审批
  -> 用章审批
  -> 盖章合同归档确认
  -> 合同版本生效
  -> 结算审批
  -> 签章结算单归档确认
  -> 结算生效
  -> 付款申请
  -> 付款审批
  -> 审批通过待付款
  -> 出纳实际付款
  -> 付款凭证上传
  -> 财务流水
  -> PDF 留档
  -> 审计日志
```

系统成功的标准不是页面数量，而是后端能强制保证单据顺序、金额安全、版本追溯、文件私有、审批留痕。

## 第一阶段范围

第一阶段必须建设：

- 组织、人员、岗位、项目授权。
- 合同台账、合同版本、合同付款条款版本。
- 合同审批、用章审批、合同归档确认、合同版本生效。
- 结算创建、结算审批、结算归档确认、结算生效。
- 付款申请、付款审批、审批通过待付款。
- 出纳实际付款登记、付款凭证上传、财务流水生成。
- PDF 留档。
- 审计日志。
- 私有文件上传、权限校验、短时效下载地址。
- Web 管理端作为主系统。
- 微信小程序作为移动审批和移动查看端。

第一阶段暂不建设完整版本：

- 物资领料审批。
- 请假考勤。
- 人事入离调。
- 完整成本驾驶舱。
- 安全闭环。
- 节超奖励池。

这些模块只保留未来扩展点，不允许稀释第一阶段合同付款主线。

## 架构方向

### 总体架构

```text
apps/web-admin
  Vue 3 + TypeScript + TDesign Web + Vite
  企业后台主系统

apps/miniprogram
  原生微信小程序 + TDesign 小程序
  移动工作端

services/api
  Node.js + NestJS + PostgreSQL + Prisma
  业务规则、审批、权限、文件、PDF、审计中枢

packages/shared-domain
  角色、状态、金额、审批、读模型等共享领域定义

storage
  腾讯云 COS 私有桶

deploy
  腾讯云轻量应用服务器 + HTTPS + PostgreSQL + 自动备份
```

### 关键架构约束

- Web 和小程序都必须调用统一后端 API。
- 前端不得直连 PostgreSQL。
- 前端不得直连 COS。
- 所有状态流转、金额限制、权限判断必须由后端和数据库事务保证。
- Web 可以做提示和按钮禁用，但不能作为业务安全边界。
- 敏感文件必须经后端鉴权后生成短时效 URL。
- 所有关键动作必须写审计日志。
- 密钥不得提交到仓库，不得暴露到 Web 或小程序客户端。

## 核心业务规则

### 合同与条款版本

- 合同草稿创建时必须同时创建合同版本 v1 和付款条款版本 v1。
- 合同生效后不得直接编辑合同条款。
- 合同变更或补充协议必须走审批、用章、归档确认，然后生成新合同版本。
- 付款比例、账期、质保金、触发条件变化时，必须生成新的付款条款版本。
- 历史结算和付款必须保留创建时绑定的：
  - `contract_id`
  - `contract_version_id`
  - `payment_terms_version_id`

### 单据顺序

- 非 `effective` 合同版本不得创建结算。
- 非 `effective` 或 `partially_paid` 结算不得创建付款申请。
- 合同和结算必须归档确认后才可生效。
- 付款审批通过不等于实际付款。
- 付款审批通过后进入 `approved_pending_payment`。
- 实际付款必须由财务/出纳登记，并上传付款凭证。

### 付款金额控制

付款申请金额必须满足：

```text
本次申请金额
  <= 结算可付金额
     - 已审批待付款金额
     - 已实际付款金额
```

要求：

- 金额统一使用分为单位的整数。
- 金额校验必须在后端事务中执行。
- 同一结算允许多次付款申请。
- 同一付款申请允许多次实际付款执行，直到付清。
- 禁止仅在前端计算剩余额度。

### 审批规则

- 合同签订和合同变更需要董事长/总经理最终或签。
- 董事长和总经理节点为 OR-sign：任一人通过即通过。
- 结算审批不经过董事长/总经理。
- 所有付款审批都需要董事长/总经理最终或签。
- 合同归档件由合同部成员上传，合同部主管确认。
- 结算归档件由合同部成员上传，合同部主管确认。
- 财务可读取合同/结算归档件，但不负责上传业务归档件。

审批引擎必须支持：

- 审批流定义。
- 审批实例冻结。
- 条件节点。
- 会签。
- 或签。
- 转审。
- 委托/代理。
- 撤回。
- 驳回到上一节点。
- 打回申请人。
- 超时催办。
- 全审批历史。

## 角色体系

使用真实业务岗位，不沿用旧泛角色：

| 角色 key | 中文名 | 第一阶段职责 |
| --- | --- | --- |
| `chairman` | 董事长 | 合同最终审批、付款最终审批，与总经理或签 |
| `general_manager` | 总经理 | 合同最终审批、付款最终审批，与董事长或签 |
| `project_manager` | 项目经理 | 项目合同、结算、付款审批 |
| `contract_director` | 合同部主管 | 合同/结算审核、归档确认 |
| `contract_staff` | 合同部成员 | 编制合同、录入条款、上传归档件 |
| `budget_director` | 预算部主管 | 成控会签、金额和付款条件审核 |
| `budget_staff` | 预算部成员 | 协助合同和结算资料 |
| `finance_director` | 财务总监 | 合同、结算、付款财务审批 |
| `finance_staff` | 财务人员/出纳 | 实际付款登记、凭证上传 |
| `material_director` | 物资主管 | 物资类合同和结算节点 |
| `material_staff` | 物资员 | 物资类结算数据和收货证据 |
| `engineering_director` | 项目总工 | 劳务/专业分包合同和结算节点 |
| `engineering_foreman` | 工长 | 劳务/专业分包结算收方数据 |
| `engineering_tech` | 工程技术部 | 高额合同审核、劳务/专业分包结算审核 |
| `comprehensive_director` | 综合部主管 | 用章审批节点 |
| `employee` | 普通员工 | 本人事项和基础查看 |
| `super_admin` | 系统管理员 | 技术管理、初始化、权限维护，不代表业务审批岗位 |

## 当前实现状态

截至 2026-06-22，项目不是从零开始，已经有早期可运行骨架。

### 已完成或已有雏形

- Monorepo 已建立。
- GitHub 远端已配置并推送。
- Docker Desktop 已安装，本地 Docker 可用。
- `services/api/docker-compose.yml` 已提供 PostgreSQL 16 容器配置。
- `packages/shared-domain` 已有：
  - 角色定义。
  - 合同/结算/付款状态定义。
  - 金额校验。
  - 审批动作/节点模式。
  - 核心读模型。
- `services/api` 已有：
  - NestJS 模块骨架。
  - Prisma schema。
  - 合同草稿创建服务。
  - 合同详情读服务。
  - 结算创建前置规则。
  - 结算详情读服务。
  - 付款申请金额校验。
  - 付款详情读服务。
  - 付款执行、财务流水、PDF 留档相关 DTO/服务雏形。
  - 审批节点冻结服务。
  - seed 数据和核心链路验证脚本。
- `apps/web-admin` 已有：
  - 企业后台布局。
  - 合同、结算、付款、资料库、审计日志页面。
  - 合同/结算/付款详情页。
  - 核心读 API 客户端。
  - 页面配置测试。

### 明确未完成

- 数据库 migration 需要稳定整理。
- Prisma schema 需要补关系、索引、约束和枚举策略。
- 组织、人员、岗位、项目授权仍是空模块或薄模块。
- 登录、员工绑定、会话和权限中间件未完成。
- 完整审批引擎未完成。
- 合同提交审批、用章、归档确认、生效状态机未完整闭环。
- 结算提交审批、归档确认、生效状态机未完整闭环。
- 付款审批到实际付款的事务闭环未完整落地。
- 文件上传、COS 私有桶、短时效下载 URL 未完成。
- PDF 生成未完成。
- 审计日志只处于早期服务状态，需要接入关键动作。
- 小程序端尚未建立。
- 生产部署、HTTPS、备份、密钥管理未完成。

## 后续开发总路线

### Milestone 1：本地可运行业务闭环

目标：在本地 Docker PostgreSQL 上跑通合同、结算、付款读写闭环。

优先事项：

1. 固化 Prisma schema。
2. 建立 migration。
3. seed 一套完整核心链路数据。
4. 确保 API 可以读取合同、结算、付款详情。
5. Web 详情页接真实 API。

验收：

- `docker compose up -d` 可启动 PostgreSQL。
- Prisma migrate 可执行。
- seed 可重复执行。
- 核心链路验证脚本通过。
- Web 端合同/结算/付款详情能从 API 读取数据。

### Milestone 2：合同状态机

目标：合同从草稿到生效由后端强制控制。

优先事项：

1. 合同提交审批。
2. 审批通过后进入待用章。
3. 用章审批通过。
4. 合同部成员上传盖章合同归档件。
5. 合同部主管确认归档。
6. 合同版本进入 `effective`。

验收：

- 非有效合同不能创建结算。
- 未上传归档件不能确认归档。
- 非合同部主管不能确认合同归档。
- 生效合同不能直接修改付款条款。
- 所有关键动作写审计日志。

### Milestone 3：结算状态机

目标：结算从有效合同版本创建，并在归档确认后生效。

优先事项：

1. 仅允许从 `effective` 合同版本创建结算。
2. 结算审批按合同类型选择节点。
3. 结算审批通过后进入待归档。
4. 合同部成员上传签章结算单。
5. 合同部主管确认结算归档。
6. 结算进入 `effective`。

验收：

- 非有效合同版本不能创建结算。
- 非有效结算不能创建付款申请。
- 结算绑定原合同版本和付款条款版本。
- 财务可读取结算归档件，但不能上传业务归档件。

### Milestone 4：付款审批与实际付款

目标：付款审批和实际付款彻底分离。

优先事项：

1. 从有效结算创建付款申请。
2. 后端事务校验付款申请剩余额度。
3. 付款审批通过后进入 `approved_pending_payment`。
4. 财务/出纳登记实际付款。
5. 上传付款凭证。
6. 生成财务流水。
7. 更新付款申请和结算已付金额。
8. 生成 PDF 留档。

验收：

- 付款审批通过不更新为已付款。
- 超额付款申请被后端拒绝。
- 实际付款必须有凭证。
- 支持同一结算多付款申请。
- 支持同一付款申请多次实际付款。
- 所有金额更新在数据库事务内完成。

### Milestone 5：审批引擎完善

目标：审批流可配置、实例可冻结、历史可追溯。

优先事项：

1. 审批流定义表和节点表落地。
2. 提交业务单据时冻结节点。
3. 支持会签和或签。
4. 支持条件节点。
5. 支持驳回上一节点。
6. 支持打回申请人。
7. 支持撤回。
8. 支持转审和委托。
9. 支持催办记录。

验收：

- 已发起审批不受审批模板后续修改影响。
- 董事长/总经理或签节点任一通过即可流转。
- 会签节点所有人通过才可流转。
- 每个动作有操作人、动作、意见、时间。

### Milestone 6：文件、PDF、审计、安全

目标：达到企业资料安全和审计要求。

优先事项：

1. 接入 COS 私有桶。
2. 文件上传只经过后端。
3. 文件下载走权限校验和短时效 URL。
4. 合同、结算、付款凭证形成资料库记录。
5. 关键单据生成 PDF。
6. 登录、审批、归档、付款、下载、权限变更写审计。
7. 敏感操作二次确认。

验收：

- 前端拿不到 COS 永久公开地址。
- 非授权岗位不能下载敏感文件。
- 每次敏感下载有审计记录。
- PDF 可从单据详情追溯。

### Milestone 7：小程序移动端

目标：移动端只做移动工作入口，不承载复杂配置。

优先事项：

1. 微信登录与员工绑定。
2. 待我审批。
3. 审批详情。
4. 附件预览。
5. 审批同意/驳回/退回。
6. 移动端上传必要附件。
7. 消息提醒。

验收：

- 小程序不直连数据库或 COS。
- 小程序不承担复杂权限配置。
- 小程序审批动作与 Web 共用同一后端审批引擎。

### Milestone 8：部署上线

目标：可在腾讯云轻量应用服务器上稳定运行。

优先事项：

1. 生产环境变量和密钥管理。
2. HTTPS。
3. PostgreSQL 不开放公网。
4. COS 私有桶。
5. 数据库备份。
6. 附件备份策略。
7. 日志和错误监控。
8. 上线初始化脚本。

验收：

- 生产密钥不在代码仓库。
- 数据库公网不可访问。
- 每日备份可恢复。
- 关键服务重启后可恢复。

## Superpowers 开发方式

后续每次进入开发任务时：

1. 先读 `AGENTS.md` 和本文。
2. 如果任务涉及代码理解，仓库有 `.codegraph/`，先用 CodeGraph 定位。
3. 修改业务规则前先写测试。
4. 金额、状态流转、权限、版本追溯必须测试覆盖。
5. 每个任务只做一个明确目标，不顺手重构无关代码。
6. 执行计划优先使用 `superpowers:subagent-driven-development` 或 `superpowers:executing-plans`。
7. 完成前使用 verification 规则跑对应命令。

建议验证命令：

```bash
/Users/leoyang/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm test
/Users/leoyang/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm typecheck
/Users/leoyang/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm lint
```

涉及数据库时追加：

```bash
cd services/api
docker compose up -d
/Users/leoyang/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm --filter @jiangkong/api exec prisma generate
/Users/leoyang/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm --filter @jiangkong/api seed
/Users/leoyang/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm --filter @jiangkong/api verify:core-flow
```

## 后续 agent 禁区

- 不要把系统重新退回旧微信云开发架构。
- 不要使用旧 6 审批流作为当前真相。
- 不要把 `super_admin` 当业务审批人。
- 不要只做前端按钮禁用来替代后端规则。
- 不要把付款审批通过直接当成已付款。
- 不要让合同/结算未归档就生效。
- 不要让历史单据引用最新付款条款版本。
- 不要提交密钥、数据库密码、COS 密钥。
- 不要做固定 IP 白名单作为主要安全方案。

## 下一步推荐任务

下一步最值得做的是 Milestone 1 的收口：

1. 确认 Docker PostgreSQL 可启动。
2. 补 Prisma migration。
3. 让 seed 和 `verify:core-flow` 在本地数据库稳定通过。
4. 更新文档中的“当前实现状态”。
5. 再进入合同状态机。

推荐后续指令：

```text
使用 superpowers，把 Milestone 1 本地可运行业务闭环做完；先写测试和验证脚本，再改 Prisma/API。
```


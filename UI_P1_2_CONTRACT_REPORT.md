# 建工智管 UI P1.2 合同管理样板迁移实施报告

> 完成日期：2026-07-14
> 实施范围：合同台账、合同详情、直接配置与测试、UI 治理规则
> 结论：本地实施与验收完成，未推送、未部署、未修改生产

## 1. 实施结论

本轮按 `UI_P1_2_CONTRACT_PLAN.md`，把 P0/P0.5 与 P1.1 已验证的企业级后台结构迁移到合同台账和合同详情。合同台账已统一为“标准页面头—业务页签—白底摘要—筛选工具栏—数据区”；合同详情已统一为标准详情头和“概览、流程办理、版本与条款、凭证资料、结算与付款、关联与审计”六个任务页签。

本轮没有重新设计合同业务，也没有修改合同工作台。后端 API、数据库、路由、角色权限、董事长/总经理或签、审批状态、用章、归档确认后生效、合同变更版本、历史版本追溯和金额事实均未改变。合同金额、结算与付款数据继续直接展示现有后端事实，页面只调整信息层级和交互表达。

## 2. 实际修改文件

### 文档与进度

- `UI_P1_2_CONTRACT_PLAN.md`：实施前范围、业务风险、文件清单、验收和回滚方案；完成后更新实施状态。
- `UI_P1_2_CONTRACT_REPORT.md`：本报告。
- `PROGRESS.md`：记录 P1.2 本地完成事实、验证结果和未发布边界。

### 页面与配置

- `apps/web-admin/src/pages/contracts/ContractListPage.vue`
- `apps/web-admin/src/pages/contracts/contract-list.config.ts`
- `apps/web-admin/src/pages/contracts/contract-list.config.test.ts`
- `apps/web-admin/src/pages/contracts/ContractDetailPage.vue`
- `apps/web-admin/src/pages/contracts/contract-detail.config.ts`
- `apps/web-admin/src/pages/contracts/contract-detail.config.test.ts`

### 治理与浏览器回归

- `apps/web-admin/scripts/check-ui-rules.mjs`：合同台账和合同详情纳入样板视觉与中文业务文案治理。
- `apps/web-admin/e2e/p0-browser-smoke.e2e.ts`：把合同详情旧摘要/平铺断言更新为标准详情头和页签结构，业务断言未弱化。
- `apps/web-admin/e2e/ui-p1-contract-visual.e2e.ts`：新增合同台账、草稿、详情和异常状态的稳定视觉回归。

未修改 `design-tokens.css`、公共组件契约、任何 API 文件、路由、合同工作台、合同变更工作台、历史合同接管或后端文件。

## 3. 公共组件复用结果

| 公共组件 | 使用位置 | 本轮职责 |
| --- | --- | --- |
| `BusinessPageHeader` | 合同管理 | 标准标题、说明、唯一“新建合同”主入口和历史接管次级入口 |
| `BusinessStatusSummary` | 合同台账 | 白底统计单元格、细分隔线和失败占位 |
| `BusinessTableToolbar` | 合同台账 | 项目、合同状态、归档状态、付款条款版本和关键词筛选 |
| `BusinessFeedback` | 两页 | 统一读取失败、动作结果、影响和下一步 |
| `EmptyBusinessState` | 台账、草稿和详情数据区 | 中性空态；新建入口不与页头重复 |
| `BusinessDetailHeader` | 合同详情 | 编号、标题、状态、合同金额、责任人、当前节点、下一步和主动作 |
| `BusinessActionPanel` | 详情“流程办理” | 后端动作、禁用原因和办理要求 |
| `EvidenceFileCards` | 详情“凭证资料” | 合同归档资料和受控下载能力 |
| `ApprovalTimeline` | 详情“关联与审计” | 审批和办理历史 |
| `SensitiveActionDialog` | 合同详情 | 审批、归档确认、撤回、转审、委托和敏感下载二次确认 |

没有增加无业务意义的 Button、Input、Select、Card 或 Table 包装，也没有引入第二套组件库。

## 4. Token 与视觉规则变化

本轮没有新增或修改全局 Design Token。现有 P0 token 已覆盖合同页面需求：

| 旧页面表达 | 收口后 |
| --- | --- |
| 自定义页头、六格元信息和重复流程摘要 | `BusinessDetailHeader` 单一事实区 |
| 全页常驻色块统计 | 仅合同台账显示白底统计单元格与细分隔线 |
| 自定义错误/成功消息条 | `BusinessFeedback` |
| 原生文件输入 | TDesign Upload，继续使用原文件规则 |
| 浏览器 `confirm` / `prompt` | `SensitiveActionDialog` |
| 连续多层 Card | 标题、间距、页签、浅色分区和必要边框 |
| 页面局部颜色、字号、圆角和间距 | 现有 `--jg-color-*`、`--jg-font-*`、`--jg-radius-*`、`--jg-space-*` |

页面保持白色主背景、蓝色唯一主操作色；绿、橙、红只用于对应状态。普通面板无阴影，未新增动画、过渡、渐变、玻璃拟态或装饰性效果。

## 5. 页面前后差异

### 5.1 合同台账

修改前没有标准页面标题，五项摘要在三个页签常驻；结构化筛选配置实际渲染为自由文本；查询与刷新混用；错误、列设置、表格和范围说明各自形成容器。

修改后：

1. 标准页面头统一承载“新建合同”和“历史合同接管”，局部工具栏不再重复入口。
2. 保持默认进入“我的草稿”，合同摘要只在“合同台账”页签展示。
3. 项目、合同状态、归档状态和付款条款版本从当前响应生成稳定 Select 选项，关键词保留输入框。
4. 本地筛选即时生效；“刷新数据”才重新读取现有接口。
5. 加载失败时摘要显示“—”，明确失败不代表没有合同记录，并保留重试。
6. 台账、草稿和作废草稿使用一致的数据区、表格、错误和空态结构。
7. 金额列右对齐，操作列固定右侧，行内只保留一个“查看详情”动作。
8. 当前接口没有服务端分页契约，页面如实说明当前响应范围，没有前端伪分页。

### 5.2 合同详情

修改前的自定义页头、元信息、流程摘要、业务链路、版本、动作、资料、审批、基础信息、条款和资金台账纵向连续平铺，状态、金额、责任人和下一步多次重复。

修改后：

1. 标准详情头一次展示合同编号、标题、状态 Tag、合同金额、责任人/部门和下一步。
2. 后端存在可执行 `primaryAction` 时，页头只显示该主动作；点击只定位到对应办理页签，不绕过权限或二次确认。
3. 刷新和审计为次级动作；变更/补充协议继续使用原资格、基版和版本一致性校验。
4. 六个页签按业务任务组织概览、生效流程、版本条款、归档资料、结算付款和审计记录，不再无限平铺。
5. 加载状态使用与真实详情结构一致的静态骨架；详情未就绪时刷新、审计等依赖动作禁用。
6. 原生文件控件替换为 TDesign Upload；文件类型、大小限制、权限、先上传私有文件再绑定合同归档的顺序和 API 均未改变。
7. 审批、归档确认、撤回、转审、委托和下载统一使用 `SensitiveActionDialog`；失败保持对话框与当前输入，成功后才清理对应内容。
8. 结算与付款页签只展示系统已核定金额和记录，不重新计算可付额度，也不改写历史合同版本。

## 6. 删除的重复信息与容器层级

| 原重复或多层结构 | 处理结果 |
| --- | --- |
| 元信息面板与流程摘要重复状态、金额、处理人和下一步 | 合并到标准详情头 |
| 业务链路既在首屏又在审计区出现 | 只保留在“关联与审计” |
| 合同金额在页头、摘要、基础信息重复 | 页头展示；概览基础信息去重；资金页仅在资金关系中因业务计算链必要出现 |
| 合同版本、付款条款、动作、资料、审批、资金连续 Card | 按六个任务页签渐进披露 |
| 台账摘要在草稿/作废页签继续出现 | 仅合同台账显示 |
| 台账空态和页头重复“新建合同” | 空态引导使用页头唯一入口 |
| 多个页面自定义状态消息 | 统一为 `BusinessFeedback` |

保留边框的区域仅包括表格、复杂动作表单、上传资料和需要明确业务边界的区块。页头、普通事实组和版本列表主要通过间距、标题与分隔线建立层级。

## 7. 保持不变的业务边界

- `GET /contracts`、`GET /contracts/:contractId`、草稿列表和合同变更资格接口地址与参数未变。
- 审批提交/处理、撤回、催办、转审、委托、用章、PDF、归档上传/确认和私有文件下载接口及负载未变。
- `availableActions`、`primaryAction`、`disabledReasons` 仍是动作权限唯一来源，页面没有按角色猜测动作。
- 董事长/总经理或签、自审确认、编号规则、当前密码、下载原因和审计链路继续保留。
- 合同审批、用章、归档确认后生效；未生效合同不得创建结算；合同变更形成新版本再生效等规则未变。
- 历史结算与付款继续引用原 `contract_version_id` 和付款条款版本。
- 合同变更提交前仍重新读取资格，并保持请求竞争、基版、金额方向和响应一致性保护。
- 合同工作台的正文、计价、清单、条款、自动保存和文档画布均未触碰。

## 8. 测试与检查结果

| 检查 | 结果 |
| --- | --- |
| 合同配置定向 Vitest | 2 文件，18/18 通过 |
| Web 全量 Vitest | 78 文件，597/597 通过 |
| `pnpm typecheck` | 通过 |
| `pnpm lint` | 通过 |
| `pnpm check:ui` | 通过；合同两页已纳入样板视觉和中文业务文案治理 |
| 现有 P0 浏览器冒烟文件 | 4 项：2 通过、2 项按现有真实账号环境变量条件跳过、0 失败 |
| P1.2 合同视觉用例 | 1/1 通过，生成 16 张截图 |
| 人工截图复核 | 1440、1280、1024 下页头、摘要、筛选、表格内部滚动、页签、禁用动作、上传和静态加载符合样板方向 |

应用内浏览器已成功连接本地应用，但现有会话停在“修改初始密码”强制流程。为避免触碰真实账号密码，本轮没有提交该表单；合同页面交互、异常状态与截图均通过仓库 Playwright 的隔离 Mock 数据完成。

## 9. 截图位置

截图根目录：`/Users/leoyang/.codex/visualizations/2026/07/14/ui-p1-2-contract`

### 1440×900

- `contract-drafts-normal-1440x900.png`
- `contract-ledger-normal-1440x900.png`
- `contract-ledger-failure-1440x900.png`
- `contract-ledger-empty-1440x900.png`
- `contract-detail-overview-1440x900.png`
- `contract-detail-process-1440x900.png`
- `contract-detail-versions-1440x900.png`
- `contract-detail-evidence-1440x900.png`
- `contract-detail-funds-1440x900.png`
- `contract-detail-loading-1440x900.png`

### 1280×800

- `contract-drafts-1280x800.png`
- `contract-ledger-1280x800.png`
- `contract-detail-1280x800.png`

### 1024×768

- `contract-drafts-1024x768.png`
- `contract-ledger-1024x768.png`
- `contract-detail-1024x768.png`

## 10. 人工视觉复核结论

1. 台账页头保持一个主动作，历史接管、刷新、重置和列设置均为次级操作。
2. 五项统计为白底单元格与细分隔线，颜色只用于数字，不存在大面积状态色块。
3. 1024px 下筛选会自然换行，宽表在数据区内部横向滚动，页面主体没有横向溢出。
4. 详情状态、合同金额、责任人和下一步集中在页头，概览不再重复展示金额和状态。
5. 版本、条款、流程、凭证、资金和审计按任务切换，首屏信息密度高但不堆叠。
6. TDesign 上传、统一敏感对话框、后端禁用原因和静态骨架均与 P0/P1.1 样板一致。
7. 截图捕获增加真实页面标题与稳定绘制帧校验，避免同路由详情切换时把未完成重绘误认为视觉缺陷。

## 11. 未解决问题与后续迁移建议

### 已知但不在本轮解决

- 合同台账 API 目前没有可确认的服务端分页契约。本轮只说明当前响应范围；若真实数据量成为阻塞，应先扩展和验证后端分页契约。
- 1024px 下合同台账业务列较多，保留表格内部横向滚动，以避免金额、状态和责任人被压缩到不可读。
- 应用内浏览器真实会话需要完成初始密码修改后才能进行真实账号只读 UAT；本轮未代替用户处理真实凭据。
- 合同工作台、合同变更工作台和历史接管页面未迁移，也没有借本轮清理其历史代码。
- 本轮没有推送、部署、修改数据库或连接生产。

### 推荐后续顺序

1. 先由合同经办人和合同主管用真实账号对合同台账、详情页签、禁用原因和归档入口做一轮只读/办理 UAT。
2. 下一批不要机械换肤合同工作台；应先单独审计“创建草稿—正文—计价—清单—条款—就绪检查—提交”的高风险写入链，再分小切片迁移外壳和录入效率。
3. 合同工作台稳定后，再迁移归档台账和审批中心等跨域支撑页面，继续复用相同反馈、敏感动作和表格规范。
4. 每批页面都必须加入 `check:ui` 与浏览器状态回归，不能只做截图或扩大 allowlist。

## 12. 回滚说明

P0/P0.5 样板基线为 `e74444f8`，P1.1 结算实现为 `30fb6234`，P1.2 计划提交为 `fc91aa2b`。本轮实现将形成独立聚焦提交；如需回滚，只反向应用该实现提交，不回退 P0/P1.1 公共样板，不涉及数据库、API 或生产数据恢复。

# 建工智管 UI P1.1 结算管理样板迁移实施报告

> 完成日期：2026-07-14
> 实施范围：结算台账、结算详情、直接公共组件、测试与 UI 治理规则
> 结论：本地实施与验收完成，未推送、未部署、未修改生产

## 1. 实施结论

本轮按 `UI_P1_1_SETTLEMENT_PLAN.md` 将 P0/P0.5 已验证的企业级后台结构迁移到结算台账和结算详情。两页已经从各自维护的页头、统计、反馈、动作和卡片样式，收口为共享业务组件、现有 `--jg-*` token、TDesign 基础控件和稳定的业务状态语义。

本轮没有重新设计结算业务，也没有修改结算工作台。后端 API、数据库、路由、角色权限、审批节点、业务状态、归档生效规则和金额事实均未改变。结算金额、可付金额、已申请、已实付和剩余可申请继续直接展示后端读模型，前端没有建立第二套计算口径。

## 2. 实际修改文件

### 文档与进度

- `UI_P1_1_SETTLEMENT_PLAN.md`：实施前范围、业务边界、验收和回滚计划；完成后仅更新实施状态与准确文件清单。
- `UI_P1_1_SETTLEMENT_REPORT.md`：本报告。
- `PROGRESS.md`：记录 P1.1 本地完成事实、验证结果和未发布边界。

### 页面与配置

- `apps/web-admin/src/pages/settlements/SettlementListPage.vue`
- `apps/web-admin/src/pages/settlements/settlement-list.config.ts`
- `apps/web-admin/src/pages/settlements/settlement-list.config.test.ts`
- `apps/web-admin/src/pages/settlements/SettlementDetailPage.vue`
- `apps/web-admin/src/pages/settlements/settlement-detail.config.ts`
- `apps/web-admin/src/pages/settlements/settlement-detail.config.test.ts`

### 公共组件与治理

- `apps/web-admin/src/components/BusinessDetailHeader.vue`：新增可选 `amountLabel`，默认仍为“申请金额”；结算详情传入“结算金额”，付款详情行为不变。
- `apps/web-admin/scripts/check-ui-rules.mjs`：结算台账和结算详情移出迁移豁免，纳入样板视觉与中文业务文案治理。

### 浏览器回归

- `apps/web-admin/e2e/p0-browser-smoke.e2e.ts`：把结算详情旧结构断言更新为标准详情头和页签结构，仍验证后端金额、禁用动作、归档文件等原业务事实。
- `apps/web-admin/e2e/ui-p1-settlement-visual.e2e.ts`：新增稳定的结算台账/详情视觉与状态回归。

未修改 `design-tokens.css`、结算 API、路由配置、结算工作台和任何后端文件。

## 3. 公共组件复用结果

| 公共组件 | 使用位置 | 本轮职责 |
| --- | --- | --- |
| `BusinessPageHeader` | 结算台账 | 标准标题、说明和唯一主入口 |
| `BusinessStatusSummary` | 结算台账 | 白底统计单元格、细分隔线、失败占位 |
| `BusinessTableToolbar` | 结算台账 | 项目、合同、节点、归档状态和关键词筛选 |
| `BusinessFeedback` | 两页 | 统一失败、影响、下一步和重试语义 |
| `EmptyBusinessState` | 结算台账 | 中性空态和一个明确主入口 |
| `BusinessDetailHeader` | 结算详情 | 编号、标题、状态、结算金额、责任部门、当前节点、下一步和动作 |
| `BusinessActionPanel` | 详情“流程办理” | 后端动作、禁用原因和办理要求 |
| `EvidenceFileCards` | 详情“凭证资料” | 归档文件、状态和受控下载 |
| `ApprovalTimeline` | 详情“关联与审计” | 审批及办理时间线 |
| `SensitiveActionDialog` | 结算详情 | 审批、归档确认、撤回、转审、委托和敏感下载二次确认 |

没有新增无业务意义的 Button、Input、Select、Card 或 Table 包装组件，也没有引入第二套组件库。

## 4. Token 与视觉规则变化

本轮没有增加或修改全局 Design Token。现有 P0 token 已覆盖本轮需求，页面局部硬编码被映射到既有变量和 TDesign 状态：

| 旧页面写法 | 收口后 |
| --- | --- |
| 页面内硬编码蓝、绿、橙、红和灰色 | `--jg-color-*` 与 TDesign 语义状态 |
| 自定义 3px 圆角 | 控件、面板、浮层既有 `--jg-radius-*` |
| 11px 关键标签 | 12px 辅助信息或 13px 摘要标签 |
| 自定义统计色块 | 白底 `BusinessStatusSummary` + 分隔线 |
| 自定义错误/成功长条 | `BusinessFeedback` |
| 卡片阴影和多层边框 | 间距、标题、浅色分区和必要边框 |
| 任意局部间距 | `--jg-space-*` 与既有布局 token |

页面保持白色主背景；蓝色仅承担主操作和品牌强调，绿/橙/红只表达对应状态；普通面板无阴影，未新增动画、渐变、玻璃拟态或装饰性效果。

## 5. 页面前后差异

### 5.1 结算台账

修改前由自定义页头、色块统计、常驻规则条、文本筛选条、消息条、原生列设置和表格卡片七段连续组成。修改后收口为四层：

1. 标准页面头，唯一主动作仍是进入原结算工作台。
2. 白底统计摘要；读取失败时所有统计显示“—”，不以 0 冒充业务事实。
3. 默认收起的结算规则提示与标准筛选工具栏。
4. 台账数据区，统一承载范围说明、错误、重试、静态加载、表格和空态。

筛选不再把项目、合同、节点和归档状态都当作自由文本，而是根据当前已加载记录生成稳定选项；关键词仍支持模糊查询。筛选继续只作用于当前已加载记录，“刷新数据”才重新读取接口。操作列固定右侧，行内仅保留“查看详情”。

### 5.2 结算详情

修改前的元信息面板、流程摘要和多个业务卡片反复展示状态、责任部门、下一步和结算金额，并将动作、资料、明细、金额、付款规则和审计无限平铺。

修改后：

1. 标准详情头一次展示编号、标题、紧凑状态 Tag、结算金额、责任部门、当前节点和下一步。
2. 当前用户存在后端允许的主动作时，页头主按钮只负责定位到对应办理区域；它不绕过权限和二次确认。
3. “概览、流程办理、结算明细、凭证资料、关联与审计”五个页签按任务组织信息。
4. 读取阶段使用与真实页头、页签、内容区一致的静态骨架，依赖详情的刷新和审计操作禁用。
5. 归档上传使用 TDesign Upload；文件类型、大小、权限、先上传私有文件再绑定结算归档的原提交链路不变。
6. 所有敏感动作统一进入 `SensitiveActionDialog`，原生 `confirm`、`prompt` 和文件控件已移除。

## 6. 删除的重复信息与容器层级

| 原重复/多层结构 | 处理结果 |
| --- | --- |
| 详情元信息面板与流程摘要重复状态、责任部门、下一步、金额 | 合并到标准详情头，各关键事实只展示一次 |
| 页头刷新被误作主动作 | 刷新和审计保持次级；主动作来自后端 `primaryAction` |
| 动作总卡片内嵌审批、归档、协助多个边框组 | 收入“流程办理”，按当前任务展示 |
| 归档、审批、基础信息、生效、金额、明细、付款规则连续卡片 | 按五个页签渐进披露 |
| 台账规则整条常驻 | 改为默认收起的紧凑业务提示 |
| 台账列设置整行常驻 | 改为 TDesign 弹出层 |
| 错误提示与正常数据说明并列 | 错误进入数据区，失败时正常范围事实不显示 |

台账外层由七段并列结构收口为四层；详情从连续多卡片平铺改为一个标准页头、一个导航和单个当前任务区。保留边框的区域只包括表格、复杂表单、上传资料和需要明确责任边界的业务区块。

## 7. 异常、空态和权限语义

- 台账读取失败：统计显示“—”；明确“读取失败不代表没有结算记录”，当前页面不可用于判断数量或状态，并提供重试。
- 台账正常：继续显示基于当前响应的数据范围；没有伪造服务端分页。
- 台账空态：只提供一个“进入结算工作台”的主动作。
- 详情读取失败：说明发生了什么、当前事实是否可用于判断、返回台账或重试的下一步。
- 详情加载：静态骨架，无旋转、闪烁、呼吸、位移或淡入效果。
- 无权限或动作禁用：展示后端禁用原因，不根据前端角色猜测，也不伪造可点击按钮。
- 动作失败：保留密码、意见、原因或上传选择等当前输入；仅成功后关闭对话框并刷新详情。
- 所有状态同时使用中文文字或 Tag，颜色不是唯一表达。

## 8. 保持不变的业务边界

- `GET /settlements` 和 `GET /settlements/:settlementId` 的地址、参数和返回事实不变。
- 审批、撤回、催办、转审、委托、归档上传/确认、文件下载和 PDF 接口及负载不变。
- `availableActions`、`primaryAction`、`disabledReasons` 仍是动作权限的唯一事实来源。
- 结算归档确认后生效、未生效不得付款、历史版本绑定原付款条款等规则不变。
- 结算和付款金额不在前端重新计算；页面只整理后端读模型的表达层级。
- 路由地址、列偏好、项目 query 预填和进入结算工作台的原跳转不变。
- 结算工作台的创建、清单选行、Excel 导入、计价和提交逻辑不在本轮范围内。

## 9. 测试与检查结果

| 检查 | 结果 |
| --- | --- |
| 结算配置定向 Vitest | 2 文件，19/19 通过 |
| Web 全量 Vitest | 78 文件，592/592 通过 |
| `pnpm typecheck` | 通过 |
| `pnpm lint` | 通过 |
| `pnpm check:ui` | 通过；两页已移出迁移豁免并纳入样板治理 |
| 现有 P0 E2E（含新增 P1 视觉用例） | 25 项：23 通过、2 项按现有环境条件跳过、0 失败 |
| P1.1 视觉用例单独执行 | 1/1 通过，生成 11 张截图 |
| 人工截图复核 | 页头、统计、筛选、金额对齐、页签、禁用动作、上传控件和静态加载均符合样板方向 |

P0 E2E 中原结算详情断言已同步到新结构，但业务断言没有弱化：仍检查下一步、后端“剩余可申请”、无办理权限提示和归档文件。

## 10. 截图位置

截图根目录：`/Users/leoyang/.codex/visualizations/2026/07/14/ui-p1-1-settlement`

### 1440×900

- `settlement-ledger-normal-1440x900.png`
- `settlement-ledger-failure-1440x900.png`
- `settlement-ledger-empty-1440x900.png`
- `settlement-detail-overview-1440x900.png`
- `settlement-detail-process-1440x900.png`
- `settlement-detail-evidence-1440x900.png`
- `settlement-detail-loading-1440x900.png`

### 1280×800

- `settlement-ledger-1280x800.png`
- `settlement-detail-1280x800.png`

### 1024×768

- `settlement-ledger-1024x768.png`
- `settlement-detail-1024x768.png`

## 11. 人工视觉复核结论

1. 页头与主动作：台账和详情都只保留一个清晰主任务，刷新、审计和辅助动作降为次级。
2. 信息密度：摘要改为白底单元格与分隔线，页面没有大色块 KPI 或装饰卡片。
3. 台账层级：筛选、错误、表格和范围说明落在同一数据任务区，失败不再与正常说明冲突。
4. 详情去重：状态、金额、责任人和下一步集中在页头，概览不再重复堆叠相同事实。
5. 渐进披露：流程、明细、凭证和审计使用页签，不需要纵向扫描全部模块。
6. 敏感办理：禁用原因明确，TDesign 上传与敏感对话框保持统一，不出现浏览器原生控件。
7. 加载表现：静态骨架与真实结构一致，加载时按钮不可用且无任何动效。

## 12. 未解决问题与后续迁移建议

### 已知但不在本轮解决

- 结算台账 API 当前没有可确认的服务端分页契约。本轮如实展示当前响应范围，没有使用前端切片伪装分页；后续若数据量成为真实阻塞，应先扩展并测试后端分页契约，再接入前端。
- 1024px 桌面宽度下，业务列较多的台账保留表格内部横向滚动，避免压缩金额和状态到不可读宽度。
- Codex 内置浏览器本轮因运行时 `Cannot redefine property: process` 无法建立连接，已使用仓库现有 Playwright 完成同等自动化与截图验收。
- 本轮没有迁移 `SettlementWorkbenchPage.vue`，其业务复杂度和写入风险应单独立项，不得夹带在台账/详情视觉迁移中。

### 推荐后续顺序

1. 先让业务人员用结算台账和详情进行一轮只读/办理 UAT，确认信息顺序、禁用原因和凭证入口符合实际操作。
2. 下一批优先迁移与真实试运行主线直接相关的合同台账和合同详情；继续复用同一页头、摘要、工具栏、详情页签和反馈语义。
3. 合同工作台、结算工作台等复杂写页面独立评审，先验证业务缺陷和录入效率，不做机械换肤。
4. 每迁移一批页面，都必须从 UI checker allowlist 移除并加入自动化治理，不能只靠截图验收。
5. 归档、审计、组织权限等支撑页面放在核心合同—结算—付款链路之后，按页面类型成批迁移。

## 13. 回滚说明

P0/P0.5 样板基线提交为 `e74444f8`，P1.1 计划提交为 `171a025a`。本轮实现将形成独立聚焦提交；如需回滚，只反向应用 P1.1 实现提交，不回退 P0 公共组件和 token，也不涉及数据库、API 或生产数据恢复。

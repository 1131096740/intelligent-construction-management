# 建工智管 - 当前进度看板

> 本文件是项目的唯一进度登记入口，只保留当前结论、正在推进的事项、门禁和下一步。
>
> `AGENTS.md` 负责业务与工程规则；详细规格、运行证据和历史记录分别进入 `docs/specs/`、`docs/progress/` 与 GitHub Issues。
>
> 接手开发时先读本文件，再读 `AGENTS.md` 和相关领域文档；任何状态结论仍须与代码、Schema、迁移、测试、Git/CI、部署和生产只读证据交叉核验。

图例：`[x]` 完成 · `[~]` 已完成当前范围但有残余风险/持续观察 · `[ ]` 未完成

---

## 当前结论（更新至 2026-08-19）

- [x] 上线修复候选：`733ddb8192b95d11043c67da8b6e3965ec784680`。
- [x] 业务发布合并提交：`308c47b51c368a4573c9857411e59a872e1e5062`。
- [x] 最终上线决策：**Conditional Go**。
- [x] 精确 SHA CI、PostgreSQL 16 动态门、四类 `--require-ready`、RC-06 隔离业务长链、真实岗位 Chromium/WebKit 浏览器门、生产健康/备份只读核验及同机隔离部署 -> 回滚 -> 再部署均已通过。
- [x] 同机回滚演练仅使用 `127.0.0.1` 临时槽位、隔离数据库和本地文件存储，未修改正式生产业务数据。
- [~] 当前仅证明同一主机上的版本回滚能力；整机故障、跨主机接管及 DNS/入口网络故障转移未演练，继续作为已知残余风险。
- [x] 完整发布收据：[`docs/progress/2026-08-05-go-live-conditional-go.md`](docs/progress/2026-08-05-go-live-conditional-go.md)。

## 当前正在推进

- [x] POL-19A-P17（Issue #191，2026-08-19）：从 fresh `origin/main@ecf1b65dae366eb517675a460d32295e2583a047` 的独立 clean worktree 仅修 page-action 检查器的严格 `server_definition` 服务端能力证明；registry 固定为 `kind=server_definition`、`source=definition.key` 且拒绝 `key`，同页同一 `fetchBusinessEntryDefinition` GET provenance 必须写入同一 ref，trigger 必须由该 ref `.key` 的 `v-if`/祖先 `v-if` 支配，handler 必须 fresh GET 并保留既有 wrapper causal chain。未知 kind、`.version`、任意对象 key、静态/不同 ref、无 v-if/仅 disabled、客户端 boolean、角色判断、错误 transport、缺 fresh GET 均 fail-closed。仅修改 `scripts/lib/whole-site-page-action-manifest.mjs` 与其最窄 Node 测试，未修改业务页面/API/auth transport、shared-domain、Schema/迁移、权限或派生 manifest。验证：page-action Node 全量 81/81、CLI 3/3、Web API manifest 32/32、官方 release manifests（API build、480 routes、465 wrappers/487 bindings、250 actions、capability matrix）通过；workspace typecheck、lint（0 errors，30 个未改动 takeover 页面 warning）、API/Web build、中文业务语言、API 业务错误检查与 `git diff --check` 通过。未部署、未访问生产、未写入生产数据库/COS；focused commit/push、单一 PR `Closes #191`、精确 head CI、merge 与 Issue 关闭待收口。
- [~] POL-19A-P15（Issue #188，2026-08-19）：从 fresh `origin/main@0af9ad9fe306ec8b41b36965c668a333f99c6c77` 的独立 worktree 仅修 page-action 检查器对已登记本人资料 facade `apps/web-admin/src/lib/user-self-profile.ts#updateProfile` 与既有 `PATCH /auth/profile` auth transport exception 的精确映射；未修改业务页面/API、auth transport、`services/api`、`packages/shared-domain`、Schema/迁移或派生 manifest。TDD、page-action 聚焦/CLI `83/83`、Web API 聚焦 `32/32`、Web API `465 wrappers/487 bindings`、page-action `250 actions/0 blockers`、route usage `480/0`、capability matrix `480/0`、authority `80/22`、typecheck、lint、Web build、UI/中文业务语言/API 业务错误检查均通过；lint 保留 30 个既有 Web warning，0 errors。未部署、未访问生产、未写入生产数据库/COS。
- [ ] #188 的 focused conventional commit、push、单一 PR `Closes #188`、精确 head CI、merge/main SHA 与 Issue CLOSE 待完成；合并不代表 #187/#113 完成。

- [x] POL-19A-P27（Issue #202，2026-08-19）：从 fresh `origin/main@46973e4a559579437f07a1cf1e87ea802712f776` 的独立 clean worktree 仅新增 `apps/web-admin/src/pages/business-parties/business-party-p22-governance.structure.test.ts`；JSON helper 使用显式最小 `RetirementManifest`/`WebApiManifest` 类型，断言七项冻结 wrapper 保持 `test_only`、默认关闭/未挂载 Drawer 与内部测试调用不形成 production consumer、真实 business-party 读取链保持 production。TDD 先以唯一 `no-explicit-any` 得到 lint RED，再以显式类型 GREEN；窄测 3/3，官方 Web API generator/check/ready `465 wrappers / 487 bindings`，Prisma generate、workspace typecheck、lint（0 errors，30 条既有 takeover 页面 warning）、`check:ui` 和 `git diff --check` 通过。未改生产源码、API、Schema、权限、manifest 分类或生产状态；focused commit、push、PR `Closes #202`、精确 head CI、merge 与 Issue CLOSED 待收口。

- [x] POL-19A-P28（Issue #204，2026-08-19）：从 fresh `origin/main@7108dea64a9eafc29c9198476f96c1046e0d7f00` 的独立 clean worktree，仅收窄 `master-data-readonly-isolation.structure.test.ts` 对 business-party 的旧“不可创建”断言，保留 business-party 查询与历史读取证明，以及 CompanyEntity/Organization 只读和 #202 七项 wrapper `test_only` 治理边界。TDD 以 test-only fixture 重放 `createBusinessParty`/“创建合作单位”导致旧正则 RED，再最小 GREEN；本票窄测 3/3、#202 P22 回归 3/3、组织只读结构 4/4、主数据响应式 3/3、改动文件 lint、Web typecheck 和 `git diff --check` 通过。仅改测试与本登记，未改页面、组件、API、shared-domain、Schema、权限、resolver、生成器、派生 manifest 或生产状态；focused commit、push、仅 `Closes #204` 的 PR、固定 head CI、merge 与 Issue CLOSED 待收口。

- [x] 依赖审计安全维护（2026-08-16）：从 `origin/main@bd1b414981328458246699fe9653cd66d0570c54` 在独立分支复核原 5 个 moderate advisory。以最小 pnpm overrides/lockfile 升级 `postcss` 8.5.18→8.5.26、`file-type` 20.4.1→21.3.2（覆盖两条 advisory）、`uuid` 8.3.2→11.1.1；重新审计仅剩 `@nestjs/core` 10.4.22 的 SSE advisory。静态复核确认项目无 `@Sse`/SSE 路由，升级要求 Nest 11 major，未强行迁移；该项记录为当前代码路径 not_actionable，后续纳入 Nest major maintenance。全量测试 shared 176、Web 1957、API 6177 passed/100 skipped，typecheck/lint/build/UI 规则通过；真实 ExcelJS 写缓冲和 Nest FileTypeValidator smoke 通过。未部署、未访问生产、未执行生产数据/数据库写入。

### P0：仓库与协作控制面收敛

- [x] 根工作区恢复为干净 `main`，历史偏离状态已保存在独立 archive/recovery 分支，没有覆盖用户改动。
- [x] 旧脏 worktree 已先建立可恢复快照，再移除物理目录；恢复分支不得直接合并。
- [x] 建立 `CONTEXT-MAP.md`、文档导航、Issue/分诊/领域文档规则，并明确 `docs/superpowers/` 仅为历史资料。
- [x] 仓库控制面 PR #21 已合并为 `fc855cfef0d2ad0629cc7cc7dc6b9253993e7332`，且未触发部署。
- [x] GitHub 使用五态标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。
- [~] 根 `PROGRESS.md` 已压缩为实时看板；完整旧内容原样保存在历史快照中。
- [x] Issue #27（`pdfjs-dist` / CVE-2026-16633）已合并关闭：PR #28 已 squash 合并至 main（SHA `55956d350979878bfc7d293e8f33f01ceda76625`），GitHub Issue #27 已 `CLOSED`（COMPLETED）。直接依赖从 `6.1.200` 升至厂商修复下限 `6.2.108`，满足 GHSA-hq66-cqwq-w95j / CVE-2026-16633 修复下限；本地与远端 `pnpm audit --prod --audit-level high` 均无 high（仍有 5 个既有 moderate）；Web 全量测试、类型检查、lint、生产构建、UI 规则及精确 SHA PostgreSQL 16 动态门全部通过；远端 CI 复跑确认唯一失败仍为既有 #23 manifests blocker（`CAPABILITY_EXIT_CANDIDATE_CONSUMER_PRESENT`，非本票引入），该债务已单独立项 Issue #29 并修复关闭，见下条。
- [x] Issue #29 已合并关闭：PR #30 已 squash 合并至 main（SHA `1a2f6d762ece1551fb2f0fc0ca7dfbc013c41ed8`，合并提交 `1a2f6d76`），GitHub Issue #29 已 `CLOSED`（COMPLETED）。修复内容：校正 `route-usage.registry.json`（移除 `GET /contracts/lifecycle-ledger` 的 `exit_candidate` override、新增 `GET /contracts/workbench` override 并同步 `classificationOverrideSha256`）、校正 `retired-web-api-wrappers.json`（core-flow-read `test_only` 登记由 `fetchContractLifecycleLedger` 改为 `fetchContractWorkbenchLedger`），并按序重生成 web-api/page-actions/route-usage/capability-matrix/contract-workbench-capabilities 五个派生 manifest（同时合法同步 #9 迁移以来一直未重生成的既有 drift：core-flow-read 各 export 的 sourceLine 偏移、ContractDocumentPage 页面动作 sourceLine/角色补全、capability-matrix 输入 SHA 等，均以 live 源码为准）。门禁证据：`inspect:contract-workbench-capabilities` `matched`（不再抛 `CAPABILITY_EXIT_CANDIDATE_CONSUMER_PRESENT`）、`inspect:whole-site-web-api:ready`（432 wrappers / 452 bindings）、`inspect:whole-site-page-actions:ready`（241 actions / 0 blockers）、`inspect:whole-site-route-usage:ready`（446 routes / 0 unclassified）、`inspect:whole-site-capability-matrix:ready`（446 routes / 0 blockers）全部通过；全量 API jest 5850 + web-admin vitest 1949 + typecheck + lint 全部保持通过；远端 PR #30 CI Release gates 全 job success（含 `Verify capability and usage manifests` 首次全绿）。仅校正 manifest/registry 描述层，未改业务代码、Schema、权限模型、生产数据与 COS。
- [x] Issue #31 已合并关闭：PR #32 已 squash 合并至 main（SHA `61675ec7f16fb20538e4c8a4e515d7f817dfbb7b`，合并提交 `61675ec7`），GitHub Issue #31 已 `CLOSED`（COMPLETED）。背景：GitHub 自 2025-09-19 弃用 runner 上的 Node.js 20，CI `release-gates` 日志出现 `actions/checkout`/`actions/setup-node` 被强制跑在 Node 24 的弃用警告。修复内容：升级三个第三方 action 至 node24 运行时（覆盖 `ci.yml` 与 `deploy-production.yml` 全部 5 处引用）——`actions/checkout` v4→v7.0.1（SHA `3d3c42e5`）、`actions/setup-node` v4→v7.0.0（SHA `82076278`）、`actions/upload-artifact` v4→v7.0.1（SHA `043fb46d`）；`node-version: 20`（构建用 Node）保持不变，仅 action 自身运行时升至 node24。验收证据：PR #32 Release gates 全 job success（19/19 步骤）；该 run 日志（12552 行）检索无 `Node.js 20 is deprecated` / `forced to run` 警告（此前 #30 日志存在该警告）。仅改 workflow 描述层，业务代码 / Schema / 依赖清单不受影响。
- [~] Issue #34（CI 编排提速）已在独立分支 `codex/ci-orchestration-acceleration` 实施并通过远端验收，待合并：静态/manifest 与精确 SHA PostgreSQL 16 动态门拆为两个无依赖并行 job，并保留 `always()` 的 `Release gates` 汇总 job；新增 `inspect:release-manifests` 聚合入口，API 仅构建一次后直接执行六项 manifest 校验（四项仍带 `--require-ready`），保留当前基线 119 migrations / 55 tests / 29 files 动态门。最终提交 `11cd8f959b67056b9b73061a2ef9ff0fceee8639` 的 run `31254903963` 全绿（静态 11m53s、PG16 13m32s、summary 4s），PG16 收据绑定最终 merge-ref `3723829edbbb5395a059e0f173b0b93e2ad98005`，`remainingTests=0`；首轮由 2026-08-07/08 新公开的 nanoid 高危 advisory 阻断，已补最小 pnpm override 至 `3.3.17`（当前 audit 仅 5 个既有 moderate）。未改生产数据库、生产数据或 COS 生命周期规则。
- [x] Issue #48（本地快速检查与耗时收据）已由 PR #51 合并至 main（merge SHA `e248e8a48769f1b6e76644ff7d4d3eb5c831bc9a`）：日常 `pnpm check:fast` 在 Mac 分流检查；完整 `pnpm release:local` 仍在 Mac 生成 schema v2、15 阶段耗时收据。PR/push 没有触发 GitHub Actions，未连接生产。
- [x] Issue #49（本地收据绑定的低分钟手动生产部署）已由 PR #52 合并至 main（merge SHA `a131188418b6913041f02ad1658d3bb28c173f58`）：`pnpm deploy:local` 会校验完整本地收据、精确 HEAD、干净工作树与当前 `origin/main` 后才手动请求 deploy-only workflow；`pnpm deploy:mac-direct` 保留为显式授权备用入口。workflow 仅 `workflow_dispatch`、串行排队且不取消在途发布；服务器端构建、备份、迁移、健康检查、确认与恢复链保持不变。该记录仅说明源码和命令契约已合并，不声明已执行真实 GitHub workflow、SSH 或生产部署。
- [x] 2026-08-12 CI 恢复与继续提速候选基于 `origin/main@202a1e12b70d34d6e486ff1680220ff2b6a2a10c`，PR #125：恢复 PR / `main` push 自动 CI，将质量、全量单元测试、构建与清单拆为 3 个无依赖静态 job，并将 PostgreSQL 16 动态门按现有 9 个清单组拆成并行 matrix，最后由稳定 `Release gates` 汇总；数据库 runner 新增受清单约束的单组执行参数，未知组和重复组失败关闭，未改变无参数时的完整本地门。本地 CI 编排契约 51/51、API Jest 6044 passed / 76 skipped、Web Vitest 1948/1948、shared-domain 158/158、typecheck、lint、Web build 与 UI 规则均通过，生产依赖审计仅 5 个既有 moderate。首轮真实 Actions run `31607075212` 暴露 `contract_settlement_v2` 手工测试表漏同步 M125 两列，已仅修正测试夹具；精确候选 `b6a25e57fd79849ba6170a142047838a2c351e7a` 的该分片以 125 个迁移、2/2 测试本地通过（29,964 ms）。最终远端 run `31607789567` 绑定 `95b80b533ec59b1296ed2d4c1e67297c63afe7e8`：质量 1:30、全量测试 2:31、PostgreSQL 16 九个分片全部通过（1:21 至 4:15），从创建到汇总 4:32；相较恢复前约 14:55 的同类 Actions run，墙钟缩短约 70%。该 run 的发布清单因 `origin/main` 继承的 9 个 blocker 正确失败关闭。2026-08-13 已按独立授权在同一分支修复这 9 项：清单检查器现验证草稿页 `draft.lifecycle` 到命令及真实写包装器的绑定链，并强制最终合同写入前的合同、版本、服务端动作、内容修订、内容指纹和正整数聚合修订坐标；同名影子对象、改接命令、删包装器或逐项删改任一坐标均会变红。派生 page-action 为 237 actions / 0 blockers，capability matrix 为 452 routes / 0 blockers；清单测试 78/78、CI 编排 51/51、合同域 Web 558/558、Web typecheck/lint/UI 规则及完整 `inspect:release-manifests` 均通过。实现提交 `5b3e74a8a3e2621cc384e6502e1d90892808e4ae` 已推送，绑定该 SHA 的 Actions run `31620285487` 中质量、全量单元测试、构建与清单、9 个 PostgreSQL 16 分片及稳定 `Release gates` 全部通过，总墙钟约 5 分钟；本条后续提交仅登记收据，不改变上述实现与门禁结论。PR #125 已 squash 合并至 `main`（SHA `4c8de0eb5872e776b725f60bf2cd55fbfff3cd35`），绑定该合并 SHA 的 main Actions run `31621763407` 全部通过；未部署、迁移、访问生产或修改业务数据。
- [ ] 文档收敛 PR 合并后，复核最终 worktree/分支拓扑并归档旧 Codex 会话。

### P0：项目经营总账、施工企业经营与历史接管

- [x] 业务规则已完成连续澄清并冻结为正式规格：[`docs/specs/2026-08-12-project-operating-ledger-construction-enterprise-takeover-unified-entry.md`](docs/specs/2026-08-12-project-operating-ledger-construction-enterprise-takeover-unified-entry.md)。规格明确项目合并经营账与多公司主体分账、施工企业资金位置、三层经营口径、成本与付款分离、历史接管、统一业务录入、中文业务语言硬门、测试业务归零和直接真实使用边界。
- [x] 已在精确 `origin/main@202a1e12b70d34d6e486ff1680220ff2b6a2a10c` 上完成只读差距审计：[`docs/progress/2026-08-12-project-operating-ledger-latest-main-gap-audit.md`](docs/progress/2026-08-12-project-operating-ledger-latest-main-gap-audit.md)。审计确认当前主线具备部分可复用基础，但尚无来源唯一经营总账、统一主体分账与资金/利润/对账模型，不能据此宣称该范围已具备上线条件。
- [x] 实施架构、替换边界、迁移顺序、自动化硬门和候选任务已拆分：[`docs/specs/2026-08-12-project-operating-ledger-implementation-plan-and-task-breakdown.md`](docs/specs/2026-08-12-project-operating-ledger-implementation-plan-and-task-breakdown.md)。候选清单为 1 个父任务、POL-01 至 POL-26 共 26 个子任务；POL-19 另含 5 个有界页面迁移子任务，这组编号已用于下述 GitHub Issues。
- [x] 本组文档已取得可引用提交 `25ce9817e49f62cc2049a7b1d121163a5e10879b`；GitHub 父任务 #93、POL-01 至 POL-18（#94–#111）、POL-19（#112）、POL-19A 至 POL-19E（#113–#117）及 POL-20 至 POL-26（#118–#124）已按 `Part of` 和 `Blocked by` 关系创建。当前仅无阻塞的 #94（POL-01）与 #120（POL-22）带 `ready-for-agent`。
- [x] POL-01（Issue #94）已在 `origin/main@a2d782c949453da498d6a12f05bd9a8b7835d197` 的独立分支完成并由 PR #127 squash 合并至 `main`（merge SHA `226d58154ff329b481a0cfab685c48aa4b1eef0a`），Issue #94 已 CLOSED（COMPLETED）。交付 shared-domain 经营事实、影响、主体种类/角色、A/B/C 证据等级、施工企业扣费生命周期、七阶段、财务对账状态、八个不可改名一级成本分类及施工企业扣费受控二级分类规则；新增 API 中文失败关闭解析器和穷尽契约测试。目标 shared-domain 测试 6/6、API 契约测试 17/17、shared-domain typecheck/lint、API typecheck/lint 已通过；全量 shared-domain 163/163、Web 1948/1948、API 沙箱外 6060 passed / 76 skipped；本地 Prisma Client 仅为 typecheck/test 前置生成，未执行迁移或数据库写入。Standards/Spec 双轴复审已完成（有效 finding 1 项，已修复并重跑验证）；未改 Schema/迁移、页面、生产配置，未触碰 #120 的 scripts/Prisma 验证/runbook 路径，未归零、部署或访问生产。
- [~] Issue #120（POL-22）以开工基线 `origin/main@a2d782c949453da498d6a12f05bd9a8b7835d197`（后续 fresh rebase 到 `origin/main@6f1c4e4a30233c82a3c90487cdbff01a37f2cecb`）实现测试业务归零预检与受控执行工具。旧候选（最新为 `c2e7f820057d6c1ab0130bd686c52af995e72742`）及对应动态/审查收据均已作废；本轮按 v8 有效 findings 收紧：从 Prisma model/field AST 建立逐表可空 lifecycle/event/status 注册表，所有已知终态字段非空和未知 lifecycle-like 字段均 fail-closed；数据库删除/CAS/锁内后检后在事务回调返回前再次验证外部 write-freeze lease；完成只有在 `Serializable` 事务内写入完整 `completed` 审计、独立 `terminal_committed` 权威 marker 并在提交前再次验证租约后才可验收，降级写失败也不能让旧完成内容成为权威；本地对象 rename 前先耐久写入 typed `object_recovery_planned` disposition、随机 quarantine key 和剩余范围；dry-run 也拒绝过期预检。执行身份绑定实际 Node executable、完整 `@prisma/client`、生成 Prisma Client/query engine 字节并拒绝符号链接/非普通文件；受信启动器通过进程内私有 capability 调用五个 `runMain`，直接入口、`require(...).runMain()`、`node -e` 及 loader/preload 旁路在业务逻辑前失败关闭。动态 runner 的 P1001 根因已由 focused `50/51` RED 证明：仅容器内 `pg_isready` 后立即执行宿主 Prisma migrate 存在端口协议 readiness 窗口；现改为同时要求容器内 ready 与宿主通过同一随机 loopback URL 执行 Prisma `SELECT 1`，任一失败共同有限重试且探测客户端总会断连，focused `51/51` GREEN。完整 shared-domain/API/Web、typecheck/lint、全部变更 CJS `node --check`、launcher `sh -n`、`git diff --check` 与真实隔离 PostgreSQL 16 动态门须在本轮新提交的精确 clean SHA 上重新运行，旧 full/dynamic 收据不得继承；全部门与完整动态 JSON receipt 通过后才启动新的 Standards/Spec v9 双轴复审。本票只改归零工具、测试、相关 Prisma 验证、运行手册及本条进度，未改 Schema/迁移或业务模块，未安装真实环境身份/公钥/注册表/租约，未连接或执行任何真实/生产归零；真实执行仍仅属于后续 #122 的独立精确授权与外部维护窗口。
  - 此前 PostgreSQL 动态复核暴露并修复了已知可空 lifecycle 字段 `null` 被误判，以及旧四阶段审计断言；这些旧 SHA 和通过收据不继承到本轮。本轮收据契约 RED 证明缺少独立 unknown 归属字段时必须拒绝；最小 GREEN 要求未知/混合归属各自经真实预检阻断、候选清空，且仅在 cleanup 成功后输出含迁移、dry-run/执行、正式/未知/混合阻断、备份恢复和 cleanup 结果的单一 JSON，focused `52/52` GREEN。当前动态夹具还将精确验证 `started -> object_recovery_planned -> object_deletion_progress -> completion_pending -> completed`，另要求唯一 `terminal_committed` 权威 marker，并在同一 clean SHA 上报告 125 migrations、最终状态、`productionAccessed=false` 与清理结果。v9 双轴复审不得在新 SHA 的完整静态、全量及动态门全部通过前启动。
  - v9 固定候选 `d1e3284eba55e60a446c2f9edf3dac8164c7ffe4` 及更旧候选/审查/动态收据已作废。本轮已按 v9 有效 findings 补齐：不对外导出的启动 capability；按真实 Prisma Schema 关系传播的正式聚合父保护；Node/Prisma 以及 API 实际生产依赖字节的执行身份；精确一条 `completed` 和一条同收据 `terminal_committed` 审计门；来自真实 PostgreSQL inventory 的未知/混合归属阻断；真实 `pg_dump -Fc`/`pg_restore --exit-on-error` 隔离恢复与私有文件独立目录逐对象比对。候选 `4f2ffba64d42271bde8a981d44badf3339ad4ee3` 因 manifest-only 依赖误判作废；候选 `deed9e5449307a11ef51f57d5a34e1c52dafa462` 因动态验证器漏导入 `rm` 作废；候选 `845afaf34109fba64a06c20f42fb7ca12d6b20c5` 因夹具 setup/cleanup 写 `User.updatedAt=NOW()` 被 fresh fingerprint 正确阻断；候选 `4e121a58f20dabb4d6d3a0aeb442290ec4465f47` 因五阶段 controlled_execution 总数被误作 completed 计数而正确阻断。候选 `cd3b4072404856633785af82e962583b079c77f1` 已把后置 SQL 分别精确筛选 `metadata.status=completed` 与 `terminal_committed`，仍各要求恰好一条并绑定同一收据；focused `59/59`、12 CJS `node --check`、launcher `sh -n`、diff-check、shared `163/163`、Web `1948/1948`、API `314 suites / 6061 passed / 76 skipped`、workspace typecheck/lint 全通过。clean SHA 隔离 PostgreSQL 16 动态门应用 125 条迁移（head `20260811090000_contract_document_content_revision`）并通过：正式聚合、未知归属、混合归属、无测试来源均 blocked 且候选为 0；真实 `pg_dump -Fc`/`pg_restore --exit-on-error` 与私有文件独立恢复逐对象校验通过；本地对象同 key 复活被独立后检拒绝；最终 `status=passed`、`productionAccessed=false`、容器/临时文件均已清理。需在本条收据提交形成的新 clean SHA 上复跑全部门与动态 JSON 后，才启动全新 Standards/Spec v10；仍未连接生产或执行真实归零。
  - v10 固定候选 `d371b4a413814fba6fd6347316a077359b000158` 及其双轴收据已作废。本轮六个根因已按 RED→GREEN 收紧：五个 `runMain` 通过不可替换的私有 capability 闭包在业务逻辑前验证，require-cache/monkeypatch 旁路失败关闭；补登记 InvoiceAllocation、ExpenseClaim 附件/付款执行、SettlementLine 附件等真实正式聚合关系，任何未登记的业务表间实际 FK 形成 `UNREGISTERED_BUSINESS_AGGREGATE_RELATION` Schema blocker；终态事务同时拒绝预存 `completed` 或 `terminal_committed`；inspect 先核 PostgreSQL custom-format `PGDMP` 工件和可解析、逐对象覆盖的 tar，真实恢复仍只能由隔离 PG16 dynamic 证明；运行摘要由实际入口依赖闭包生成，不再把 manifest-only 包冒充已加载依赖，同时真实递归依赖缺失或字节漂移仍失败关闭；write-freeze lease 的 `testProvenanceRegistrySha256` 等精确字段由实现常量和 runbook 共享回归。focused 已由 59/60、60/61、61/62 及 require-cache exports 替换 62/63 等真实 RED 收敛为 `63/63` GREEN；12 CJS `node --check`、launcher `sh -n`、`git diff --check`、shared `163/163`、Web `1948/1948`、API 沙箱外 `314 suites / 6061 passed / 76 skipped`、workspace typecheck/lint 均已通过。上述门与真实隔离 PostgreSQL 16 dynamic 单一 JSON 收据仍须在本轮新 clean SHA 上重新运行，通过前不得启动 v11 双轴复审。未安装真实环境信任工件，未连接生产或执行真实归零。
  - [~] Issue #149 已在本任务独立 worktree 将 PR #129 head `097780b78bd96e80463a6bc984702d3d255beed9` 与锁定 `origin/main@8829d0bdbf92efb40fe3d3087782397d93fd4b07` 建立集成候选，`PROGRESS.md` 唯一冲突仅机械保留双方真实历史。RED 在 API build 前置补齐后精确得到 63 项中 62 passed / 1 failed，唯一失败列出 `BusinessEntrySubmissionSnapshot`、`OperatingFact`、`OperatingImpactEntry`、七个 `OperatingTakeover*` 模型及 `ProjectParticipatingCompany` 共 11 个主线 Prisma 模型缺少策略登记。按正式规格、Prisma relation 与领域服务逐项复核后，仅将项目我方参与公司登记为逐主键复核的基础配置 `review`，其余提交快照、经营事实/分录和历史经营接管记录登记为 `business_review`；未改变逐主键 preserve/delete、未知关系阻断、preview/apply、授权签名、本地 disposable、生产保护或自清理。targeted policy 2/2 GREEN，原始 business-zeroing tool 全套 64/64 GREEN，未触发 136 迁移坐标、施工企业夹具、未登记业务 FK 或 dist blocker。本收据与两个策略/测试文件组成同一本地 merge commit；未运行 PostgreSQL 动态归零门、未 push、未更新 PR/Issue，未连接生产、执行归零、部署、迁移、COS/权限或业务写入。
  - [~] PR #129 / POL-22 当前施工企业 dynamic fixture 固定修复从 clean 集成候选 `f0496387395dc426e937bee1ca8612904494cbe5` 开始；受信 dynamic 流程已完整应用 136 migrations 并在 `ProjectUpstreamFundFact` 夹具写入处复现 PostgreSQL `23514`“正式经营事实发生前必须先设置唯一施工企业”。逐项核对 `ProjectAffiliateAssignment` Schema、项目施工企业配置服务、正式来源快照解析与 `20260814010000_project_operating_profile` 触发器后，仅在 `verify-business-zeroing.cjs` 的 disposable fixture 创建同项目 active `BusinessParty`、对应 `BusinessPartyVersion`、覆盖事实日期且未结束的 assignment，并让 guarded fact 复用该真实 assignment/version/name；归零后追加 Project=1、assignment=1、active assignment=1 断言。最窄 fixture unit `1/1`、CJS syntax 与 `git diff --check` 已 GREEN；本条所在最终 clean commit 的 PostgreSQL 16 dynamic、#149 targeted/full business-zeroing、全仓非生产门、双轴复审及 GitHub 收口仍待执行。未改业务约束、Schema/migration、#149 策略或其他夹具，未连接生产或执行真实归零、部署、迁移、COS/权限/业务数据写入。
  - [~] Issue #150 从 clean 候选 `78c3936bf94eb448843175c7c9dca90be1e99534` 开始，仅处理 disposable PostgreSQL 16 dynamic 暴露的 `OperatingLedgerWriteContext` 与 `OperatingLedgerWriteSecret` 两个 `UNKNOWN_TABLE`。migration 与运行服务复核确认前者按 backend PID、transaction ID 和 actor 形成事务写入授权上下文，后者只保存经营账运行时写入密钥的 singleton 摘要；二者均为经营账安全控制面，不是测试业务记录，故分别以“经营账事务写入授权上下文”和“经营账写入授权密钥摘要”唯一登记为 `protected`。RED 精确显示 unknown 集合包含上述两表及虚构 future 表；最小 GREEN 后 business-zeroing tool `65/65`，只剩虚构 future 表继续 `UNKNOWN_TABLE`，逐 PK、正式/有效/未知/混合保护、来源证明、授权签名、preview/apply、生产与 disposable 边界均未放宽。本条所在最终 clean commit 仍须重跑受信 PostgreSQL 16 dynamic，并在到达 `UNREGISTERED_BUSINESS_AGGREGATE_RELATION` 或其他不同 blocker 时立即停线；未改 Schema/migration、业务约束、施工企业夹具、#149 的 11 个模型分类或业务模块，未 push、更新 PR/Issue、连接生产、执行真实归零、部署、迁移、COS/权限或业务数据写入。
  - [~] Issue #151 从 clean 候选 `65bc7c0f351009c5f7565a25aff17fffc9c77c4a` 开始，仅处理同一候选 disposable PostgreSQL 16 完整 Schema 扫描暴露的 `UNREGISTERED_BUSINESS_AGGREGATE_RELATION`。受信 dynamic 已应用 136 migrations；未截断 RED 共 210 条、210 个唯一稳定外键名，首项为 `ApprovalFormGenerationClaim_approval_instance_fk`、末项为 `SupplierBalanceReservation_paymentId_fkey`。逐条核对 Prisma/scalar 坐标、migration FK、领域事务写入、正式状态与 POL-22 保留传播语义后，以每条完整 from/to 坐标、显式 `protectsChildLifecycle` 和中文理由登记：91 条父子组成关系传播保护，119 条当前/结果指针、版本/冲销沿袭、来源/目标、审批/委托归因、失效来源或跨聚合坐标不传播保护；其中历史接管批次与付款条款阶段关系保持保护，零采付款 `sourcePaymentId` 保持 lineage 不保护。新增 future 业务 FK 仍由既有测试 fail-closed；最窄 business-zeroing tool 已由 `65/66` RED 收敛为 `66/66` GREEN。本条所在最终 clean commit 仍须重跑受信 PostgreSQL 16 dynamic，出现任何不同 blocker 即停线；未改 Schema/migration/数据库 FK、领域写入、施工企业夹具、#149/#150 策略、页面、shared-domain 或其他 POL，未 push、更新 PR/Issue、连接生产、执行真实归零、部署、迁移、COS/权限或业务数据写入。
  - [~] Issue #152 从精确候选 `6c5568402190adf88a62c144d2859ee905779f32` 开始，仅修复 POL-22 终态审计重复守卫的查询范围。未改代码的受信 disposable PostgreSQL 16 dynamic 已成功应用全部 136 migrations，随后在合法阶段审计已经按 `started -> object_recovery_planned -> object_deletion_progress -> completion_pending` 写入、真实 `completed=0` 且 `terminal_committed=0` 时，被只按 action/businessType/batchId 查询的宽守卫误判为“completed 审计已存在”。行为测试先以 `66/67` RED 固化阶段顺序、两个终态零计数和精确查询参数；最小修复仅让 controlled execution 守卫增加 `metadata.status='completed'`、terminal action 守卫增加 `metadata.status='terminal_committed'`。GREEN 为 business-zeroing tool `68/68`，同时证明正常批次只写一次 completed/terminal、预存真实 completed 或 terminal_committed 均保持零写入失败关闭；两个 CJS `node --check` 与 `git diff --check` 通过。本条所在 clean commit 仍须重跑受信 PostgreSQL 16 dynamic、全量非生产门和双轴复审；出现任何不同 blocker 即停线。未改 Schema/migration、业务 FK、#149/#150/#151 策略、施工企业 fixture、业务模块或其他 POL，未连接生产、执行真实归零、部署、迁移、COS/权限或业务数据写入。
  - [~] Issue #153 从精确候选 `837353c9d4181c67c9f3ead507976704e68d5516` 开始，仅修复 POL-22 动态收据的迁移坐标硬编码。未改代码的受信 disposable PostgreSQL 16 dynamic 逐条成功应用当前 136 migrations，末项为 `20260816120000_pol08_contract_lineage_operating_sources`，随后仅因 final receipt 仍要求 `migrationCount===125` 报“POL-22 动态收据迁移坐标无效”。行为测试先以 `67/68` RED 固化当前坐标；GREEN 不写死 136，而是先校验受控 database dynamic gate manifest 与 migration 目录，再把目录内每个 `migration.sql` 的 SHA-256 与 `_prisma_migrations` 完整记录双向一一核对，缺失、额外、重复、checksum 漂移、未完成、回滚、异常步数或失败日志均继续失败关闭；最终 JSON 保存完整成功迁移名称/checksum 集合及摘要，business-zeroing tool 为 `68/68`。本条所在 clean commit 仍须重跑精确 SHA 的受信 PostgreSQL 16 dynamic、全量非生产门和双轴复审；出现任何不同 blocker 即停线。未改 Schema/migration、#149/#150/#151 策略、#152 终态守卫、施工企业 fixture、业务模块、页面、shared-domain 或其他 POL，未连接生产、执行真实归零、部署、迁移、COS/权限或业务数据写入。
- [~] Issue #162（POL-22-P14）从精确起点 `84939e18f026d2dac5fff97f2ed26b5132f1d998`、fresh fetch 后 `origin/main@8829d0bdbf92efb40fe3d3087782397d93fd4b07` 在独立 worktree 实施；唯一修复范围为 RC06 mock `settlement-signature-governance.e2e.ts` 的“生成当前修订版”等待超时。官方命令先在 Chromium 与移动 WebKit 以旧 locator 精确 RED（均在第 83 行 60 秒超时）；源码复核确认页面已由 `f9ece623` 将公开契约统一为“生成冻结结算单/重新生成冻结结算单”，故仅更新两个精确测试 locator，保持冻结结算、能力校验、请求 revision、签署/关联断言和权限语义不变。第三个同批失败场景已单独重跑通过；本条最终 SHA 仍须完成双浏览器 GREEN、相关 Web 门、CJS/POL-22 全部门、一次 disposable PostgreSQL 16 dynamic、一次正式 `release:local`、双轴只读复审及 GitHub 收口。未触碰 #146 所有权文件、根工作树、Schema/迁移、生产数据库/COS、部署、真实归零或 #122/#123/#124。
- [x] POL-02（Issue #95）已在 `origin/main@6f1c4e4a30233c82a3c90487cdbff01a37f2cecb` 的独立分支完成本地实现：新增项目经营账生效日、接管完成日和接管状态，两个日期独立读写；新增唯一施工企业设置入口，并由数据库触发器在首笔正式合同、结算或资金事实产生时锁定已有且覆盖事实日的施工企业，存量正式事实在迁移升级及后续启用/调整经营账生效日时同步校验和补锁，待审批事实不提前锁定，存量事实携带的施工企业映射/版本与当前映射不一致时失败关闭。新增带主体版本快照和实际生效区间的动态参与公司名单、停止新增业务和“已有正式事实不可删除”约束；公司必须资料完整，未来生效/未来停止分别显示“待生效”/“已安排停止”，任意停止日变更都不能回溯截断正式事实。合同生效、报销、实际付款分摊、零采付款和施工企业-我方公司合同均复用数据库参与期间硬门，经营账启用、停用与正式事实写入按项目行锁串行。写接口及两类候选读取同时由项目作用域权限守卫与服务层项目财务校验保护，旧施工企业映射入口亦已收口；施工企业在页面按企业名称、信用代码和版本选择，不暴露技术编号。项目经营页新增 TDesign“项目设置”入口，档案及全部 mutation 均以 projectId + 请求代次隔离，切换项目时清空项目专属表单并丢弃过期响应。定向 shared-domain/API/Web 测试、三端 typecheck/lint、API/Web build、`check:ui`、Prisma Schema 校验和动态门清单自检已通过；真实 PostgreSQL RED 证明未配置施工企业时曾可在经营账启用前新增正式事实，最小触发器修复后已 GREEN，任何新正式事实现在均要求事实日存在唯一施工企业。一次性本机 PostgreSQL 16 remaining 动态门已在代码候选 `7baacb0811215e6af4207fc625f2bc9ffd021f4c` 通过 126 条迁移、10/10 组和 62 条测试；升级组真实执行“前 125 条迁移→写入存量事实/施工企业→POL-02 迁移”，正例补锁，反例以 `ProjectProxyPayment.paidAt < assignment.effectiveFrom < createdAt` 证明按业务日不会误锁。完整动态清单为 86 条/34 文件、remaining 0；本条所在最终提交继续作为完整本地动态门的精确 SHA 验收目标。`/code-review` Standards/Spec 独立双轴最终复核均为 0 findings；未连接或修改生产数据库。
- [x] POL-03（Issue #96）已在依赖 commit `7bfb5e488b2d87bd0ee394972e805977ac9ff88e` 上完成本地实现：新增只追加的 `OperatingFact` 经营事实信封与 `OperatingImpactEntry` 强类型影响分录，覆盖来源/版本/发生日/确认日/施工企业及参与公司主体快照、整数分、证据等级、更正/冲销引用和来源唯一键；新增事务写入服务，按项目财务权限、经营账生效日、事实日主体期间和来源/影响键幂等校验；新增 PostgreSQL 约束及触发器保护跨项目引用、原行 UPDATE/DELETE/TRUNCATE、C 级正式影响、未验证主体、缺少成本分类、缺少必要事实主体、重复/部分冲销和并发重复写入。当前未接入项目范围验证器的 owner/downstream/employee 主体按失败关闭处理。服务定向 7/7、API typecheck/lint、Prisma validate、动态门静态 11/11 已通过；一次性本机 PostgreSQL 16 已通过 128 条迁移和 POL-03 集成并发测试 1/1（含成功更正/冲销、主体快照、重复来源、跨项目、直接 SQL、UPDATE/DELETE/TRUNCATE 防护）。双轴代码复核最终均为 0 findings；本地已新增数据库 owner/runtime role 硬化与受控写入函数；生产角色配置和迁移仍未执行。全局其余动态组未因本票重跑，不据此宣称全局 87 条测试通过。未连接或修改生产数据库。
- [x] POL-03 P1 数据库写入硬化已在当前 worktree 完成本地闭环：新增第 128 条最小迁移，以 SECURITY DEFINER 受控函数登记事务授权上下文并撤销 runtime role 对 `OperatingFact`/`OperatingImpactEntry` 的直接 INSERT/UPDATE/DELETE/TRUNCATE；应用服务改走受控函数，错误 secret、伪造 GUC 和直接 SQL 均在真实 PostgreSQL 16 中失败关闭。runtime role 配置为显式 owner-only 操作步骤；部署迁移仅从 root-only `DATABASE_MIGRATION_ENV_FILE` 读取 `DATABASE_MIGRATION_URL`，API 运行时与定时任务加载的 `API_ENV_FILE` 明确拒绝该 owner URL。remaining 动态门为经营账隔离 PostgreSQL 组生成一次性写入密钥，并由 fixture 仅向同一隔离库写入 hash，以覆盖受控写入并发验收；未执行生产角色配置、生产迁移或生产数据修改。
- [~] POL-04（Issue #97）切片 1 已在 POL-03 focused commit `c348226d3045f25299beb5a5b54122ef89c184d0` 上完成本地契约骨架：新增构造后封闭的来源适配器注册表、只接受正式状态的冻结来源快照契约、同事务单来源幂等重放，以及 PostgreSQL `SET TRANSACTION READ ONLY` 保护的按项目重建比较和含来源/中文业务编号的差异结构；缺失或重复适配器、非正式快照、跨来源坐标均中文失败关闭。TDD 先由缺少实现得到目标 RED，随后新增测试与原 POL-03 服务测试合计 13/13、API typecheck 通过。真实 PostgreSQL 金额/权限/状态/并发与动态门仍在下一切片，当前不得宣称 POL-04 收口。
- [~] POL-04 切片 2 已新增独立 PostgreSQL 契约场景和动态门编排：测试专用适配器覆盖正式/草稿源、项目财务权限、金额漂移、并发和重复重放、正式事实与两类影响分录、数据库只读事务写入探针及缺失注册；动态清单由 87 条/35 文件更新为 88 条/36 文件、remaining 0，并为经营账测试在每次一次性运行中生成独立测试密钥，未写入仓库或输出收据。静态清单门 11/11、manifest validate、目标单元测试 13/13（数据库场景 1 条按开关跳过）、API typecheck 和目标 lint 已通过；真实 PostgreSQL 16 执行仍须绑定本切片提交后的干净候选 SHA。
- [~] POL-04 第一次绑定候选 `9ce95c6fb14f047ba4e59d72ec3ebff081fb86ed` 的 PostgreSQL 16 remaining 全组执行在到达 POL-04 前，被既有 `project_operating_profile_upgrade` 旧迁移夹具触发“正式经营事实发生前必须先设置唯一施工企业”阻断；未把该 POL-02 夹具问题扩大进本票。现仅为原 runner 增加精确子组选择和未知/重复参数失败关闭，默认全量路径不变；下一收据只运行包含 POL-03/POL-04 的 `generic_database_constraints`，不得冒充全量 64 条收据。
- [x] POL-04 `/code-review` 初轮 Standards/Spec 轴提出的 P1/P2 已全部回修：适配器注册表新增独立必需来源目录并在比较前校验完整性；重放先校验当前项目财务权限、再读取来源，且受控写入保留冻结来源的原确认人；一致性比较使用 `RepeatableRead + READ ONLY` 单一快照，复用写入侧主体/影响快照物化，并只输出受控中文字段标签。TDD 先由缺少目录/权限/重放/快照接口得到 RED，随后目标单元测试 15/15、API typecheck、全量 API lint、Prisma validate、动态门静态测试 13/13 和 manifest 88/88（remaining 0）通过；干净候选 `61c17bbdd0a9331f64f0a69ff64d90bbf2a6b684` 的一次性 PostgreSQL 16 `generic_database_constraints` 收据通过 128 条迁移、12 个数据库测试文件和 Jest 38/38，覆盖零事实缺适配器、权限、不同确认人与重放人、金额/状态、幂等并发、正式事实与影响分录、Repeatable Read 并发屏障、只读写入探针及派生快照漂移。Standards/Spec 二轮均为 0 个未关闭 P0/P1/P2；具体业务域来源映射留给后续 POL 票。该子组收据不冒充 remaining 全量 64 条，既有 POL-02 迁移夹具阻断仍独立保留；未执行 push、PR、合并、Issue 关闭、生产访问、生产迁移、生产数据修改、COS 或真实归零。
- [~] POL-05（Issue #98）切片 1 已从 POL-04 最终本地提交 `37cc76698863b409fc5de4d9d9819b3782a356ae` 在独立 worktree 开始：新增业主结算和下游生效结算正式来源适配器，冻结来源确认人、确认时间、业务版本、经营账生效日、施工企业、我方公司/业主/下游相对方主体及归档依据；业主结算仅形成收入与应收，下游结算按四类受控合同映射一级成本和应付，负向结算使用冲减方向，付款状态变化不改写结算来源金额。经营账写入口新增同事务业务确认模式，只允许当前激活业务操作人与冻结确认人一致，不授予其项目财务权限；未启用经营账的存量项目由后续业务钩子兼容跳过。TDD 先由缺少适配器和业务确认接口得到 RED，随后适配器 8/8、经营账与重放服务 17/17、API typecheck 通过。付款执行、施工企业代付、FinanceRecord 兼容边界、业务域钩子、注册表、真实 PostgreSQL 和动态门仍在后续切片，当前不得宣称 POL-05 收口。
- [~] POL-05 切片 2 已接入我方付款执行与施工企业实际付款：部分付款和多结算核销按 `contract_due_payment` 分摊逐笔冲减应付，`advance_deduction` 不冒充本次现金核销；实际付款只减少对应我方公司或施工企业项目资金，不产生 `confirmed_cost`，批准付款主体与实际付款主体不同则保留两个主体并形成一笔主体间代付往来。付款来源读取会按付款申请批复总额失败关闭超批复存量异常，施工企业付款保留合同/结算、凭证和冻结施工企业主体。四类 POL-05 正式来源已进入封闭注册表，`FinanceRecord` 明确不注册为经济事实来源，仅在付款事实依据快照中标记为财务凭证兼容角色。业主结算确认、下游结算归档生效、付款执行（含幂等重试）和施工企业付款均在原业务事务内投影；经营账未启用时兼容跳过。来源/注册表/业务钩子及既有服务目标测试 474/474、API typecheck 与目标 lint 通过；真实 PostgreSQL、动态门、全量 lint 与双轴复核仍未完成。
- [x] POL-05 切片 3 已新增第 129 条追加迁移，只把 POL-05 所需 `owner` 与 `downstream_counterparty` 主体加入事实/影响数据库硬门，原 POL-03 项目、期间、施工企业、参与公司、追加写和服务授权校验保持不变，员工主体仍失败关闭；所有精确迁移基线 runner 和动态门终点同步更新。新增真实 PostgreSQL 16 契约场景覆盖草稿状态拒绝、非财务重放拒绝、结算并发幂等、两笔生效结算成本与应付、我方跨公司代付、多结算核销、施工企业付款、主体快照、金额汇总、FinanceRecord 不生成事实、只读一致性比较和超批复异常失败关闭。动态清单为 89 条/37 文件、remaining 0，静态编排 13/13、manifest validate、Prisma validate、API typecheck 与目标 lint 已通过；精确候选 `ac2a7eddc76a5a3cdd637b9ccf53c15e5f771f41` 已在一次性 disposable PostgreSQL 16 通过 129 条迁移至 `20260814130000_pol05_operating_source_subjects`，本动态分组执行 13 个数据库套件 / 39 个 Jest 用例（清单口径 35 项）全通过，容器镜像 `postgres:16@sha256:95206741a5b214807675e14165369d05b93a9cf692223b616d07cca227e74b0b`，未连接生产数据库。
- [~] POL-05 双轴初审修复切片已完成：付款批准主体统一冻结为 canonical CompanyEntity ID，同一公司不再因实体/版本 ID 形态差异产生虚假主体间往来，真实不同公司仍保留一次代付往来；施工企业付款适配器现在从冻结合同版本派生并核对债务、批准和实际付款主体，关联我方签约合同或不一致施工企业时失败关闭，PostgreSQL 夹具改用可由真实写服务接受的独立施工企业签约合同。经营主体新增事实/影响角色矩阵，业主或下游相对方不能进入批准付款、实际付款或成本承担公司，并由服务校验与数据库约束双层保护。Project、Settlement、Payment 三个正式源投影依赖不再使用 Nest 可选注入或可选调用；未注入回退只兼容未启用经营账的旧单元夹具，项目已启用经营账时明确失败关闭。TDD 先取得 5 项目标 RED，随后 8 个目标套件 471/471、API typecheck 和全量 API lint 通过；更新后的 PostgreSQL 动态收据及双轴二审仍待绑定干净候选，当前不得宣称 POL-05 收口。
- [~] POL-05 双轴二审修复切片已完成：主体角色矩阵修正为允许业主作为 `owner_payment` 的实际付款主体，同时继续拒绝下游相对方冒充实际付款主体；服务层先取得约束 RED，随后 `owner_payment` 正例和非法下游反例 11/11 通过，PostgreSQL 回归也已加入事实与影响双层业主实付探针。三个业务服务对经营源的依赖已收窄为仅含同事务追加方法的 port，并以显式 Nest token 保留生产强制注入；测试兼容 fallback 不再伪装成完整重放/比较服务。精确候选提交、PostgreSQL 16 复跑与双轴终审仍待完成，当前不得宣称 POL-05 收口。
- [~] POL-05 Standards 终审文档修复切片已完成：共享领域词汇已明确业主付款事实中业主可作为实际付款主体，同时保留项目向下游付款时只能由施工企业或具体我方公司实付的语义，与服务角色矩阵、PostgreSQL 约束及 `owner_payment` 正例恢复一致。该切片仅修正共享 CONTEXT 与进度登记，不改业务代码、Schema 或迁移；Standards 归零复核仍待完成，当前不得宣称 POL-05 收口。

- [x] POL-05（Issue #98）本地已收口，本条作为当前状态覆盖上述切片中的过程性 `[~]` 与“待完成”语句：首次 `git fetch origin` 后记录的 `origin/main` 为 `6f1c4e4a30233c82a3c90487cdbff01a37f2cecb`，本票从 POL-04 最终本地提交 `37cc76698863b409fc5de4d9d9819b3782a356ae` 在独立 worktree 实施。最终业务实现候选 `5a3da745a7951099d1a426707d45c9fd9c0e1c59` 已接入 `project_upstream_settlement`、`settlement`、`payment_execution` 和 `project_proxy_payment` 四类正式来源，在原业务事务中冻结金额、状态、权限、主体、凭证与影响分录，多结算核销、同公司 canonical 主体、真实跨公司/施工企业代付往来、业主实付与下游主体角色失败关闭均已覆盖；付款不产生 `confirmed_cost`，`FinanceRecord` 不另立经济事实，不重复计成本。该精确 SHA 在本机一次性 PostgreSQL 16 子组门中于 `2026-08-14T08:25:45.722Z` 至 `2026-08-14T08:26:09.443Z` 通过 129 条迁移至 `20260814130000_pol05_operating_source_subjects`，13 个数据库套件 / 39 个 Jest 用例全通过（manifest 口径 35 项），总耗时 23,721 ms，镜像 ID 为 `sha256:11a9d238fbb48bab14599c57e41123254452b1a2d93c6c8595bce96f346bd082`；同 SHA 的 7 个目标套件 470/470、API typecheck、全量 API lint、Prisma validate、动态清单契约 13/13 通过，清单覆盖 89 项/37 文件且 remaining=0。终审对上述实现给出 Spec P0/P1/P2=0；共享词汇修正提交 `1a770f4168d8de402819b49bf531cdd6342a17bf` 后 Standards P0/P1/P2=0。四类来源当前没有正式可达的更正/冲销业务动作，本票未虚构新入口，应在对应来源生命周期实际交付时同事务接入。GitHub #97 和 #98 仍为 OPEN，此处仅表示受上游指定本地固定点支持的 POL-05 代码本地完成；未 push、未创建 PR、未 merge、未关闭/写入 Issue，未访问生产、未执行生产迁移或数据写入，未访问 COS 或执行真实归零；本最终进度登记为纯文档后续提交，不继承 `5a3da745a7951099d1a426707d45c9fd9c0e1c59` 的 PostgreSQL 动态收据。

- [x] POL-06（Issue #99）已从 fresh fetch 锁定的 `origin/main@89eda4e6fb883e8f73e1e45e790f38ad1fd0c275` 在独立 worktree 完成本地实现，后续获得单独授权后创建 PR #135：费用与报销申请正式确认只投影一次成本与应付，实付只清偿应付及登记我方公司项目资金；员工借款发放、冲账、还款均不产生成本。旧项目支出五类入口均停止新建，历史读取和存量完成路径保留；资金工作台补入零星费用实付读取。`employee_project_loan_entry` 的还款冲销是唯一允许从正式业务确认入口写入 `reversal` 的来源：必须保留并复核同项目的原经营事实、原还款业务主键、事实种类/原始分录种类、等额和六类主体；经营影响逐项保持原 impactKind、键、主体和金额，仅反转 direction。其它正式来源的任何更正/冲销仍失败关闭，既有权限控制未放宽。TDD 已取得专用冲销映射与错配原事实引用的 RED，随后 GREEN；目标 API 套件 303 passed / 1 skipped（受环境开关控制的既有数据库守卫），API/Web typecheck 与 lint、Web 1953/1953、`check:ui`、Prisma schema validate、`git diff --check` 全通过。新增第 130 条 POL-06 迁移仅作脚本/静态契约校验，未执行迁移或任何数据库写入；未访问生产/COS，未部署、归零或执行任何生产迁移、数据写入。Spec/Standards 双轴复核无未关闭 P0/P1，前者建议的原事实一致性校验与后者发现的真实冲销 impactKind P0 均已回修并重测。

- [x] POL-06 CI 基线收口修补：PR #135 首轮 CI #31873228790 的质量门已绿，但动态数据库门发现源码有 130 个迁移、清单和 9 个 runner 尚固定 129，静态 Unit 门另漏结算草稿与受环境门控的付款实付并发断言，页面动作清单也因旧项目支出写入口收口产生源码行号漂移。已将动态门清单、runner、静态断言和说明同步到第 130 条迁移 `20260815150000_pol06_expense_employee_subjects`（含 SHA-256 钉住的 settlement-approval-withdrawal runner），并用现有生成器刷新页面动作清单。`--validate-manifest` 确认 130 迁移、89 测试/37 文件且 remaining=0；8 个受影响 API suite 52 passed / 1 skipped；全量 `pnpm test` 为 shared-domain 165、Web 1953、API 6133 passed（98 个受环境门控用例 skipped），`pnpm typecheck`、`pnpm lint`、`pnpm inspect:release-manifests` 和 `git diff --check` 均通过。未执行动态门 `--execute`、迁移或数据库写入；PR 重跑 CI 待核验，未 merge、未关闭 Issue。

- [x] POL-06 第 2 次 CI 收口修补：上述候选的 CI #31873836131 又发现 65 项 remaining PostgreSQL 16 动态分片中，一条故意绕过服务的未接入影响主体直写，已被 POL-06 新增的“正式经营影响必须通过已授权的经营账服务登记”守卫更早拒绝；该失败关闭语义未放宽，仅更新测试断言为实际且更严格的守卫消息。能力矩阵检查还要求把已刷新页面动作清单的 SHA-256 回写至 JSON/Markdown 生成文件。动态门只读校验、全量 `pnpm test`（shared-domain 165、Web 1953、API 6133 passed；98 skipped）、`pnpm typecheck`、`pnpm lint`、`pnpm inspect:release-manifests`、页面动作与能力矩阵 ready 检查、`git diff --check` 均通过；未执行动态门、迁移或数据库写入。此修补推送后才重新触发 CI；未 merge、未关闭 Issue。

- [~] POL-07（Issue #100）已从 fresh fetch 锁定的 `origin/main@6d9d923e94ba7c76f9928a0f4142538f4611b171` 在独立 worktree 完成本地实现：收货复核只确认一次材料 `confirmed_cost`（零金额收货不产生经济分录）；新版实际付款只形成我方公司项目资金减少，旧版轻量付款在没有公司主体时准确归属施工企业项目资金，均不重复成本；整笔供应商退款以关联原付款和收货复核的资金流入冲减原资金影响，不重记成本，且同样区分新版公司与旧版施工企业资金主体；结构化 `InvoiceRecord` 仅形成可追溯的 `invoice_reference` 依据分录，不自动产生收入、成本或付款，并由服务和数据库共同强制 `notice` 方向。四类来源均在各自正式业务事务中同事务追加，并进入封闭经营来源注册表；第 131 条迁移扩展 impact kind 白名单并约束发票依据方向，动态门清单、9 个 runner、说明和静态契约均同步为 M131。独立 Spec 复审为 P0/P1/P2=0，Standards 为 P0/P1=0；后者记录一项重放逐行关联读取的 P2 性能建议，因无性能阈值且批量重构会扩大本票，未在本候选实施。定向回归 12 个套件为 10 passed / 2 个既有环境型套件 skipped、293 passed；API/shared-domain typecheck 与 lint、Prisma validate、动态门 manifest validate 和静态 13/13、`git diff --check` 已通过。最终本地候选为 `057d188202b5fe914cac491079dd5ffa2909f0ea`（实现提交 `ebfa3e0d5118b4cc03dd06070238bcfdfbf7e1ff` 后补一条 M131 runner 测试同步 `057d188202b5fe914cac491079dd5ffa2909f0ea`）；本机一次性 PostgreSQL 16 动态门于 `2026-08-15T09:39:04.978Z` 至 `2026-08-15T09:44:43.320Z` 通过 131 条迁移、9 组、89 条待覆盖测试 / 37 文件，`remaining=0`，镜像为 `postgres:16@sha256:11a9d238fbb48bab14599c57e41123254452b1a2d93c6c8595bce96f346bd082`，一次性容器均已清理。未 push、创建 PR、写入/关闭 Issue、访问生产或写入生产/COS。

- [~] POL-08（Issue #101）已从 fresh fetch 锁定的 `origin/main@5f5817a09cbb7dba22e61da4b34a714745316ec3` 在独立 worktree 完成本地实现：施工企业合同、结算、付款、上游资金事实进入经营来源重放；正式下游合同只形成合同承诺引用，不伪造成本或应付，施工企业付款按结算或合同付款义务核销应付并登记实际付款，上游收款不计入我方现金；经营账生效日后的三类施工企业付款均强制关联同来源、已审批付款申请，历史/异常事实保留原始来源且不伪造审批；施工企业向我方拨款必须保留同一条施工企业—我方合同、已确认结算和有效发票链路，并在资金台账读取结算应付、未付和差额；结算链路、付款确认锁顺序、施工企业主体快照、事实更正/冲销、幂等和旧一步代理付款路由 410 均由服务/数据库边界保护。用户可见业务语言统一为“施工企业”，API/Web 入口、TDesign 表单、业主结算关联和派生读模型同步补齐。终审后又收口业主付款更正不得替换原上游结算关联、施工企业拨款来源重放改用结算有效净额，并为施工企业结算与施工企业拨款登记/确认复用同一原账本锁；结算更正/确认按整个结算账本汇总既有付款和拨款，更正/新登记/确认均禁止有效结算金额低于既有或拟登记金额。新增第 136 条迁移，动态门基线与 runner 计数同步。最终定向 API 回归为 44 passed，全量 API 为 339 suites / 6204 passed / 100 skipped；此前 Web 183 files / 1958 passed，API/Web typecheck、lint、build、`check:ui`、business-language、release manifest 和 `git diff --check` 均通过。PostgreSQL 16 动态门待在本次最终修复提交的 clean candidate 上重新运行；未 push、创建 PR、合并、部署、执行生产迁移或写入生产/COS。

- [~] POL-09（Issue #102）已从 `origin/main@9663273c739dad7c0fcaa9b575a5554c37482a4c` 在独立 worktree 完成本地实现：历史接管激活在同一事务内把 A/B 级历史期初结算与逐笔实付接入经营来源重放，实付只登记下游实际付款、应付核销、资金减少及历史预付款/异常超付余额影响，不创建 `PaymentRequest` 或 `PaymentExecution`；普通直签合同不伪造期初结算，零结算不创建零金额来源，C 级接管在正式激活入口拒绝；生效日后的付款拒绝走历史接管，实际付款主体按项目冻结主体解析并保留主体间往来，跨主体结算核销同步形成主体间往来；历史期初结算复用正式结算来源并优先使用已保存结算截止日，已应用历史结算更正会从当前源行重建原始快照，后续更正以 append-only correction/reversal 事实接入 source replay/compare；历史实付分配更正不改现金，仅调整对应应付/余额影响，并以更正 ID 隔离影响幂等键；余额流水的业务反向更正保留 reversal 语义但以部分 correction 事实接入经营账，避免误冲销整笔历史实付；实际付款主体按冻结主体唯一匹配，施工企业与我方公司同名或多重命中时 fail-closed；余额更正无法唯一绑定历史实付来源时拒绝写入，经营来源依赖缺失时 fail-closed。来源坐标、付款凭证、实际付款主体证据与项目经营账生效日均冻结在来源快照中，未引入独立硬截止日。POL-09 定向 4 套件 40 passed，全量 workspace 测试通过（shared-domain 165、Web 1953、API 331 套件 / 6154 passed / 98 skipped），API typecheck、lint 与 `git diff --check` 通过。该条仅表示本地实现和验证；未 push、建 PR、写入或关闭 GitHub Issue、部署、迁移或访问生产。

- [x] POL-10（Issue #103）已从 fresh fetch 锁定的 `origin/main@71011c4a0458e7739fc23f2affa7d61b7c492d92` 在独立 worktree 完成本地实现和收口，最终本地候选 `464663e399bbf4ced18ab458574e6d5ca0a48466` 的完整 release:local 门禁通过：CI 编排 51/51；shared-domain 176 passed、API 338 suites / 6177 passed / 100 skipped、Web 183 files / 1957 passed；typecheck、lint（0 errors，30 个既有 warning）、build、UI 和全站清单均通过；PostgreSQL 16 exact-SHA 动态门覆盖 133 migrations、91 tests / 38 files，remaining=0，终点迁移为 `20260816110000_pol10_operating_takeover`；P0 浏览器 3 passed / 2 skipped，RC-06 mock 14 passed。组合 Excel 按工作表解析且原始文件留存；页面编辑、批次/行级附件和敏感操作二次确认；员工垫付与施工企业扣费的受控类型、主体/资金/成本语义；contract_director 创建权限、激活状态推进，以及来源重放时更正/冲销元数据保真均已收口。PR #139 与 Issue #103 的 GitHub 合并/关闭待本候选完成；未执行生产迁移、部署或生产状态写入。
- [~] POL-17（Issue #110）已从 fresh fetch 锁定的 `origin/main@b827700104675fd3a62a22ed3709c12ca5e767e3` 在独立 worktree 实现：shared-domain 提供代码注册的封闭场景、主单/明细行层级与改名追溯、完整中文字段/展示/Excel/批量契约、受控选项、敏感/岗位权限、只读 import 门、格式/日期/精度/跨字段校验、版本失败关闭和正式对象目标；API 提供 `getSceneDefinition`、`validateDraft`、`freezeSubmissionSnapshot`，正式提交受项目级岗位守卫保护，`project_operating_profile` 通过既有项目经营档案领域服务在同事务内写入正式字段、按 revision 保留不可变快照并审计，未知场景/字段、越权项目、非 P2002 数据库异常均失败关闭，未提供用户自由搭建规则写入口，未迁移既有页面。新增 Prisma Schema/迁移、不可变 trigger、动态门终点 checksum 与真实 PostgreSQL 约束测试；shared-domain 全量 17 文件 / 171 passed，API 定向业务字段套件通过，shared/API typecheck、lint、Prisma validate、动态门静态 13/13 通过；全量 API 334 suites / 6164 passed / 100 skipped；最终 Spec/Standards 代码复审无 P0/P1；候选已在本机 disposable PostgreSQL 16 动态门通过 132 个迁移、37 个动态测试 / 14 个套件，终点迁移 checksum 与 manifest 对齐，临时容器已清理。待最终 SHA 复跑该动态门后 push、PR、CI、merge 和关闭 #110。未执行生产迁移、部署或生产状态写入。
- [x] POL-18（Issue #111）已从 fresh fetch 锁定的 `origin/main@5f5817a09cbb7dba22e61da4b34a714745316ec3` 在独立 worktree 完成统一业务录入适配层，并基于候选 `355b05024e3d0da5dc92400034cbdfe89d310702` 收口审查修复：Web 表单、桌面表格、复制粘贴、手机卡片与只读展示共用 POL-17 定义和值契约；日期保持 Asia/Shanghai 本地 date-only，有限数字转 number、非法原值交后端权威校验；三种可编辑入口按 `visibleWhen` 固定点剔除单层/多层隐藏值，批量入口统一强制 `bulk.enabled` 与最小 `maxRows`，手机过期草稿只读。桌面对象/枚举字段用显式 opt-in 可搜索选择器显示中文 label、提交技术 value，空选项不降级自由文本，未知值不泄漏且编辑其他单元格时保留原始值等待后端失败关闭。Excel 上传在解析前校验 `.xlsx`、MIME、ZIP 签名、中央目录与本地头，并以每项 20 MB / 总计 50 MB 的硬输出上限核验实际解压大小和总条目；同时限制 dimension、实际 row/cell/col 坐标，拒绝 mergeCell，模板与导入共用最严 bulkLimit，真实 XLSX、伪造大小、目录洪泛、稀疏坐标和模板往返回归通过。组件行为由真实 SSR/harness 覆盖；最终 Standards/Spec 双轨复核均无 P0/P1/P2。最终验证：Web 定向 6 文件 / 23 passed、API 定向 2 suites / 11 passed；shared-domain 全量 18 文件 / 176 passed、Web 全量 190 文件 / 1983 passed；API 全量在 sandbox 中 331 suites / 6129 passed / 100 skipped，另 8 suites / 68 tests 仅因 `listen EPERM: operation not permitted 127.0.0.1` 失败，未扩改产品代码。两侧 typecheck/lint（Web 仅 #101 既有页面 30 warnings）、Web `check:ui`、业务错误/中文语言门、路由 478 与 Web API 464 wrappers / 486 bindings、合同能力矩阵单项检查及 `git diff --check` 均通过；正式生成已将合同能力矩阵运行时路由数由 476 同步为 478，并同步 `docs/product/manifests/web-page-actions.json`、`docs/product/manifests/whole-site-capability-matrix.json` 与 `docs/product/whole-site-capability-matrix.md`，聚合 release manifests、page-action ready 与 whole-site capability ready 均通过。未修改 pages、shared-domain、Prisma、#101、POL-19 或 PR #129，未执行生产数据库/COS/部署/迁移或真实业务写入。
- [x] POL-19P1（Issue #143）在 fresh fetch 锁定的 `origin/main@2b39687a6fc10f07341249e0bdaa8ed045ace4df` 上完成最终候选收口：严格保留 10 文件边界，并以 RED→GREEN 收敛两轮复审的七项 finding——场景访问策略仅显式登记 `project_operating_profile` 与 `OPERATING_TAKEOVER_SCENE_DEFINITIONS`，漏登记场景失败关闭；项目权限恢复 canonical `resolveEffectiveRoleKeys`，global-only `contract_staff` 不得访问任意项目、项目级 `contract_staff` 与合法 global `contract_director` 保持允许；global target 必须通过领域 resolver 确认存在与归属；空 Excel batch 仍执行 target contract；注册期拒绝 target 与 roleScope 不兼容配置，并显式封闭 `target.scope` 仅为 `project|global`、`permission.kind` 仅为 `business_action|role_keys`，未知值不得进入 service 分支。错误域与跨项目 target 均在 validate/freeze 前失败且不保存快照。最终定向 9 suites / 54 tests、API 全量 340 suites / 6217 passed / 100 skipped、typecheck、lint、build、中文业务错误门及完整 `git diff --check` 已通过；Standards/Spec 双轴独立代码复审均无 P0/P1/P2，Spec 指出的旧进度收据已在本行修正；最终候选在本机 disposable PostgreSQL 16 exact-SHA 动态门通过 133 migrations、91 tests / 38 files、9 groups，remaining=0，终点迁移 `20260816110000_pol10_operating_takeover` 及 checksum 与 manifest 对齐，临时容器均已清理。PR #145 的 CI manifest 收口仅通过官方生成器同步 `nest-business-routes.json`、`route-usage.json`、`whole-site-capability-matrix.json` 与 `whole-site-capability-matrix.md`；聚合 release manifests 以 478 routes、464 wrappers / 486 bindings、250 actions、0 blockers 全绿，生成 JSON 未经手工编辑。未修改页面、合同/结算/付款写入、Schema/Prisma migration 或生产配置，未执行生产数据库写入、归零、迁移、部署或 COS 改动。
- [x] POL-19P2（Issue #144）在 fresh fetch 锁定的 `origin/main@cb19e9e2e767d44b598c5dafa398d872b0e5e696` 上完成统一录入正式快照事务原子性契约：新增受限 `freezeSubmissionSnapshotInTransaction` / `saveInTransaction`，只复用调用方传入的 Prisma transaction client，不由 joined 路径开启独立事务；既有单独冻结改为显式 `saveStandalone` 并继续自行管理事务。项目场景的正式快照、项目经营档案更新和审计沿同一 transaction client 执行；外层业务写入失败与快照冻结失败两条路径均有回滚测试，独立冻结路径仍合法。全局场景因本票没有可证明的同事务正式持久化 store，joined 路径以中文错误失败关闭，RED→GREEN 证明缺少该保护时只会返回内存快照，恢复后两个快照存储入口均不执行。定向 3 suites（2 passed、1 skipped）/ 25 passed / 5 skipped、API 全量 340 suites / 6222 passed / 103 skipped、API typecheck/lint/build、中文业务错误门和 `git diff --check` 已通过；本条与 7 个实现/测试/动态门文件保持同一最终提交，PostgreSQL 16 exact-SHA 动态门及 Standards/Spec 双轴复审必须以该提交的 PR/Issue 收据为准。未修改页面、Schema/Prisma migration、合同/结算/付款等领域写入或生产配置，未执行生产数据库写入、归零、迁移、部署、COS 改动或正式开放。

### P0：已删除草稿生命周期

#### Contract Architecture V3 父票 #53：文档验收收口

- [x] 2026-08-12 fresh fetch 后在干净、detached 的独立 worktree 复核：`origin/main` 与 `HEAD` 均为 `835885fc193e9170eb0d9179c18b122c37cf7ae3`。GitHub 只读事实为 #54–#67 全部 `CLOSED`（`COMPLETED`），对应 PR #69、#71、#72、#77、#81–#88、#90、#91 全部已合并；其 merge SHA 均为该精确 HEAD 的祖先，PR #91 的 merge commit 即该 HEAD，并关闭 #61。
- [x] 父票 8 个 user stories 的源码/契约验收账本如下；“完成”只表示父规格在该 SHA 上具备可追溯的合并实现，不表示生产观察、迁移、发布或物理删除已完成。

| Story | 状态 | 合并证据与验收结论 |
| --- | --- | --- |
| 1. 恢复数据只允许显式保存 | [x] | #54 / PR #69 与 #66 / PR #88；恢复数据只供检查，继续由 `useContractDraft` 的显式命令保存。 |
| 2. 删除结果不确定时冻结 autosave | [x] | #54 / PR #69；`deleting`、待重试和结果未知均保持冻结，fresh server state 与 current lease 决定是否恢复可写。 |
| 3. 单一 version/revision/lease/capability snapshot | [x] | #55 / PR #71、#62 / PR #83、#66 / PR #88；页面不再拼接第二份 authority，完整回执漂移时 fail-closed。 |
| 4. 签章确认只随文书内容变化失效 | [x] | #63–#65 / PR #84–#86 与 #67 / PR #87；`documentContentRevision` + canonical fingerprint 承担文书有效性，metadata-only `draftRevision` 不误伤确认。 |
| 5. 双边确认并发下最多激活一次 | [x] | #61 / PR #91；公开 side-confirmation 路由、固定锁序、幂等回放及 PostgreSQL 冲突映射已合并。 |
| 6. 旧客户端在 cutover 后一致收到 410 | [x] | #57–#60 / PR #77、#81、#82、#90；目标旧写入均为 all-mode tombstone，旧 authority 不再投影。 |
| 7. tombstone 可观察后才允许物理删除 | [x] | #56–#60；运行时 route/authority manifest 保留可观察 tombstone 分类。生产观察与物理删除仍未执行，继续要求独立授权。 |
| 8. route usage、cutover、迁移与 exact-SHA release 分别授权 | [x] | 父规格的授权分层保持有效；本次只把文档验收绑定到 `835885fc193e9170eb0d9179c18b122c37cf7ae3`，不构成 exact-SHA 生产发布收口。 |

- [x] 父票限定的 4 个 public seams 已在该 SHA 上逐项复核：

| Public seam | 当前验收事实 |
| --- | --- |
| `ContractDraftAggregateService.saveAggregate` | 仍是完整 aggregate snapshot 的唯一普通成员 writer，覆盖 snapshot、lease、revision、idempotency、invalidation 与 receipt；#62、#63 已合并。 |
| `useContractDraft` | 继续统一拥有 recovery、save queue、deletion/retry、lease、readonly 与 lifecycle authority；#54、#55、#66 已合并，未保留第二个 session owner。 |
| Takeover side-confirmation interface | 保留合同侧/财务侧 confirmation、双方 withdrawal 与 change-baseline-confirmation public routes；#61 / PR #91 合并后，旧总确认 POST 仍保持 #60 的 tombstone。 |
| Runtime route manifest / capability projection | `contract-mutation-authority.json` 为 `ready`：80 routes、1 个 aggregate writer、55 个 governed commands、24 个 exit candidates、0 blocker；`route-usage.json` 的 14 条 V3 目标旧写入带 tombstone 标记，且不可用动作不再广告。 |

- [x] 明确保留非生产边界：本收口没有修改业务代码、路由、测试、Schema、迁移或 ADR，没有写入/关闭 GitHub Issue #53，没有读取生产日志或执行生产检查、Gate 4、迁移、部署、route 物理删除、push 或 PR；本地文档 commit 不产生生产效果，也不授予下一步动作。#53 继续保持 `OPEN`，关闭 Issue 需另行授权。
- [x] 下列 #54–#67 详细条目保留各票实施时点的验证收据；其中“未推送 / 未建 PR / 未关闭”等原时点边界不再代表当前 Git 状态，当前合并结论以本节 fresh 核验为准。

- [x] Issue #61（双边确认并发与单一 lifecycle authority）已在固定、detached 且开工前 clean 的基线 `91e49ba56cd9f9ec95b0a99471961664cbea4394` 完成本地 RED→GREEN TDD 与 Standards/Spec 双轴 fixed-point 复核，当前保持未提交：既有 `ContractTakeoverService` 继续作为单一 lifecycle Module authority，以接管根为首的固定锁序一次锁定双方事实、实付、凭证、合同、版本和付款条款；内部 activation Implementation 仅执行已锁定、已判定的 aggregate，不再自行重锁或重判。公共 Interface 仅保留既有合同侧/财务侧 confirmation、双方 withdrawal 与 change-baseline-confirmation 路由；未恢复 #60 旧总确认 POST，未新增公开 test Seam、Adapter、repository、facade 或通用并发框架。真实 PostgreSQL `40001` 与 Prisma `P2034` 均稳定映射 HTTP 409，服务端不自动重试；同幂等键在冲突后由 fresh HTTP request 重放并再次 fresh-read 得到同一最终回执。一次性本机 PostgreSQL 16 容器部署 125 个既有迁移后，公开路由与余额并发套件 5/5 通过并自动清理，覆盖 wait/activate、唯一 confirmation receipt/activation audit/期初结算、两笔历史实付与凭证、600 期初结算 + 100 历史预付款、主体版本冻结，以及确认与撤回/基线确认竞争；四组竞争均由数据库 `PgSleep` barrier 证明真实重叠。contract-takeover 领域回归 344/344、API 全量 6044 passed/76 skipped、typecheck、lint、动态门 manifest 9/9 与 `git diff --check` 通过；双轴复核 0 findings。未新增 Schema/迁移，未访问任何非 localhost 数据库、生产、日志或 COS，未执行 Gate4、部署、push、PR、Issue 写入/关闭、分支或 commit；本地临时数据库证据不代表生产效果。
- [x] #66 local implementation started（本地实施完成，GitHub Issue 未更新/关闭）：基于新鲜基线 `5c997ec18d38aedb493300a4d7bc9a8885ac8eda` 完成 session extraction deletion test，未保留独立 session candidate，仅深化现有 `useContractDraft.lifecycle` 的只读状态与命令边界；`ContractWorkbenchPage.vue` 删除页面级生命周期编排与重复 authority 状态，最终净删 126 行。公开 seam 的恢复、save queue、删除/重试、lease、readonly 与可见页面动作/结果目标测试 86 项通过，相关结构契约合计 122 项、Web 全套 1950 项、typecheck、lint、`check:ui` 均通过；Standards / Spec 双轴固定点复核均 0 发现。未修改 Schema/迁移，未运行数据库迁移或动态 gate，未执行 GitHub Issue 变更、push/PR/merge、部署、生产访问或 COS 操作，也未启动 #67。
- [x] Issue #54（合同工作台：恢复与原始草稿删除状态安全）已由 PR #69 squash 合并至 `main`（合并 SHA `02764934888b5fc73508561a58028baf87937dee`），GitHub Issue #54 已 `CLOSED`（`COMPLETED`）：恢复较旧本地副本只供人工检查且不再自动排队保存；原始草稿删除请求结果未知或服务端确认待重试时撤销本地租约并冻结 autosave/写入，只有重新读取服务端状态并取得当前租约才恢复可写。已通过目标 Web Vitest 161 项、typecheck、lint 与 `check:ui`；未运行生产检查、迁移或部署。
- [x] Issue #55（合同工作台：单一 authority snapshot）已完成本地实施：`useContractDraft` 对同一份工作台读取收据统一投影 exact contract version、draft revision、capability、lease、readonly 与 lifecycle；页面不再二次读取 capability 后拼装可写结论，版本/修订漂移或 capability 撤销时 fail-closed。#54 的恢复与删除结果未知冻结边界保持不变。目标 Web Vitest 87 项、Web Admin typecheck、全仓 lint、`check:ui` 与 `git diff --check` 已通过；全仓 typecheck 仍受既有 API Prisma client 未生成阻断。未改 API、Schema、迁移、部署或生产数据/COS。
- [x] Issue #56（合同 mutation authority 收敛）已获授权按当前边界收口：以运行时 route manifest 的 80 条合同 mutation 为唯一分类输入，分别登记 aggregate member writer、受治理专用命令与 exit candidate；`saveDraft` 是唯一 aggregate writer，退出候选不出现在合同草稿 capability projection，未知 action fail-closed。旧 `POST /projects/:projectId/contract-takeovers/:takeoverId/confirmation` 仍被生产工作台调用，且 ADR 0001 要求它全模式 fail-closed，authority manifest 因此保留 `blocked`（1 个 `routeAuthorityDrift`）作为后续 #57 的显式门；本票未移除消费者、tombstone route 或改变 cutover mode。最终 typecheck、lint、`check:ui`、目标 API Jest 39 项、authority manifest Node 测试 5 项与 CI 编排 48 项通过；未改 Schema、迁移、部署、生产数据或 COS。
- [x] Issue #57（普通 party 写入 all-mode tombstone）已完成本地 TDD、双轴复核与提交：三条普通 party 写路由在 `release-a`、`maintenance`、`release-b-maintenance`、`release-b` 均在业务 service 前一致返回 `410 CONTRACT_WORKBENCH_CLIENT_UPGRADE_REQUIRED`，无 feature flag、fallback 或第二 aggregate-member writer；party 修改仅保留 `saveDraft` aggregate writer 或既有受治理命令。运行时 route manifest 与 route-usage 均标记 `contractCutoverTombstoneWrite: true`，authority manifest 将三条路由分类为 `tombstoned_cutover_exit`，不投影普通 party action。仓库扫描未发现应用生产消费者；本地 `prisma/verify-contract-workbench.cjs` 验证脚本仍会命中旧路由，未运行或迁移。目标 API Jest 15 项、manifest/capability Node 测试 142 项、全仓 typecheck/lint 与 `git diff --check` 通过；全仓测试仅有两个未改动 Web 结构断言失败（`contract-list.config.test.ts`、`contract-change.structure.test.ts`），全站 capability matrix 自检仍先受既有 `CAPABILITY_MATRIX_WEB_SOURCE_DRIFT` 阻断。Gate 4 不再是本票实施硬阻塞：用户明确接受未知旧调用方可能收到 `410` 的残余风险；未声称生产零调用、未新增支持票，且未读取生产日志、部署或处置真实调用。生产部署、生产验证及 Issue/PR/push/merge/关闭仍须另行授权。
- [x] Issue #58（旧 checkpoint 创建与恢复写入 all-mode tombstone）已完成本地 TDD、双轴复核：`POST /contract-workbench/:contractVersionId/checkpoints` 与 `POST /contract-workbench/:contractVersionId/checkpoints/:checkpointId/restore` 在 `release-a`、`maintenance`、`release-b-maintenance`、`release-b` 均在业务 service 前一致返回 `410 CONTRACT_WORKBENCH_CLIENT_UPGRADE_REQUIRED`，不调用 checkpoint 创建或恢复写入。`useContractDraft` 的本地 recovery/readonly、current revision、lease 与 capability 边界保持不变（目标 Vitest 80 项通过）。runtime Nest/route-usage manifest 均标记两条路由为 `contractCutoverTombstoneWrite: true`，authority manifest 分类为 `tombstoned_cutover_exit` 且无 checkpoint action 投影；应用代码 caller 扫描只发现 API 模块内两个 test-only wrapper 定义，未发现工作台生产调用，`prisma/verify-contract-workbench.cjs` 的旧路由验证调用未运行或修改。目标 API Jest 24 项、manifest/capability 定向校验、全仓 typecheck/lint 与 `git diff --check` 通过；双轴复核 Standards 无硬性发现、Spec 0 项。未读取生产日志、未访问生产、未声称 route family 的生产零调用、未改 Schema/迁移/数据库/COS、未部署；push/PR/merge/关闭 Issue 仍须另行授权。
- [x] Issue #59（普通 bill 写入 all-mode tombstone）已完成本地 TDD、双轴复核与提交：`POST /contract-bills/:billId/rows`、`PATCH`/`DELETE /contract-bills/:billId/rows/:rowKey`、`POST /contract-bills/:billId/rows/reorder`、`PUT /contract-bills/:billId/rows`、`POST /contract-bills/:billId/excel-imports` 及 `POST /contract-bill-imports/:importId/apply` 在 `release-a`、`maintenance`、`release-b-maintenance`、`release-b` 均在业务 service 前一致返回 `410 CONTRACT_WORKBENCH_CLIENT_UPGRADE_REQUIRED`，无 feature flag、fallback 或第二 bill-write authority。受治理的 `cancelRemainder` command 保持，且其 lease、revision、historical occupancy 与 lineage 不变量未改。runtime route-usage/Nest manifest 均将七条路由标记为 `contractCutoverTombstoneWrite: true`；authority manifest 分类为 `tombstoned_cutover_exit`，capability projection 不投影普通 bill action。caller 扫描未发现应用生产消费者；目标 API Jest 50 项、route-usage/authority/page-action/capability ready 校验、全仓 typecheck/lint、全仓测试（shared 158、Web 1949、API 6022 passed/76 skipped）及 `git diff --check` 通过；双轴复核 Standards 无硬性发现、Spec 0 项。独立 route-usage manifest Node 基线中 `GET /contract-workbench/:contractVersionId/offline-revisions` 仍有既有 page/exit_candidate 断言不符，非本票范围，未修改。未读取生产日志、未访问生产、未声称生产零调用或生产证明，未改 Schema/迁移/数据库/COS，亦未部署；push/PR/merge/关闭 Issue 与生产验证仍须另行授权。
- [x] Issue #60（永久关闭旧工作台保存与旧接管确认）已在固定基线 `37e606e93763c287e75a0d3ce0be10311b3ed51e` 完成本地 TDD、双轴复核与 Git 收口：`PATCH /contract-workbench/:contractVersionId` 与 `POST /projects/:projectId/contract-takeovers/:takeoverId/confirmation` 在四种 cutover mode 均于业务 service 前一致返回 `410 CONTRACT_WORKBENCH_CLIENT_UPGRADE_REQUIRED`，目标路由测试证明旧 `saveDraft`/`confirm` 零调用；旧 Web wrapper、页面确认对话框与 `confirm_takeover` capability 已退出。新 `PUT /contract-drafts/:contractVersionId` 仍为唯一 `aggregate_member_writer`，合同侧/财务侧 confirmation 路由与内部激活链未改，历史接管详情读取四模式保持 200。目标及相关 API Jest 196 项、Web Vitest 223 项、manifest 单元 164/165 项、全仓 typecheck/lint、`check:ui` 与 `git diff --check` 通过；Nest 452 routes、Web API 439 wrappers/461 bindings、route-usage 452 routes/0 unclassified、authority 80 routes/22 advertised actions 与合同工作台 capability 均匹配当前源码。page-action/全站 capability 重新生成后仍分别有 9/10 个既有非本票 consumer-chain blocker；route-usage 单元唯一失败仍是 #59 已登记的 `offline-revisions` page/exit_candidate 基线不符，未扩展处理。未改 runbook（现有测试/校验未要求）、Schema/迁移或 #61；未访问生产、日志、数据库、COS 或 Gate4，未推送、建 PR、合并、部署或变更 Issue；生产 cutover mode、精确 SHA 发布收据与回滚链仍须另行授权验证。
- [x] Issue #62（深化 `saveAggregate` aggregate writer）已完成本地 TDD、双轴复核与提交：基线为重新 fetch 核验的 `1f7a51f0e2dc3def907337e7e74d88529e446d45`。`ContractDraftAggregateService.saveAggregate` 仍是完整 snapshot 的唯一 aggregate-member writer，保留 revision、lease 与 idempotency 边界；成功与冲突回执均给出 server revision、capability 与 invalidation，旧持久化幂等快照会本地回填该契约且转交后不恢复原经办人的写能力。前端仅接受完整 authority 回执并 fail-closed；普通 bill 受治理命令未并入通用 adapter/repository/planner。caller scan 未发现生产调用方；目标 API Jest 39 项、Web API/composable 154 项、受治理 bill command 29 项、全仓 typecheck/lint、Web `check:ui`、route/web-api/page-action/capability/authority ready 校验与 `git diff --check` 通过；双轴复核的 P1/P2 均修复后无遗留项。全仓 `pnpm test` 两次均仅在未改动的 `receipt-watermark.service.spec.ts` 图像渲染超时（第二次 313 API suites passed、1 failed），单独重跑 25/25 通过；未因此修改无关模块。未运行 opt-in PostgreSQL aggregate concurrency 场景，未改 Schema/迁移、未访问生产/数据库/COS、未部署、推送、PR、merge 或关闭 Issue；后续 GitHub/生产动作须另行授权。
- [x] Issue #63（文书内容 revision 与 canonical fingerprint）已完成本地 TDD 与双轴复核：`ContractVersion` 增加 `documentContentRevision` 与 `documentContentFingerprint`；`saveAggregate` 与业务模板切换均从事务内已持久化的主体、付款条款、清单/行、附件、条款、事项/范围、金额/税和选定模板快照生成可审计 canonical SHA-256 fingerprint。lease、幂等、recovery、audit/task 与 negotiation read-model/reference 仅元数据不推进新 revision、不会标记文书失效；空 fingerprint 会经 CAS 本地回填但不推进任何 revision。旧 `draftRevision` 回执和正式归档确认消费者刻意未迁移，待 #64/#65 授权处理。目标 API Jest 47 项、迁移动态门 Node 9 项、受影响 runner-source Jest 44 项、`prisma validate`、authority/capability manifest、全仓 typecheck/lint、全仓测试（shared 158、Web 1949、API 6033 passed/76 skipped）与 `git diff --check` 通过；新增迁移未执行，未访问生产/数据库/COS、未部署、推送、PR、merge 或关闭 Issue。
- [x] Issue #64（乙方签章确认 consumer 迁移至内容 revision）已完成本地 TDD、双轴复核与隔离 PostgreSQL 16 动态验证：上传/确认记录冻结 `documentContentRevision` 与 canonical fingerprint，确认有效性、readiness preflight、提交桥接及 submission snapshot 均按两者联合验证；文书内容变化使既有确认失效，metadata-only 的 `draftRevision` 变化不再误伤确认，Web 只消费后端 `confirmationValid`，`draftRevision` 仅保留聚合并发与追踪职责。目标 API Jest 162 项、全仓 typecheck/lint、`check:ui`、`check:fast`（API 716 passed/6 skipped；Web related 900）通过；全仓测试中的 3 个无关图像/PDF 并行资源超时已单独串行复跑 35 项通过。双轴复核的过时领域注释、误导命名与非公开结构测试均已修正；重复的窄域 fingerprint 校验为避免本票引入通用框架而保留。候选 `13d260a6a062871e2ae5d2af591786c842750d25` 在一次性本机 PostgreSQL 16 容器中成功部署 125 个迁移并通过 remaining dynamic 9 组 42 项（含 `contract-lifecycle-route.spec.ts`），容器已自动清理。以上均非生产证据；未访问生产/数据库/COS，未部署、推送、创建 PR、合并或关闭 Issue，也未启动 #65/#67/#60/#66。
- [x] Issue #65（生成与预览文书 consumer 迁移至内容 revision）已在固定基线 `0d7af8c2720614ec79d2f8a35afa574199700a14` 完成本地 TDD 与双轴复核：真实文书内容变化会将 queued/processing/success 生成文书置为 stale，并以新的 `documentContentRevision` 与 canonical fingerprint 生成新幂等身份；metadata-only 聚合修订不再误伤预览、重试、内部送审 readiness、终态发布或前驱文件链。每份新生成文书在输入快照及 queue/retry/success 审计中冻结两项内容坐标，工作台与文档列表只公开追溯坐标而不公开完整渲染快照；`sourceRevision`/`draftRevision` 仅保留命令并发与聚合追踪。目标 API Jest 230 项、Web Vitest 23 项、全仓 typecheck/lint 与 `check:ui` 通过；全仓测试 shared 158 项、Web 1949 项全绿，API 本票相关套件全绿且全量共 5982 passed/76 skipped，但严格沙箱内 9 个无关套件因 HTTP 监听 `127.0.0.1` 被 EPERM 拒绝或并发图像/PDF/Excel 超时未形成全绿回执，其中 3 个超时套件已串行复跑 47 项通过，沙箱外全量复跑未获审批，故不作完整门禁或发布就绪声明。双轴审查发现的 readiness 聚合修订代理、前驱链聚合修订筛选、工作台快照过度暴露及 CAS-loss 误分支均已修复并复审收敛；未执行 PostgreSQL 动态验证或迁移，未访问生产/数据库/COS，未部署、推送、创建 PR、合并、变更 Issue 或启动 #67/#60/#66，亦未改 #64 乙方签章 consumer。
- [x] Issue #67（移除旧文书 revision 语义）已在固定基线 `49a00eb8e527dc5a75b2669bf230d6ce6fb48cf3` 完成本地纵向红→绿 TDD 与 Standards/Spec 双轴 fixed-point 复核：生成/预览文书、离线差异、磋商前驱、审批原件/乙方签章确认、用章最终归档及详情/工作台 capability projection 只以 `documentContentRevision` + canonical fingerprint 联合判断文书有效性；真实内容变化会 stale/阻断，metadata-only 聚合修订不再误伤，`draftRevision`/`sourceRevision` 仅保留聚合 OCC、命令 CAS 或非有效性追踪。公共 seam 覆盖当前/过期文书投影、离线预览终态、乙方确认/submission preflight、最终归档 HTTP 与 metadata-only R 漂移下的 Web 上传；目标 API 12 suites / 582 tests、Shared 158 tests、Web 1951 tests 全绿，API 全量 338 suites / 6118 tests 为 314 suites / 6042 tests passed、24 suites / 76 tests skipped、0 failed，全仓 typecheck/lint、Web `check:ui`、contract capability/authority manifest 与 `git diff --check` 通过。双轴复核发现的最终归档页面旧 revision consumer、坐标类型强转及上传冻结测试缺口已修复并复审收敛。仅保留 legacy 持久字段为 inert/追踪，未改 Schema/迁移/路由 tombstone/生产门，未运行本地或生产迁移、未访问生产/数据库/COS，未部署、推送、创建 PR、合并或变更 Issue，也未启动 #60/#66。
- [~] 发布门 Web 基线断言修复（独立于 #57）：#57 已由 PR #77 squash 合并为 `f91ee7490c134694dfd5dacf22311c11612e5420`，Issue 已关闭。#55 的单一 authority snapshot 已正确取代二次 capability 请求与可变 action/revision 状态；两个旧结构断言据此更新，并将精确响应版本错配和 authority snapshot 坐标错配分别约束到各自的 `clearAuthoritySnapshot()` fail-closed 分支。目标 Vitest 28/28、Web 全量 1949/1949、全仓 typecheck、lint、Web `check:ui`、`git diff --check` 与双轴代码复核均通过；未改产品源码、Schema、迁移、生产或 COS。待合并后的新精确 `origin/main` 运行完整本地发布门并生成收据；此前不部署、不连接生产。
- [~] 完整本地发布门首轮矫正（独立于 Web 基线断言）：候选 `5b193bb961ab8f5a6c81831277319a3757c68a9b` 已通过 CI 编排、冻结依赖、Prisma client 生成、生产依赖审计，随后在 business-errors-and-operations-safety 被 `CONTRACT_MUTATION_AUTHORITY_OVERLAP` 这一已测试的 authority 重叠 fail-closed 内部不变量缺少 allowlist 项阻断。现仅按精确文件/类型/文本/出现次数及原因登记该内部哨兵；authority 定义、业务响应、Schema、迁移、生产与 COS 均未改。business-error gate、自测、ops safety、authority Jest 4/4、全仓 typecheck、lint、脚本语法、`git diff --check` 与双轴代码复核均通过。待该独立修复合并后，以新的精确 `origin/main` 完整重跑本地门；不复用失败候选的发布证据，且此前不部署、不连接生产。
- [x] Issue #74（脱敏规范化路由观测输入适配）本地 TDD、双轴代码复核与提交已完成：observer 仅在显式 `route-observation-v1` 下读取严格 JSON 行（显式时区、规范化 URI、HTTP method/status、`$upstream_status`），默认 combined 输入保持原有逐行 fail-closed；新报告升级为 schema v2 并由 capability gate 要求 top-level/evidence 双重格式声明，旧 schema v1 报告仍可读取。observer、capability gate 与版本化 targets 合计 47 项 Node 测试和全仓 lint 通过。未改 Nginx/logrotate、未部署、未读取生产日志，未形成生产零调用结论；#57 已在接受残余风险的本地实施范围内完成，仍不构成生产结论。
- [x] 合同草稿删除与签署生命周期规格已收敛至 `docs/specs/2026-08-05-contract-draft-deletion-and-signing-lifecycle.md`。
- [x] 发布门两项独立历史基线曾在旧候选完成修复并复核；其中工作台 authority snapshot 结论仍有效。该旧候选曾将历史接管确认 `POST /projects/:projectId/contract-takeovers/:takeoverId/confirmation` 保留为受治理专用命令，但此结论现已由 Issue #60 的四模式永久 tombstone/`exit_candidate` 结论明确取代，不再代表当前候选。旧候选的 authority、页面动作与发布门证据均不继承；未改 Schema、迁移、生产或 COS。
- [x] GitHub 父 Issue #7 与 13 张子票 #8–#20 已建立，实施顺序和 `Blocked by` 依赖已冻结；当前仅无 blocker 的 #8 标记为 `ready-for-agent`，#9–#20 已移除该标签并须在各自 blocker 关闭后再逐票启用。
- [x] 规格已区分即时清理、审批型记录保留、数据库聚合、COS 全版本/删除标记及共享文件解除关联等边界。
- [x] 规格与 Issues 先于代码冻结；本候选未修改生产数据库、COS 生命周期规则或生产业务数据。
- [x] Issue #8 实现完成并已独立双轴复审：最终驳回已归入已结束、旧 `abandoned` 仅凭可核验旧删除事实进入隐藏清理候选、作废根合同视图互斥，合同台账/工作台按生命周期选择正确版本并收紧未提交草稿读取。全量 jest 295 套件/5761 测试、typecheck、lint、business-errors、ops safety、api/web build、check:ui、`git diff --check` 与四类 `--require-ready` 全部通过；本地 PostgreSQL 16 动态门绑定最终代码 SHA `9042d62034ffe8a4bf8ec14ec704bfac71ac0c4c`（119 migrations、pending 55/55、remaining 31/31、29/29 files、9 groups）。业务轴逐项满足 Issue #8 四项验收标准；代码轴独立复审 APPROVE（0 CRITICAL / 0 HIGH）。已知项：retention worker 暂只匹配 `abandoned` 且处于 preview-only，旧 `deleting` 记录清理归属后续子票；`canReadContractVersionDraft` 未传 `actorUserId` 时默认放行，当前所有调用点均已传值，作防御性观察。
- [x] Issue #9 实现完成（生命周期化合同可见性与台账视图）：后端按角色强制读取可见性三层（10 个全局全貌角色 / 8 个项目全貌角色 / `employee` 仅 7 字段摘要，未配置角色拒绝全貌），合同部主管与 `super_admin` 可在「我的草稿」查看全部未提交草稿（owner 仍仅见自己），正式台账排除草稿与已结束、`ended` 视图收纳已结束历史；前端由 7 标签工作台切换为 4 标签生命周期台账（正式台账/我的草稿/退回待修改/已结束），无权限访问 `my_drafts` 时回退正式台账，正式台账与已结束为只读。门禁证据：contract-read 79 + contract-draft-lifecycle 38 + ledger-read-positions 3 测试通过、API 全量 jest 5793 通过、web-admin vitest 1948 通过、`pnpm typecheck`/`lint`、`check:ui`、`git diff --check`、PG16 动态门 9/9 全部通过。附加：顺带补齐 `system-governance-readonly.config.ts` 缺失的 `abandoned`/`deleting` 状态标签，解除 main 上预先存在的 typecheck 阻塞（纯 label 补全，不改变业务行为）。
- [x] Issue #9 独立双轴复审完成：Spec 轴逐条核对五条验收标准全部 PASS（含拒绝路径与员工 7 字段摘要证据）；Standards 轴独立复审 0 CRITICAL / 0 HIGH / 3 MEDIUM / 2 LOW，已全部闭环修复——① 遗留 `/contracts/workbench` 端点补上可见性拦截（employee/未配置角色不再能经此拿到全量行，新增测试锁定），② 移除 `canReadPrivateDrafts` 死字段并让可见性解析直接返回 `full/summary/none`，③ 角色解析不可用时回退 full 增加一次性警告日志便于观测，④ 员工摘要排除已作废根合同（`voidedAt`），⑤ 前端 `fetchContractWorkbenchLedger` 标记 `@deprecated`（新代码统一走 `fetchContractLifecycleLedger`）。修复后 contract-read 79 + API 全量 5793 + web-admin 1948 + typecheck/lint 全部保持通过。
- [x] Issue #9 已合并关闭：PR #23 已 squash 合并至 main（SHA `1a8fa1058b9d70ef8d072218dda713cd33288284`），GitHub Issue #9 已 `CLOSED`；实现、门禁与双轴复核记录随 PR 进入 main。下游子票 #10–#20 的 blocking edge 依序解锁。
- [x] Issue #10 实现完成（精确文件绑定与版本化 COS 清理接缝）：新增 `VersionedObjectStorage` 接口 + `CosVersionedObjectStorage` 适配器（COS `GET /?versions&prefix=<精确键>` 版本/删除标记枚举、`DELETE /{key}?versionId=` 逐版本永久删除、`isConverged` 新鲜枚举收敛证明；4xx 除 429 判定为确定性错误不重试、429/5xx 才指数退避重试；诊断日志仅暴露对象键指纹）与 `InMemoryVersionedObjectStorage` 测试伪实现；新增 `file-binding-manifest.ts`（基于中心注册表 `NON_RECEIPT_FILE_BINDINGS` 的**行级**引用扫描 + 收货照片 + 替换链两侧，`classifyFileBinding` 纯函数区分 exclusive/shared，共享文件永不进入对象清理候选；每行可追溯到业务记录/对象键/版本快照/绑定类型），`buildContractFileBindingManifest`/`buildFileBindingManifest` 固定 `preview_only`/`executionAllowed=false`；新增 `FileCleanupSeamService`（`previewManifest` RepeatableRead 只读事务、`assertExactObjectKeyScope` 拒绝 uploads/uploads/ 前缀/尾部斜杠/`..`/`\0`/空集、`deleteExactObjects` 精确键删除原语收敛失败抛 `PartialDeletionError` 仅暴露指纹与残留版本数，诊断重枚举失败用 -1 表示未知）；无 HTTP 删除路由、无桶级 lifecycle/versioning/deleteBucket API，生产 COS 生命周期规则与桶配置不变。门禁证据：file 三套件（versioned-object-storage 14 + file-binding-manifest 20 + file-cleanup-seam.service 12）46 测试 + 全量 API jest 5839 通过，typecheck/lint/build/check:business-errors（新增 1 条启动配置保护 allowlist）/git diff --check 全部通过；本地 PostgreSQL 16 动态门绑定最终代码 SHA `8c3a961bff19ed45e44dde0dfeb0e05b1dca6ea9`（119 migrations、9 groups 全部 passed，含新增 `file_binding_manifest` 组在隔离 schema 真实 PG 验证 11 exclusive / 2 shared / 13 对象与替换链分类）。四类 `--require-ready`：route-usage 本体 PASS；web-api/page-actions/capability-matrix 因全站既有 #23 遗留 blocker（`core-flow-read.api.ts` 两个 orphan wrappers `fetchContractWorkbenchLedger` test_only / `fetchContractLifecycleLedger` registry_invalid）按预期 exit 1，本票引入 0 个新 blocker；顺带同步 `nest-business-routes.json`/`route-usage.json` 中 #23 `LEDGER_READ_POSITION_KEYS` 角色变更的 6 条 ledger 路由角色元数据（合法同步，非本票业务改动）。
- [x] Issue #10 独立双轴复审完成：业务轴逐条核对 Issue #10 五项验收标准全部 PASS（每对象可追溯业务记录/对象键/版本/绑定类型、共享文件只解除业务关联而独占文件才进入对象清理候选、适配器测试覆盖当前/历史版本/删除标记/重试/幂等、默认只读且拒绝 uploads 整体前缀作为删除范围、生产 COS 生命周期规则与桶配置保持不变），七项安全边界（不连生产 COS、不删真实对象、无 HTTP 删除路由、preview-only 默认、禁 uploads 前缀删除范围、仅精确键删除、日志仅暴露指纹）全部符合；代码轴独立复审 APPROVE（0 CRITICAL / 0 HIGH / 2 MEDIUM / 1 LOW）且已全部闭环修复——① COS HTTP 4xx 除 429 判定为非确定性错误不再空转 3 次退避重试（新增测试锁定），② 不收敛诊断重枚举失败时残留版本数用 -1 表示未知而非误报 0（新增测试锁定），③ 重构 `withObjectStorageRetry` 消除不可达的兜底抛出并使末次重试不再执行无用退避（行为不变）。修复后 file 三套件 46 测试 + 全量 API jest 5839 + typecheck/lint/build 全部保持通过。
- [x] Issue #10 已合并关闭：PR #24 已 squash 合并至 main（SHA `91c9be5fa7f7589e98fe34e8581cbc885fa53f65`），GitHub Issue #10 已 `CLOSED`；实现、门禁与双轴复核记录随 PR 进入 main（含 file 三套件 46 测试、全量 API jest 5839、PG16 动态门 9 groups 全过、CI 仅 manifests 步骤因全站既有 #23 blocker 按预期失败）。下游子票 #11–#20 的 blocking edge 依序解锁。
- [x] Issue #11 实现完成（生成无水印外发合同文件并提前分配正式编号）：`ContractDocumentPurpose` 新增 `external`；首次 external 生成在 queue 事务内 `allocateDaily(tx, "HT")` 分配正式编号并永久锁定，重复生成复用编号（`updateMany({where:{code:null,voidedAt:null}})` count!==1 抛错，失败不产生半成品编号），提交审批路径复用已锁定编号；`renderValues` 对 external 输出空水印，renderer 新增 `allowBlankWatermark` 策略仅豁免 `document.watermark`（其余必填校验不变）；`processDocument` 按 purpose 传 `{ allowBlankWatermark: job.purpose === "external" }`，`failDocument` CAS 补上 `sourceRevision` 守卫与 success/stale 对称；前端移除用途分段选择，改为单一「生成合同文件」（purpose: external），`purposeLabelMap` 保留 legacy 读取展示。门禁证据：contract-document 六套件 + file.service 回归全绿、API 全量 jest 5850、web-admin vitest 1949、typecheck/lint/build/check:business-errors、`git diff --check` 全部通过；四类 `--require-ready`：route-usage 本体 READY（446 routes、0 unclassified），web-api/page-actions/capability-matrix 因全站既有 #23 blocker（`core-flow-read.api.ts` 两个 orphan wrappers）按预期 exit 1，本票引入 0 个新 blocker；CI manifests 步骤 `inspect:contract-workbench-capabilities` 抛 `CAPABILITY_EXIT_CANDIDATE_CONSUMER_PRESENT`（`GET /contracts/lifecycle-ledger` ← `fetchContractLifecycleLedger` ← `ContractListPage.vue`），经临时 worktree 在干净 main（`706c6e41`）复现同一失败，确认三者文件本票零改动，属 #23 既有失败、非本票引入（与 #23/#24 合并时 "Release gates FAILURE" 放行惯例一致）；本地 PostgreSQL 16 动态门绑定最终代码 SHA `e11a1ae503ff593b45ef461dbd14b151106e1fa2`（119 migrations、32 tests、9 groups 全部 passed）。
- [x] Issue #11 独立双轴复核完成：业务轴逐条核对五条验收标准全部 PASS（首次生成分配并锁定编号/重复生成复用、DOCX 与 PDF 均无水印、下载走实时权限校验 + 审计日志、失败不产生半成品编号或错误文件绑定、扩展期保留旧用途读取兼容且有测试保护）；代码轴独立复审 APPROVE（0 CRITICAL / 0 HIGH / 1 MEDIUM / 1 LOW）且已闭环——① MEDIUM：`failDocument` CAS 补上 `sourceRevision` 守卫，防止并发翻 stale 后误标 failed（新增测试锁定），② LOW：前端 `purposeLabelMap` 与后端 `PURPOSE_FILE_LABELS` 存在显示层重复，属既有模式且带回退保护，记录接受不做跨包重构。修复后 API 全量 5850 + typecheck/lint 保持通过。
- [x] Issue #11 已合并关闭：PR #25 已 squash 合并至 main（SHA `8045b42c3e455b0af1675e6dab98519ced51ee22`），GitHub Issue #11 已 `CLOSED`（COMPLETED）；实现、门禁与双轴复核记录随 PR 进入 main，CI 唯一失败为 manifests 步骤既有 #23 blocker（`inspect:contract-workbench-capabilities` 抛 `CAPABILITY_EXIT_CANDIDATE_CONSUMER_PRESENT`，已在干净 main `706c6e41` worktree 复现同一失败，本票零改动），重跑受 GitHub runner 基础设施抖动影响。下游子票 #12–#20 的 blocking edge 依序解锁。
- [x] Issue #12 实现完成（接收并冻结灵活格式的乙方签章文件）：复用 `ContractFormalFile` 表新增 `counterparty_signed`（每个原文件一行）与 `counterparty_signed_preview`（规范化预览）两种 purpose，**无 Schema 迁移**。规范化策略：单 PDF inline（preview fileId=原文件、不新建对象）、单 DOCX 用 libreoffice 转 PDF、多图用 pdf-lib 合并 A4 PDF（等比居中、按 magic bytes 识别 PNG/JPEG）；混合格式拒绝并提示分批上传。upload/confirm 在 Serializable 事务内执行：`lockEditableVersion` + `sourceRevision` 与 `draftRevision` 一致性检查 + 旧批次 superseded + 审计（`contract.formal_file.counterparty_upload` / `counterparty_confirm`）；确认记录操作者、时间与 `confirmedAtRevision`，list 实时计算 `confirmationValid`（草稿修订变更后自动失效）。文件访问复用 `governedContractFileAccess`（无 purpose 过滤，与既有 `approval_original` 同标准，实时权限校验 + 下载审计）；`governedSigningFacts` 过滤 counterparty purpose 防止泄漏到既有 formal-files 视图。前端新增 `ContractCounterpartySignedFilesPanel`（t-upload 多文件 auto-upload=false + 一次整体确认），三个新 wrapper 经 workbench capability action 门控（`upload_contract_counterparty_signed_files` / `confirm_contract_counterparty_signed_files`）。门禁证据：API 全量 jest 5864、web-admin vitest 1951、typecheck/lint/build、`git diff --check` 全部通过；CI manifests 步骤 6 项全绿（contract-workbench capability matrix `--check` matched、whole-site capabilities 449 routes OK、web-api 435 wrappers/455 bindings、page-actions 244 actions/0 blockers、route-usage 449 routes/0 unclassified、capability-matrix 449 routes/0 blockers）；本地 PostgreSQL 16 动态门绑定最终代码 SHA `cf72822d977a479c4f8b85f6d82743ad99f9246f`（119 migrations、32 tests、9 groups 全部 passed）。修复记录：CI 首跑在 manifests 步骤因 `inspect:contract-workbench-capabilities` 检出 `contract-workbench-capability-matrix.md` drift 失败（3 条 counterparty 路由未登记、面板未入调用方列表），以 `--write` 重新生成并提交 `c2bb6722` 修复，6 项校验全部复跑通过。
- [x] Issue #12 已合并关闭：PR #26 已 squash 合并至 main（SHA `75e8f4279f9325a24e903cec208e2e4307c38afe`），GitHub Issue #12 已 `CLOSED`（COMPLETED）；实现、门禁、独立审查与修复记录随 PR 进入 main，CI Release gates **success**（manifests 步骤 6 项校验全绿），下游子票 #13–#20 的 blocking edge 依序解锁。
- [x] Issue #13 实现完成（切换审批提交并退出强制磋商流程）：提交审批由「强制 approval_original PDF + 五项签章声明 + 磋商硬阻断」切换为「乙方签章文件整体确认」。核心桥接设计：`contract-formal-file.service` 的 `assertReadyForSubmission`/`freeze` 重构为校验 `counterparty_signed_preview` 确认状态（active、sourceRevision 匹配、confirmedByUserId 非空），提交事务内从已确认 counterparty 预览创建 `purpose: "approval_original"` 桥接记录（fileId 指向 counterparty 预览文件，五项旧字段置 true 并附加 `_counterparty_confirmed` 元数据追溯来源），审批/用章/归档下游代码（contract-seal、governedSigningFacts）零改动；`contract-readiness.service` 移除整块磋商硬阻断（requireInternalReviewDocument 分支：open_round、incomplete_comparison、pending_difference、confirmed_candidate_mismatch、document.failure、document.internal_review），counterparty 检查改为确认状态（未确认 → `counterparty_signed_not_confirmed`、sourceRevision 不匹配 → `counterparty_signed_stale`）；`contract.service` 提交审批 `readiness.check(..., false)` 不再触发磋商阻断；磋商写接口 5 条（offline-revisions、negotiation-rounds、rounds/close、differences/disposition、offline-revisions/retry）与旧 approval_original 上传入口标记 `@ContractCutoverLegacyWrite()`，release-b 模式返回 410 `CONTRACT_WORKBENCH_CLIENT_UPGRADE_REQUIRED`，读接口只读保留旧历史；授权变化时 `contract-authorization.service` 同步 supersede active counterparty 记录。前端：`ContractWorkbenchPage` 移除磋商 section/canvas 挂载与磋商事件处理、`ContractFormalDocumentSection` 不再挂载（组件文件保留，后续单独迁移删除）、`ContractCounterpartySignedFilesPanel` 增强提交前置提示（未确认提示「请完成乙方签章文件确认后才能提交审批」）。新增受控下载 helper `downloadPrivateFileByTicket`（capability 检查 + 创建短时效 ticket + apiFetch downloadUrl 的复合 wrapper，本地 handler 带 early-exit capability 门控），将 `GET /files/:fileId/download` 的 signed_ticket_delivery 证据解析到生产 wrapper，修复 SIGNED_TICKET_EVIDENCE_MISSING。门禁证据：新增 route-level 退役验证 spec `contract-document-cutover-route.spec.ts`（7 测试：release-b-maintenance 下 6 个退役写接口返回 410 且 code 正确、读接口 200 只读可查）、API 全量 jest 5877、web-admin vitest 1936、typecheck/lint/build/check:business-errors/check:ui/`git diff --check` 全部通过；6 项 manifest 门禁全绿（contract-workbench capabilities matched、whole-site capabilities 449 routes OK、web-api 436 wrappers/457 bindings、page-actions 236 actions/0 blockers、route-usage 449 routes/0 unclassified、capability-matrix 449 routes/0 blockers）；`retired-web-api-wrappers.json` 登记 contract-negotiation.api.ts 8 个磋商 wrappers 与 `uploadContractFormalApprovalFile`（test_only）；本地 PostgreSQL 16 动态门绑定最终代码 SHA `564fb6d3834a9e066180675264290f22e09d59bc`（119 migrations、55 covered tests、29 files、remaining 31/31、9 groups 全部 passed）。本票无 migration/schema 变更，未修改生产数据库、COS 对象或生命周期规则。
- [x] Issue #13 最终 PG16 证据与记录校正（以上初始动态收据中的“无迁移/119 迁移/SHA 564fb6d3”已由本项取代）：真实 `POST /contract-drafts/:id/submission` 现覆盖受管草稿的提交、同幂等键重放、两张 active `counterparty_signed` 原件与已确认 preview 的冻结绑定、撤回后回 draft 且保留首次提交时间、双方签署冲突 409，以及末次审计失败的整事务回滚；测试同步生产 `apiJsonReplacer`，避免 BigInt 响应路径在测试中失真。新增唯一前向 M120 `20260808070000_contract_counterparty_signed_formal_files`：purpose CHECK 增加 `counterparty_signed`/`counterparty_signed_preview`，并将 active unique index 精确收窄到 `approval_original`、`counterparty_signed_preview`、`mutually_signed_final`，因而只允许多条乙方原件；无业务数据 DML、无 COS 或生产操作。所有动态 runner 与 4 份自检测试同步为 120/M120，迁移 SHA-256 `5c0b58929a5a8d53ff1858b4708b5f5aa9b9bdc5cb34c8cb92aadf0e4797ffce`。最终代码 SHA `d74edf19ffadde32fd44baeaf7fa390c62cf53d4`：API 全量 Jest 5878 passed（66 skipped）、web-admin Vitest 1936 passed、typecheck/lint、API/Web build、business-errors、check:ui、6 项 manifest ready、`git diff --check` 全部通过；目标和全套本机一次性 PostgreSQL 16 动态门均以该精确 SHA 成功退出（55 项/29 文件/120 migrations，含真实 Issue #13 路由）。独立代码/迁移轴复审 0 P1/P2；未修改生产数据库、生产业务数据、COS 对象或生命周期规则。
- [~] Issue #13 PR #33 CI 依赖审计修复：GitHub run `31245157015` 在 `pnpm audit --prod --audit-level high` 因传递依赖 `nanoid@3.3.12` 的两项 high 公告失败，未进入后续门禁；根级 pnpm override 将其精确锁定为 `3.3.17` 并更新 lockfile。新候选已本地通过冻结锁文件安装、同一 production audit（仅余 5 项既有 moderate）和 Web 生产构建；未改业务代码、Schema、迁移、生产数据库/数据或 COS。旧 SHA `d74edf19` 的动态门证据不自动延续，待此分支新 HEAD 的 GitHub CI 重新执行。
- [~] Issue #13 PR #33 PG16 动态门计数修复：GitHub run `31246043039` 已通过依赖审计、typecheck、lint、全量测试、两端构建和 UI 检查，但在精确 SHA 动态门应用 120 个迁移后，`payment-execution-concurrency.spec.ts` 仍断言 119 个迁移而失败；静态复核同时发现 `settlement-draft-lifecycle-concurrency.spec.ts` 的同类遗漏。两处仅同步 `EXPECTED_MIGRATION_COUNT` 至 120；两个临时 localhost PostgreSQL 16 runner 均已通过并自动清理。当前工作树再次通过全仓 typecheck/lint/test（API 5878 passed、Web 全量通过）、API/Web build、business-errors/ops safety/check:ui、production audit（5 项既有 moderate）及六项 manifest ready；待提交后以新 SHA 重跑全套 PG16 动态门和远端 CI。未改业务逻辑、Schema、迁移或生产资源。
- [~] Issue #14 已完成本地实现，待独立复审/合并：双方最终归档原件接受私有 PDF、DOCX、PNG/JPEG 扫描件，保留原始摘要并只校验文件完整性，不再要求与审批原件页数、字节或版式一致；合同员上传接口强制 `contract.archive.upload`，合同部主管语义确认不再重试密码且仍写审计，确认前可替换（旧最终件 superseded）而确认后冻结选定版本。正式合同文件每次 ticket 创建与实际读取均实时重验权限并记录下载审计；`ContractFormalFile` 绑定继续排除所有正式文件，草稿候选查询仅限 `abandoned` 原始草稿，`effective`/`superseded`（含历史生效版本）永久排除。补齐读模型：仅受治理的 `confirm_final_contract` capability 返回 `requiresPassword=false`，旧 `confirm_archive` 和其他敏感动作保持原密码要求。未改 Schema、迁移、生产数据/COS 生命周期或合同/收款凭证水印。验证：API 聚焦 6 套件 549 tests、Web 聚焦 4 文件 169 tests、两端 typecheck/lint、`check:ui`、API build，以及 6 项 manifest（含 built Nest route matrix）均通过。
- [~] Issue #14 CI manifest 修复（已合并基线 `a54ce777`）：仅重生成正式归档上传路由的权限派生清单及 capability 哈希；未改业务源代码、Schema、迁移、COS 或 #16/#17，待此修复提交复审。
- [~] Issue #15 已完成本地实现，待独立复审/合并：合同经办人兼**当前**合同部主管保留合同部主管冻结节点并在该节点提交理由+当前密码自审，项目经理、财务主管、董事长/总经理节点仍冻结且排除申请人；审批日志归因实际岗位、节点与被代表业务身份。双方最终版上传与同人归档确认均由服务端限定为同一冻结经办人且当前合同部主管（上传亦可为当前项目合同员），读模型仅在该精确例外开放 capability；两项审计统一记录服务器派生的 acting role、归档节点、用章任务、经办人和合同业务身份；`super_admin` 未获业务身份扩展。为落实服务器当前任职控制，受治理审批的直接冻结候选统一按当前岗位拒绝，结算、付款、项目支出、待办与权限守卫回归均已锁定调岗拒绝；常驻委托夹具则明确要求委托人仍在当前岗位。CI 补救还恢复了董事长/总经理的既有自审错误文案，并给合同部主管提供独立文案；合同审批测试夹具模拟了实际的现职查询。验证：API 聚焦 11 suites / 601 passed（1 skipped）、shared 45、Web 聚焦 144、Chromium 自审/同人确认 2/2、API 全量 300 suites / 5,897 passed（21 suites、66 tests 按既有配置跳过）、两端 typecheck/lint、`check:ui`、`inspect:release-manifests`、`git diff --check` 均通过；未改 Schema、迁移、生产数据或 COS。
- [~] Issue #15 复审补救候选待双轴复核：冻结审批节点按 global/project 作用域与当前任职判定；双方最终件的读 capability、HTTP Guard、写侧上传/返修/确认统一使用“当前全局合同部主管”谓词，普通 archive-files 规则未收紧。旧合同 governed 节点缺少 scope 时仅在节点名称和完整岗位组精确匹配合同路由后推断作用域，无法归类的合同节点拒绝审批；合同审批的读 capability 与 HTTP Guard/写侧透传同一合同 scope 标记，因此全局合同部主管转为项目同名岗位后被一致拒绝，而合法全局主管及项目经理仍可处理各自节点。项目岗→全局岗同一经办人的真实 HTTP/读模型/服务审计链已覆盖；隔离 UAT 现真实执行主管自审（理由+密码+审计）并保留项目经理、财务、领导节点与最终件自确认。静态验证：API 全量 302 suites / 5,918 passed（21/66 既有 skip）、API/Web typecheck/lint、`check:ui`、`inspect:release-manifests` 均通过；动态 PG/Chromium 证据将绑定双轴复审后的最终 SHA。
- [~] Issue #15 RC-06 编排同步：隔离 UAT 的原合同与变更边界均改为“草稿域上传乙方签章 PDF →（原合同仅验证确认前 4xx 且仍为草稿/无审批实例）→ `formal-files/counterparty` 整体确认 → `approval-submission`”，runner 已无 legacy `formal-files/approval`；变更 9.99%/10% 允许和 10.01% 阻断均在确认后执行。静态审计枚举全部合同提交和 approval/counterparty/final 请求体与现行 controller DTO 对齐，最终确认不发送已废弃的 `confirmationPassword`；新增断言先 RED 后 GREEN，runner Node 9/9 与 Nest runner 5/5 已通过。新的动态 PG/Chromium 证据仍待候选双轴复核后重新绑定。
- [~] Issue #15 RC-06 浏览器收据修正：主管经办人最终版自确认旅程从治理收据的 `contract_director_handler_self_archive` 已通过用例解析并校验真实合同 UUID 后路由；浏览器进程只写 `pending` 收据，外层 runner 仅在两端测试成功且证据完整时提升为 `passed`，任何 Playwright/收据失败均覆写为 `failed`，防止 worker 重启掩盖失败。新增三项 Jest runner 守卫先 RED 后 GREEN，旧 CJS runner 覆盖合并后 API 定向 8/8、全量 302 suites / 5,921 passed、两端 typecheck/lint、`check:ui` 与 release manifests 通过；最终隔离 PG16 + Chromium/WebKit 证据待新 SHA 双轴复审后重跑。
- [~] Issue #15 RC-06 UUID 详情路由跟进：真实浏览器验证发现详情接口接受合同 UUID，但展示 `id` 保持合同编号，导致最终件确认把合法 UUID 误判为“合同已切换”。敏感动作现同时冻结原始路由别名与服务端不可变 `contractVersionId`，fresh capability 也按版本身份复核；因此 UUID 或合同编号作为初始路由均可提交，而导航到其他合同、同路由详情版本漂移或 fresh capability 撤销均不发 POST。组件回归先 RED 后 GREEN（合法 UUID 实际请求确认及三类 0‑POST 拒绝）4/4，合同相关 31/31、Web 全量 179 files / 1,939 tests、API/Web typecheck/lint 通过；新 SHA 双轴复审后重新运行完整隔离 PG16 + Chromium/WebKit。
- [~] Issue #15 PR #38 CI 收敛（新候选待动态门）：重生 page-action、route-usage 与 capability matrix 后发现 UUID 最终件预检的抽取 helper 使页面动作检查器无法静态证明“服务器 capability 支配确认写入”；将相同的 `contractVersionId` 服务器身份比对内联回受控确认函数，UUID/合同编号 alias 语义不变，页面动作、route-usage、capability matrix 均恢复 `ready`/0 blocker。PG 路由动态用例同步当前安全规则：`super_admin` 删除他人纯净草稿为 403，并断言其后合同版本、删除审计/收据均无副作用；未恢复任何 `super_admin` 业务旁路。静态验证：UUID 回归 4/4、API 全量 302 suites / 5,921 passed（21/66 既有 skip）、API/Web typecheck/lint、`check:ui` 通过。提交后必须以精确新 SHA 重跑 `verify:database-dynamic-remaining:local`，旧 PG/浏览器证据不继承。
- [x] Issue #16 实现完成：已将确认删除改为受控即时物理删除命令，仅接受当前、从未提交且无审批/正式/下游事实的原始草稿；工作台客户端按动作直接调用该 DELETE 端点并接受 `deleting`/`deleted` 可重试响应。若对象清理返回 `retryable`，工作台保留原确认界面、冻结草稿写入并要求再次确认，同一受控 DELETE 重试成功后才清空并跳转结束列表；浏览器回归覆盖规范工作台路径、`expectedRevision`、零草稿保存调用及失败后重试。能力矩阵沿导出的业务动作追踪到传输 wrapper，已将该生产 DELETE 正确登记为 `matched`，不再误列 legacy/no-consumer。删除精确聚合与独占文件对象全版本，shared 文件仅解除绑定并保留，失败保留最小可重试回执，正式编号 tombstone 在审批提交、工作台自动/手工编号、外发合同文件自动编号、历史接管新建及修改等全部写入点均永久拒绝复用，重复请求幂等。全局 super_admin 不获该业务删除权限；该权限裁决已同步 Issue #16：仅当前草稿经办人或当前全局合同部主管可触发或重试。物理对象复用以同 bucket/objectKey 视为 shared；回执从成功清理时起按上海自然月月末截断的三个自然月保留，并由单独限量收据清理单元在未来部署后按日物理删除 `completed` 到期项（部署仅安装，未启用/未执行）。实现与验证均不触及生产数据库、业务数据、COS 对象或生命周期配置。
- [x] Issue #16 本地 PG16 remaining 动态收据：实现候选 `ce049c66aa4caaccff4883bdd8062dbe4450e2df` 在本机 PostgreSQL 16 通过 121 条迁移（M121）、remaining runner 的 9/9 组及其实际执行的 32 项测试，总耗时 81,139 ms，容器已自动清理。完整动态门清单覆盖 55 项测试，须以其单独的全门收据为准；此 remaining 收据仅绑定该实现提交，不代表生产验证，也不表示记录本收据的后续文档提交已通过此门禁；未连接生产数据库、业务数据或 COS。
- [x] Issue #16 本地完整动态/浏览器收据：治理候选 `f29ebfa2e559b7947790c63b3e2560e04216e3d6` 的一次性 PostgreSQL 16 全门通过，121 条迁移至 M121、55 项覆盖测试/29 文件、9/9 组、`remaining=0`，总耗时 323,161 ms、退出码 0，所有本票临时容器已清理。相同 SHA 的本地 preview 定向工作台删除回归：Chromium 1/1、WebKit 1/1 均通过；该浏览器用例使用现有 `page.route` mock harness，不替代独立真实 API 长链证据。上述均非生产验证，未连接生产数据库、业务数据或 COS；记录本收据的后续文档提交不继承此门禁。
- [x] Issue #16 CI 运维安全自检修复：#16 的部署脚本新增“纯净草稿删除回执”清理 service/timer 安装后，成功部署夹具仍只提供旧 draft-retention 源单元，导致 API-only 成功路径在安装阶段无提示退出。夹具现提供两份对应源单元，并在目标 systemd 目录逐项断言 service/timer 已安装；`bash scripts/ops/go-live-safety-self-test.sh`、business-errors、release manifests 通过。未改删除业务、迁移、生产部署或计时器启用；必须以新 SHA 重跑远端 CI。
- [x] Issue #17 结束申请保留与只读历史已由 PR #40 squash merge/关闭：基于 #16 合并主线保留当前任职/归档治理与纯净草稿删除边界；结束申请仅在精确 `versionId`、完整项目阅览权限下打开只读历史，所有办理动作/复制广告关闭，最终驳回与已提交放弃经共同草稿写入边界拒绝。结束保留预览按范围分页，保留/解除由合同部主管审计执行。预览以参数化数据库内联接按授权范围过滤版本，不扫描项目表；全局合同部主管查询全部合同，项目范围岗位仅由自身 `UserPosition`/`ProjectMember` 派生项目 ID，无范围返回空页；查询和写入均拒绝停用项目。动态门清单统一为 M122、57 项/31 文件（remaining 33 项），route/web/capability 清单为 452 routes、0 blocker；PR #40 的 CI 收据为 green。未改生产数据库、业务数据或 COS。
- [~] Issue #18 到期结束申请整项物理清理候选：代码候选 `dc3f4ac53212d2495bc61199c0aec00f65590a88` 新增 M123 `20260808110000_contract_ended_application_purge`、最小技术回执和**内部可调用**受控后台 batch service（刻意不新增 controller/CLI/scheduler，生产 batch 未授权）。仅 `system` 新建原始首版的 `abandoned`/`approval_rejected` 申请，经过三个月上海自然月保留、active hold、已释放 hold 在保留结束后 30 天缓冲、有效/历史/正式/下游事实和审计范围的二次 fail-closed 复验后才进入清理。反向复制引用、未知嵌套审计均阻断清理；允许的 `contract`/`contract_version` 及账单、谈判、离线修订、生成文档、差异、税务事实审计与聚合一并删除。业务聚合先在 Serializable 事务中解除，独占 FileObject 仅暂留 receipt 技术指针；随后在文件绑定锁下枚举精确键、删除全部对象版本/删除标记、删除技术 FileObject 并完成回执。空文件列不再作为绑定 ID 传入查询；真实 COS adapter 在外部删除前逐行核验 `FileObject.bucket === COS_BUCKET`，失配时 0 外部调用、仅留 `retryable` receipt/技术行；COS 或最终 completed 回执持久化失败都可再次收敛，shared 文件只解绑。PG RED→GREEN 专项 5/5 覆盖 COS 失败、COS 成功后回执落库失败的二次收敛、historical bucket 失配零 COS 调用、active/released hold、有效/历史、复制引用、允许/非允许嵌套审计及策略缺失；API 全量 Jest、typecheck/lint、文件绑定回归、动态清单守卫（M123、62 项）与 release manifests 均通过。未执行生产 batch、生产数据库/数据或 COS 操作，也未改 bucket lifecycle。
- [x] Issue #18 本机完整动态收据：代码候选 `dc3f4ac53212d2495bc61199c0aec00f65590a88` 在 disposable PostgreSQL 16 通过 123 条迁移至 M123、9/9 组、62 项覆盖测试/32 文件（remaining=0）、退出码 0，耗时 325,869 ms；合同草稿聚合组的 5 suites / 21 tests 含到期结束申请 purge 5 条 PG 用例。全部临时容器和目录已由 runner 清理。此后仅追加本登记的文档提交不继承该动态收据；未连接生产数据库、业务数据或 COS。
- [~] Issue #19 存量合同清理只读预检候选：新增仅报告的 `inspect:legacy-contract-cleanup-preflight`，默认无 apply 实现并拒绝 `--apply`；在 PostgreSQL 只读事务中以迁移基线、保留策略、合同版本/审批/后续业务事实及精确文件绑定生成候选、阻断和人工复核清单。仅 `system` 来源、原始首版、从未提交且无审批动作的已放弃草稿可获得 legacy 删除授权；`approval_rejected` 从受控启用日重新计算三个月保留期，其他状态失败关闭。报告含候选 `contractVersionId`、代码 SHA、非端点数据库指纹、批次和对象清单哈希/计数，不含业务名称、对象键、SQL、连接串或凭证；COS 仅精确列举版本，未增加删除入口。已登记本机 PostgreSQL 16 动态测试（33 文件/63 项）；待候选提交后的全动态门和双轴复审。未连接生产数据库、业务数据或 COS，未执行 apply、删除或清理。
- [x] Issue #19 固定点 P1 修复：原实现将 500 条作为报告上限，超过时返回空 records 与 `REPORT_TRUNCATED`，不能完成存量审计；现保持单个 `RepeatableRead` + `READ ONLY` 事务，将 500 改为内部 keyset 页大小，按稳定 `contractVersionId` 完整遍历并核验数据库总数、输出全部脱敏记录与准确累计摘要。无 HTTP 游标、apply 或生产执行入口。501 条静态事务回归先 RED 后 GREEN，断言两页均被读取、一个只读事务内完整列出且没有 `REPORT_TRUNCATED`；修复候选已按精确 SHA 重跑动态门，仍待远端 CI 后合并。
- [x] Issue #19 本机 PG16 动态收据：代码候选 `cc0384ebc8085a9cfadf7a7cd438f974aeec5e2e` 在 disposable PostgreSQL 16 通过 M123、remaining runner 9/9 组及 39 项测试、退出码 0、耗时 81,029 ms；合同草稿聚合组 6 suites / 22 tests 含预检的只读 UPDATE 拒绝、COS 仅列举（删除/收敛调用为 0）、参数化策略查询和内部 keyset 分页回归。动态清单为 33 文件/63 项、remaining=0；临时容器已清理。未连接生产数据库、业务数据或 COS，未执行 apply、删除或清理；记录本收据的后续文档提交不继承该动态门。
- [x] Issue #7 存量 legacy 清理已按受控例外收口：代码候选 `929321e407a076ba7762b072881822ec4e2d1717` 的双轴复核 P0/P1/P2 均为 0，disposable PostgreSQL 16 remaining 动态门以 124 migrations/M124、9/9 组、42 tests、退出码 0 通过；普通入口仍对非白名单审计 fail-closed。生产新鲜预检 `/private/tmp/issue-7-production-preflight-929321e4.json`（文件 SHA-256 `47d15b46f808827f6b1bdfa1dda0231d746359f56d326ffb733ff7b88873ad0`，报告 SHA `3db05019596e2ea1ec3a3ee6a57b1c357bf33de7509064da0c0c58f11bb4fb4e`）确认目标 `17f96740-3317-4db6-895e-3fd09d51c9ad` 仅因精确 `contract_version` + `contract.authorization.update` 两条审计事实进入 `manual_review`；普通 candidate 为 0，人工复核 `722bb87e-700d-40d2-95b5-c82604cfb92c` 与正式/下游阻断 `bb6dfeb0-59a9-43d8-8c4b-835ada8a5f9c` 未进入执行。获授权的独立 CLI 批次 `legacy-audit-exception-929321e4-20260809-2245` 完成删除目标聚合及恰好两条指定审计（执行回执 `/private/tmp/issue-7-production-audit-exception-execution-929321e4.json`，文件 SHA-256 `671e5ea138c0c3d2fb9f25c53bcddc8e1c66d96462067437950e78e1e40bbee1`）。新鲜后预检 `/private/tmp/issue-7-production-post-preflight-929321e4.json`（文件 SHA-256 `97b553a52f14448ffed4de73f72340efea53a5f20ac65b1176a867173f527706`，报告 SHA `859cf8e0ede988b8bfec3261aafd6450a261b6d34b72461966c067a5a9657133`）只剩原人工复核与阻断各 1 条；只读 SQL `/private/tmp/issue-7-production-post-sql-929321e4.json`（SHA-256 `2bb9f0fbaa325e037a9cc7c17a19619802af8d0d6e15743ad69ed65762d1e358`）确认目标 Contract/ContractVersion、全部目标审计及两条指定审计均为 0，人工/阻断版本仍为 draft；生产 API active、health=ok、124 migrations，临时 staging 已清理。未部署该候选、未改迁移、未触碰其他业务/COS 对象。其后本地合并最新 `origin/main` 生成 `7120afa8e19cb079feee951638a1988cb95e6d1e`，移除旧 CI/deploy workflow；合并树定向门禁与本地 PG16 remaining 仍通过；登记提交 `6af78488ffe301bc281967b43a50ae0904d5049c` 的本地 PG16 remaining 也已再次通过（124/M124、9/9、42 tests，收据 `/private/tmp/issue-7-dynamic-6af78488.log`），后续仅为本登记文档提交，不改变业务代码。
- [~] Issue #20 发布候选本机发现性门：基线 `5dcfde8fbefeb7cd4f8edca38e01cbb4456b006e` 的 release manifests 已匹配（452 routes、440 wrappers/462 bindings），隔离合同结算治理 21/21 场景以及真实 Chromium `1366x768`、WebKit `390x844` 岗位链均通过；两种浏览器均覆盖 400/403/409/503，且浏览器错误、失败请求、测试失败均为 0。该基线仅用于发现环境与门禁缺口。
- [~] Issue #20 最终本机候选收据（本登记前固定点）：`fc8d13324ef9a0e7b4512a23b56a0faf4cf086dc` 已通过完整本机门——Jest 527 suites / 8,148 tests（8,075 passed、73 pending、0 failed；API 331 suites / 6,043 tests）、typecheck、lint、Prisma validate、API/Web build、UI governance、business-errors、go-live safety 与 release manifests；disposable PostgreSQL 16 动态门通过 123 migrations（M123）、33 files / 63 tests、9/9 groups、remaining 0、315,907 ms；隔离治理 UAT 21/21；Chromium 为 200:72、201:15、400:1、403:7、409:1、503:1，WebKit 为 200:64、201:13、400:1、403:7、409:1、503:1，二者浏览器错误、失败请求、测试失败均为 0。该 SHA 的收据不自动继承给本次补充文档形成的新候选；新候选须重新绑定本机门，详见 `docs/progress/2026-08-09-issue-20-release-candidate.md`。
- [x] Issue #20 发布后时区修复已合并：M123 将上海本地 `TIMESTAMP(3)` 策略时间误当 UTC；前向 M124 `20260809150000_contract_retention_policy_timestamptz` 以 `Asia/Shanghai` 转为 `TIMESTAMPTZ(3)`，Schema native type、固定上海墙钟→UTC epoch 回归、`lock_timeout=5s` 与 124/M124 动态清单均已同步。PR #44 先合并为 `f5f470db5af6649e41881ae2296981d30e092c5e`；正式 verify 暴露 GitHub runner `pnpm test` Node heap OOM（exit 134），随后 PR #45 仅将部署工作流 Test 步骤堆上限提高到 `4096 MB`，合并运行时基线 `a6a9b08a94c0ac9747b9741bb5563e8522ce435c`。PR #45 静态/动态/发布门全部通过。
- [x] Issue #20 生产门已完成：Deploy Production run `31301434286` 以精确 SHA `a6a9b08a94c0ac9747b9741bb5563e8522ce435c` verify/deploy success；生产只读核验确认代码 SHA 同值、API/nginx/PostgreSQL active、`/health` 为 `ok`，`_prisma_migrations` 为 124 条，M124 checksum 为 `d68568c402656eb67c4175696f527aadce95f8dc06ef5bc8d51c7bfdd37d9b21`。预检收据 `/private/tmp/issue20-production-preflight-a6a9b08.json`（SHA256 `9f5e91d0cc9447d49d8a685aa37ddff2e740a9467aa203b24d92057f5442ca93`）为 `read_only`/`executionAllowed=false`/`apply=disabled`，策略起算 `2026-08-09T05:50:59.621Z`（上海 `13:50:59.621`）已按 M124 正确解释；4 条存量记录中 2 条 legacy candidate、1 条 manual review、1 条 blocking，阻断为 `ACTIVE_DRAFT_IS_NEVER_AUTOMATICALLY_SELECTED` 与 `FORMAL_OR_DOWNSTREAM_FACT`。未执行 apply、业务数据/COS 清理或恢复操作；后续处置需另行人工复核授权。
- [x] Issue #118 本地候选收口：基线 `origin/main=71011c4a0458e7739fc23f2affa7d61b7c492d92`；实现候选 SHA `39c38947c493b80177831a46db269fc36c657b36`。审计读模型、历史合同接管 Excel、结算导入/结果 Excel、结算 Excel/PDF 及既有结算页面错误入口已清除用户可见内部枚举、ID、JSON、修订号、分值和英文状态；中文业务语言登记表、自动扫描、制品证据和 CI 硬门已接入，技术枚举输入 fail-closed。验证：shared-domain 17 files/171 tests、Web 182 files/1953 tests、API 334 suites/6174 tests passed/100 skipped、API/Web typecheck/lint、业务语言/UI/英文错误门、CI 编排 51/51、`git diff --check` 均通过。未推送、未合并、未部署、未修改生产数据。
- [ ] Issue #9–#20 在各自 blocking edge 解除前不得并行写共享 Schema 或权限模型；Issue #8 关闭后才重新启用直接下游票据。
- [ ] 未经单独授权，不修改生产业务数据、生产数据库记录或 COS 对象/生命周期规则。

### 上线后业务观察

- [ ] 合同部/法务对最新合同母版 DOCX 逐页人工签认，不以 PDF/PNG 自动检查代替。
- [ ] 按“发生后续结算/付款前先接管并确认余额”的原则继续完成历史合同接管，不把存量事实视为已全部初始化。
- [ ] 使用真实岗位验证首批合同、结算、付款、实付、凭证和归档闭环，并保留业务与财务签认。
- [ ] 继续抽查真实中文附件的移动端上传、受控下载、二次确认和审计记录。
- [ ] 在具备第二台生产等价主机后补做跨主机恢复与接管演练；完成前不得宣称完整灾备已验证。

## Go-Live 门禁状态

| 门禁 | 状态 | 当前证据口径 |
| --- | --- | --- |
| `POST /files` 权限旁路 | [x] | 岗位白名单、Guard -> ValidationPipe -> Service 顺序和拒绝路径已验证 |
| 结算审批过期状态 | [x] | 稳定 HTTP 409 / `SETTLEMENT_APPROVAL_REVIEW_CONFLICT`，并验证零写入 |
| 99 个治理 blocker | [x] | Web/page/route/capability manifests 均为 ready，无 blocker |
| 3 个重复 mutation route | [x] | 已收敛为领域唯一写入口，不以通配豁免隐藏 |
| CI 动态数据库门 | [x] | PostgreSQL 16：最终精确 HEAD 已通过 119 migrations、55/55 tests、29/29 files、9 groups；remaining-dynamic 31/31 tests |
| 四类 `--require-ready` | [x] | Web API、页面动作、route usage、capability matrix 全部硬门通过 |
| 最终 SHA 与差异证据 | [x] | CI/部署输入绑定候选 SHA；候选工作树和 `git diff --check` 已核验 |
| P0 与五包 Task 11 | [x] | 已纳入最终候选并通过候选级回归；详细过程见历史快照和发布收据 |
| RC-06 隔离业务长链 | [x] | 合同 -> 结算 -> 付款闭环与 20 个治理场景，`productionData=false` |
| RC-06 真实岗位浏览器 | [x] | Chromium 1366x768、WebKit 390x844；五类岗位及 400/403/409/503 |
| 503、双击幂等、移动文件链 | [x] | 写冻结 503、并发上传幂等、上传下载内容回读均有证据 |
| 备份隔离恢复与监控 | [x] | 自然异机备份、checksum、隔离恢复、健康/readiness 和监控证据通过 |
| RC-09 / 阶段 F | [~] | 同机隔离部署 -> 回滚 -> 再部署通过；跨主机与整机故障未演练 |

## POL-08 主线候选收据（2026-08-17）

- [x] Issue #101 候选从 fresh fetch 后的 `origin/main@1b5df670282c200c32a4a12dd99599f38887e60e` 重组；按既定顺序完整接入 `9f8c78fe` 至 `9cd2641e` 的 12 提交链。仅两份生成型全站能力矩阵与主线发生内容冲突，均保留主线后按当前代码重新生成；最终相对基线仍与原候选严格一致为 71 个文件，未接入其他 Issue 行为。
- [x] 本地验证：POL-08 定向 API 5 suites / 259 tests、定向 Web 3 files / 24 tests；全量 API 341 passed suites / 6241 passed tests / 29 skipped suites / 103 skipped tests；workspace typecheck、API/Web build、lint（0 errors；30 条既有 takeover 页面 warning）、`check:ui`、business-language、`check:business-errors`、全部 release manifests、动态门静态 runner 13/13 与 `git diff --check` 均通过。
- [x] PostgreSQL 16 候选门已绑定 `1bd18cac32981ba89f8d2f0834e7ed070b017022` 通过：136 migrations，terminal migration `20260816120000_pol08_contract_lineage_operating_sources`，terminal checksum `1eff5e501fd82a23e5912b02e07969fbd6f9d69750b960e1c84c073038a308b0`，94 tests / 38 files，remaining 0/0；镜像 `postgres:16@sha256:e17e86066e5ef83e0952a9347f5c792b7ece00972e2aa787a6986f471b3dd3d5`。本收据提交后仍须对最终精确 HEAD 重跑同一门禁，旧收据不直接继承。
- [~] Issue #101 独立复审修复已严格限定于 7 项确认 blocker：内部施工企业结算不形成项目成本/应付且冻结正确 lineage；付款申请强绑定外部合同与结算账本；结算确认按已生效事实加当前待确认事实校验，不允许其他 pending 增额掩护减额；结算冲销保持原事实 impact kind；待核对差额的重分类可保留来源关联并物化为合法正式事实；历史拨款冻结当时结算净额；项目经营页改用可读业务选项而非手填内部 UUID，服务端校验继续作为权威。TDD 先得到 API 6 项、Web 1 项 RED，修复后定向 API 5 suites / 184 tests、Web 2 files / 152 tests；全量 API 341 suites / 6247 tests（29 suites / 103 tests 按既有配置跳过）；workspace typecheck/lint（0 errors；30 条既有 takeover 页面 warning）、API/Web build、`check:ui`、business-language、`check:business-errors`、聚合 release manifests（479 routes、465 wrappers / 487 bindings、250 actions、0 blockers）、动态门静态 runner 13/13 与 `git diff --check` 均通过。未改 Schema/Prisma migration，未访问或修改生产数据库/COS；最终精确 SHA PostgreSQL 16 门、Standards/Spec 双轴独立复审与 GitHub 收口仍待完成。
- [~] Issue #101 第二次独立复审修复严格只处理候选 `83946975e1274e24cc231f72f9e128b7acb2bd73` 的 3 个 P1：结算容量仅以已确认净额为权威并在当前确认场景单独纳入当前事实，其他 pending 增额不能掩护减额或拨款；历史拨款首次物化时冻结事务内真实可见的已确认结算净额，后续 replay 复用既有经营事实快照，不再用 `confirmedAt` 猜测同毫秒先后；业主付款创建必须与上游结算审批单位一致，更正继承原交易对方且不得换主体。TDD 先得到 API 6 项 RED，最小修复后定向 API 3 suites / 54 tests，候选原定向 API 5 suites / 188 tests、Web 2 files / 152 tests；全量 API 341 suites / 6251 tests（29 suites / 103 tests 按既有配置跳过），全量 Web 190 files / 1986 tests；workspace typecheck/lint（0 errors；30 条既有 takeover 页面 warning）、API/Web build、`check:ui`、business-language、`check:business-errors`、聚合 release manifests（479 routes、465 wrappers / 487 bindings、250 actions、0 blockers）、CI 编排 51/51、动态门静态 runner 13/13 与 `git diff --check` 均通过。未改 Schema/Prisma migration，未访问或修改生产数据库/COS；本次最终精确 SHA PostgreSQL 16 门、Standards/Spec 双轴独立复审与 GitHub 收口仍待完成。
- [ ] 待完成 Standards/Spec 双轴独立复审、push、PR、CI、merge、远端 main SHA 与 Issue 关闭核验；未部署、未执行生产迁移、未写入生产数据库/COS、未执行真实归零或正式开放。

- [~] Issue #101 / POL-08 第三轮 Spec 复审的 3 个 P1 已从完整候选 `0d7e712ea8e3f2d8ec6411b3d328083ca3adf62d` 定点修复：生效日后例外付款复用既有待复核事实与确认审计链且不伪造审批；页面仅展示中文合同/付款业务引用并隐藏传输 UUID；付款申请在同合同同期间存在多笔有效外部结算时 fail-closed。TDD 取得 API 3 项与 Web 1 项行为 RED；修复后 API 服务 24/24、Web 结构 4/4，全量 shared-domain 18 files / 176 tests、Web 190 files / 1987 tests、API 341 suites / 6253 tests（29 suites / 103 tests 按既有配置跳过），workspace typecheck/lint（0 errors；30 条既有 takeover 页面 warning）、API/Web build、`check:ui`、business-language、business-errors、聚合 release manifests（479 routes、465 wrappers / 487 bindings、250 actions、0 blockers）、CI 编排 51/51、动态门静态 runner 13/13 与 `git diff --check` 均通过。最终精确 SHA 的全量复跑、PostgreSQL 16 canonical 动态门、Standards/Spec 双轴独立复审与 GitHub 收口待执行。未改 Schema，未执行部署、生产迁移、生产数据库/COS 写入、真实归零或正式开放。

- [~] Issue #101 / POL-08 第三轮 Standards 复审固定点仅修复未来例外付款：生效日后未关联付款申请的事后补录在服务端拒绝晚于当前时间的 `paidAt`，避免尚未发生的付款进入 `pending_confirm`；已发生例外付款和关联已审批申请的普通施工企业付款行为保持不变。最窄 Jest 先证明未来付款会被错误接受为待复核事实，再转为 GREEN；完整 `project-affiliate-business.service.spec.ts` 25/25 通过。未改 Schema、迁移、状态、权限、业务 ownership 或 resolver；最终精确 SHA 的全量门、PostgreSQL 16、Standards/Spec 双轴独立复审及 GitHub 收口待执行。

## 生产与运维边界

- [x] Issue #146 / POL-19P3 已由 PR #166 完成并合并：最终 head `e21958251482122c62501fcf7a6cd60ac8976341`，merge/main SHA `784b4e49f2b0374e7a7af20b35086dcd92cc3e20`；PR CI `32129951815` 与精确 main CI `32130537748` 全绿。合并链覆盖显式基础资料统一录入场景、actor-aware target resolver、`authenticated_self`、签名短期 create-target、领域授权复用入口及全局 Web adapter；#163/#164/#165/#167/#168/#169/#170/#171/#172 均已 CLOSED（completed；#166 为 PR，不存在同号 Issue）。未部署、未生产验证，未执行生产数据库/COS/权限/业务数据写入、真实归零或正式开放。
- [x] Issue #163 / POL-19P4 已由 PR #166 完成并 CLOSED（completed）：四项统一录入复审 finding 均已收敛——全局 freeze 无领域事务持久化时 fail-closed；模板目标 resolver 统一校验 `draft` 可编辑状态；定义/Excel/create-target/validate/freeze 复用目标与授权边界；全局/项目 scope、空白 `projectId` 与 Web adapter 组装 fail-closed。未部署、未生产验证。
- [x] Issue #164 / POL-19P5 已由 PR #166 完成并 CLOSED（completed）：`BusinessEntrySceneAuthorizationService` 已收紧为 Nest 必需依赖，create-target、validate、freeze 统一调用领域授权，缺失 provider 稳定返回中文错误且不签发目标令牌/保存快照；`BusinessEntryDefinitionModule` wiring 已核验。未部署、未生产验证。
- [x] Issue #165 / POL-19P6 已由 PR #166 完成并 CLOSED（completed）：`validateDraftWithRoles`、批量 `validateDraftBatch`、事务 `freezeSubmissionSnapshotInTransaction` 的缺失授权 provider 测试，以及生产 `BusinessEntryDefinitionModule` wiring 测试均已补齐；TDD 变异证据与 2 suites / 22 tests 通过。未部署、未生产验证。
- [x] Issue #172 / POL-19P12 已由 PR #166 完成并 CLOSED（completed）：在精确 head `a039397ef28b88e0c6e11fe6269c94ee8579bb96` 上完成 #167–#171 清单治理，业务错误 62 处、合同能力矩阵/Nest route 480、Web API 465/487、route-usage 480/0 unclassified，并由官方 whole-site capability matrix generator 刷新为 480 routes/0 blockers；PR CI `32129951815` 与 main CI `32130537748` 全绿。未部署、未生产验证、未执行生产写入、真实归零或正式开放。

- 生产业务写入、账号/权限变更、数据库修复、COS 对象删除或生命周期变更必须获得单独明确授权。
- 日常备份巡检默认只读，只验证自然 Cron 产物、checksum、`pg_restore --list`、异机回执、日志/进程和公共健康状态。
- 发生发布异常时先停写、确认当前运行 SHA 与最新可恢复备份，再按 runbook 回滚；不得在证据不完整时继续迁移或写入。
- 精确候选 SHA 发生任何代码变化后，旧的 CI、数据库、浏览器和生产证据不再自动适用，须重新绑定并运行受影响门禁。
- 当前生产为单机部署；自然异机备份是数据恢复控制，不等于应用跨主机高可用。

## 模块状态

- [x] 核心业务闭环：合同草稿、审批、用章、归档、生效、结算、付款申请、实付、凭证、财务记录、PDF 与审计已具备。
- [~] 审批引擎：实例冻结、会签、或签、撤回、退回、转审、委托和催办覆盖核心链路；条件节点仍缺显式合同类型字段，后续变更继续以服务端状态机为准。
- [x] 认证与授权：手机号密码、强制改密、项目岗位权限、敏感动作校验和审计已覆盖当前生产范围。
- [~] 文件/PDF/审计：私有存储、短时效下载、二次确认和审计已覆盖主要链路；真实业务附件继续观察。
- [~] Web Admin：桌面与手机共用 Vue 3 + TDesign 响应式 Web；真实尺寸和业务长链持续验收。
- [~] 小程序退出：目标架构已决定，但 `apps/miniprogram`、微信登录入口和 `wxOpenid` 清理尚未按两阶段计划实施。

## 不在当前 P0 范围

- OCR/AI 批量识别、开票、考勤、人事、安全、完整物料领用和大型经营驾驶舱。
- 已签历史合同重走合同审批；历史合同继续按接管、余额确认和后续业务触发原则治理。
- 未经重新定界，不新增第二套 UI 库、低代码运行时、通用工作流引擎或全站重写。

## 历史与证据索引

- 本次压缩前的完整进度快照：[`docs/progress/full-history-snapshot-through-2026-08-05.md`](docs/progress/full-history-snapshot-through-2026-08-05.md)
- 2026-07-07 以前的早期历史：[`docs/progress/full-history-through-2026-07-07.md`](docs/progress/full-history-through-2026-07-07.md)
- 当前发布收据：[`docs/progress/2026-08-05-go-live-conditional-go.md`](docs/progress/2026-08-05-go-live-conditional-go.md)
- 文档导航：[`docs/README.md`](docs/README.md)
- 文档有效性索引：[`obsidian-current/建工智管_文档有效性索引.md`](obsidian-current/建工智管_文档有效性索引.md)

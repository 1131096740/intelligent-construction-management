# 合同工作台五包完成状态审计

日期：2026-07-30

## 结论

当前可以证明：

- 五包核心运行代码已经形成同一候选；
- Release A、生产迁移、备份隔离恢复和 Release B 已完成；
- 精确 Release B 生产版本为
  `5234fd37bc5c320922f73323af77b20317fcf5f7`；
- 现有 11 个账号、2 个项目已按负责人决定正式使用。

当前不能证明“原五包全部 Task 和全部发布门禁完成”。主要缺口集中在实施包 5
的 C1、全站能力矩阵、旧接口退出、小程序退役和真实生产业务长链验收。

因此根目标继续保持 active，不把 Release B 上线改写为五包全阶段完成。

## Git 与基线

| 项目 | 证据 |
| --- | --- |
| 实施基线 | `a0bbfacb5008abbcb255a96c79cb0bd05c76db56` |
| 已部署 Release B | `5234fd37bc5c320922f73323af77b20317fcf5f7` |
| 当前隔离分支 | `codex/whole-site-five-packages` |
| 审计起点本地 HEAD | `6cc4800ef69a31f04501958151c89d934c7dc0b8` |
| 基线追溯 | `a0bbfacb…` 是 `5234fd37…` 的祖先 |
| 基线到生产提交数 | 98 个线性提交，无 merge |
| 审计开始时本地相对候选远端 | ahead 3 |
| 生产之后的本地差异 | 仅 `PROGRESS.md` 和 3 份 `docs/progress` 收据 |

未推送的 docs-only 提交：

1. `210e093afe0cbe91c6c9c4828ba954ac065c4c30`：Release B 生产收据；
2. `bc1ef95850843e3ef941488e20f7f28cdb1ee39e`：全员正式上线收据；
3. `6cc4800ef69a31f04501958151c89d934c7dc0b8`：首次 temporary retention 收据。

本审计提交后还会新增一个 docs-only SHA；这些 SHA 均没有新的 push、main
fast-forward 或生产部署授权。

实际提交顺序为：

```text
实施包 1
  -> 实施包 2 核心
  -> 实施包 3
  -> 实施包 4
  -> 实施包 2 真实 Excel 最终门
  -> 实施包 5
```

真实 Excel 门虽然在 Release A 和任何生产发布之前补齐，但没有严格发生在实施包 3
开始之前；这是原“1→2→3→4→5 严格顺序”的一项历史偏差，不能隐藏。

## 五包矩阵

| 实施包 | 代码/迁移/自动化 | 生产证据 | 当前判定 |
| --- | --- | --- | --- |
| 1 核心契约、草稿聚合与模板治理 | Task 1–14 已进入候选 | Release B 聚合读写、租约、相同内容保存守恒通过 | 完成 |
| 2 清单金额与统一资金规则 | Task 1–13 已进入候选；实际报价回归和 75 万元合成精度回归已锁定 | 生产 transition 写入 11 行权威不含税单价并二次 0 写 | 实现完成；原索引的“约 5 元差异真实源文件”证据及指定资金长链生产验收不足 |
| 3 响应式工作台、签名与页面入口 | Task 1–17 已进入候选，Chromium/WebKit/移动门通过 | Release B 只做 canary API/权限和最小聚合烟测 | 实现完成；真实岗位浏览器长链不足 |
| 4 历史合同与挂靠持续接管 | Task 1–16 的 Schema、服务、页面和测试齐全 | 生产 `ContractTakeover=0`，尚无真实挂靠/历史结算付款链 | 本地实现完成；生产业务验收缺失 |
| 5 迁移、能力收口与旧运行时清理 | Task 1–6 与最小 Release B 完成 | Release A/B、备份恢复、迁移、健康通过 | Task 7 后半及 Task 8–15 多项未完成 |

## 已证明的总门禁

- 实际报价 XLSX：
  `1.一标段-绿化专业分包-报价单2026.06.17(2)(1)..xlsx`；
- SHA-256：
  `b6840ba245d77ae6e017b02fb42523f539de0f14b4400a4e13cc82057550293d`；
- 有效表 12 行、9% 税率、逐行公式/格式/金额回归；
- 独立的 75 万元合成精度回归输出不含税 `688,073.39` 元；
- shared-domain 149/149；
- API 4,751/4,751；
- Web 1,248/1,248；
- API/Web typecheck、Web E2E typecheck；
- API/Web lint；
- Web `check:ui`；
- API/Web build；
- Prisma validate/generate；
- 业务错误扫描、合同工作台能力矩阵、`git diff --check`；
- Chromium P0；
- Chromium + WebKit 清单桌面、960、640、375 响应式；
- 全局搜索、Canvas/手机签名和历史接管浏览器门；
- PostgreSQL 16 空库 109/109 迁移；
- transition 首次写入和二次 0 写幂等/守恒；
- Release A 自然备份独立下载、隔离恢复、91→109、RTO 9 秒；
- Release B 完整部署、三 canary、四账号 410/503/403/权限边界；
- 相同内容聚合保存 revision 13→13、业务快照守恒；
- 生产备份 checksum、`pg_restore --list`、异机收据、健康和日志。

`5234fd37…` 与最终完整验证树的运行代码、Prisma Schema 和迁移无差异，后续差异
仅为生产结果文档。

上述两个 Excel 证据不能合并表述成原索引要求的第三项证据。用户已经明确：

- 实际绿化报价文件不是此前提到的 75 万元工程量清单；
- 实际报价 12 行按当前规则没有产生约 5 元舍入差异；
- 75 万元用例是仓库中的合成精度边界测试，不是已保全的真实源文件。

因此 `implementation-index` 中“合同部提供造成约 5 元差异的原始 Excel，旧实现
复现、新实现逐行对账”的字面硬门仍为**证据不足**。要关闭该项，需要取得该真实
源文件并按只读保全流程回归，或由用户明确变更锁定需求、接受“实际报价回归 +
75 万元合成精度回归”作为替代门禁；本审计不代替用户做该选择。

## 实施包 5 当前逐 Task 状态

| Task | 当前状态 | 未完成事项 |
| --- | --- | --- |
| 1 合同能力矩阵 | 完成合同专用矩阵 | 尚无全站矩阵和批准观察窗口生产命中证据 |
| 2 草稿迁移只读预检 | 完成 | — |
| 3 受控 transition | 完成并在生产执行 | — |
| 4 存储保留策略 | 实现完成、生产部分启用 | 仅清理 8 个未绑定文件；余 9 条导入预览，timer 未启用 |
| 5 Release A | 完成 | — |
| 6 ready 草稿迁移/旧写冻结 | 完成 transition 与维护门 | — |
| 7 Release B | 完成最小获批范围 | 多章节真实岗位长链、3–5 份合同和历史接管后结算付款未发生 |
| 8 Release C1 | 未开始 | 旧 PATCH、checkpoint create/restore、旧 wrapper 仍在 |
| 9 Release C2 | 独立关闭 | checkpoint model 保留，清理迁移按设计不存在 |
| 10 最终生产验收 | 部分 | 无真实 Excel 生产导入及历史接管/ledger/更正全链 |
| 11 全项目能力矩阵 | 未实现 | 三份 machine-readable manifest 与 `whole-site` 报告不存在 |
| 12 旧接口退出集合 | 未完成 | 多组旧 route/wrapper/lifecycle 仍在 |
| 13 模板/场景/合同类型/岗位迁移 | 模型存在 | 缺生产报告 SHA、fingerprint、逐记录 revision 绑定 apply 证据 |
| 14 小程序退役 | M0–M2 未完成；M3 独立关闭 | `apps/miniprogram` 19 个文件、`/auth/wx-login`、`wxOpenid` 仍在 |
| 15 扩展最终生产验收 | 未完成 | 12 项真实岗位业务场景无完整生产证据 |

## 不能被全员上线决定替代的证据

负责人决定取消逐账号、逐合同试点并直接正式使用。该决定合法改变了上线方式，
但上线收据明确没有把以下事项伪写为通过：

- 人工 UAT；
- 真实附件恢复抽样；
- 合同母版逐页签认；
- 普通岗位权限验收；
- 3–5 份真实合同闭环；
- 一份历史接管后的结算和付款；
- 零星材料付款先行/收货先行；
- 零星费用直接办结；
- 固定金额/无固定总价直接付款；
- 垫资额度补足；
- 真实挂靠项目资金不重复核对。

这些证据可以在正式使用期间自然形成，不要求重新组织试点；但在真正发生和验收前，
不能作为 C1、旧接口删除或“全部完成”的依据。

## 仍然关闭的删除边界

- C2 checkpoint 表物理删除；
- M3 `wxOpenid` 等专属数据清理；
- 业务草稿 purge；
- 正式业务记录物理删除；
- AuditLog 物理删除；
- 旧表和旧字段物理删除。

这些事项不是 Release B 的隐含组成，也不能因直接全员上线自动获准。

## 下一合法顺序

1. 取得原索引所指的约 5 元差异真实 Excel，或由用户明确批准替代门禁；
2. 由当前正式使用自然积累 3–5 份真实合同闭环和至少一份历史接管后结算/付款；
3. 在不要求业务人员重新做试点的前提下，记录上述真实业务产生的金额、权限、
   状态、文件和审计证据；
4. 获取批准观察窗口内的旧 route 零调用证据和 checkpoint 新增为零证据；
5. 取得 C1 精确候选/部署授权后执行 Task 8，删除旧调用代码但保留旧表只读；
6. 完成 Task 10 的剩余生产验收；
7. 按顺序完成 Task 11 全站 manifests、Task 12 旧接口退出、Task 13 受控分类迁移；
8. 完成 Task 14 M0→M1→M2；M3 数据清理继续独立授权；
9. 完成 Task 15 扩展生产验收并形成新的精确候选；
10. 只有届时证据逐项齐全，才把五包根目标标为完成；
11. C2/M3 仍在各自获得独立授权后单独执行和验收。

当前没有获得 C1、小程序退役、剩余 production retention、其他业务写入或物理删除
授权。本次审计不执行这些动作。

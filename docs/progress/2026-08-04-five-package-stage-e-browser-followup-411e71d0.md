# 五包阶段 E：付款浏览器门禁复验与发布阻断复盘

日期：2026-08-04  
工作树：`codex/five-package-go-live`  
当前候选 SHA：`411e71d093fe94f405e4e0caa8da4f66b87cfec1`  
运行时代码基线：`728de13a742e56b8d6773e1ba23b802bc739e4d3`

## 本轮完成

发现付款创建浏览器用例漏 mock `GET /api/payments/create-capability?projectId=*`，导致请求代理到未启动的 API 端口并停留在付款工作台。补充精确 capability fixture 后，付款发布门禁重新通过：

| 门禁 | 结果 |
| --- | --- |
| 付款工作台 | 5/5 passed |
| P0 浏览器烟测 | 2 passed / 2 skipped（真实账号场景仍按环境变量跳过） |
| 全局搜索工作台 | 4/4 passed |
| 合同详情 canary | 2/2 passed |
| 结算工作台 v2 | 6/6 passed |
| 手写签名、移动交接、敏感文件 | 6/6 passed |
| Web E2E typecheck | passed |

付款用例覆盖创建、领导审批、移动端拒绝、财务实付、凭证上传和重复提交保护；上述结果均为本地候选工作树浏览器夹具验证，不等同于真实生产账号或生产数据库验收。

## 仍为 NO-GO 的硬门

1. **RC-06**：缺少不 mock 业务 API 的四类真实岗位 storageState、合同→结算→付款→OR-sign→实付/凭证长链，以及绑定同一 SHA 的 400/403/409/503 请求账本。当前付款/结算/签名用例仍主要是 API route mock。
2. **RC-05**：数据库动态门仍有 19 个 suite、54 个测试被 `RUN_*` 环境门跳过；需要 PG16 空库全迁移和二次 deploy、最新自然备份隔离恢复、约束/并发/回滚/readiness 收据。现有专用 runner 只覆盖其中 24/54。
3. **RC-09**：维护停写仍只覆盖合同/接管表面；结算、付款、费用、资金、零采、文件和组织写入口未统一冻结。运行监控缺少 5xx/P95/慢 SQL/队列/COS/审计/幂等/金额不变量，并且尚无版本化 runtime-health timer；自然 Cron、告警送达、异机恢复和生产等价回滚证据缺失。
4. **阶段 F**：尚未使用同一冻结 SHA 完成生产等价迁移、恢复、部署、回滚和四岗位烟测。

## 边界

本轮只改 Web E2E 测试夹具和发布证据；未修改 API、Schema、迁移、部署脚本、生产配置或业务数据，未连接生产、未推送、未合并、未部署。下一步应先按 RC-06/RC-05/RC-09 的依赖顺序分别补齐验证入口和最小实现，再重新冻结 SHA；不能把本轮局部浏览器绿灯当作全站上线批准。

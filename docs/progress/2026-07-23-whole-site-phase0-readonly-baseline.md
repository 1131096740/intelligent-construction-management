# 整站受控改造阶段 0：生产只读基线收据

审计时间：2026-07-23（Asia/Shanghai）
范围：阶段 0 兼容性盘点；生产服务器、生产数据库均只读
状态：完成；没有部署、服务重启、生产迁移、业务数据写入、文件写入或备份触发

## 执行边界

- 运行 SHA、服务状态和健康检查仅通过既有 SSH 通道读取。
- 数据库盘点使用 `PGOPTIONS='-c default_transaction_read_only=on'`；项目草稿脚本同时在事务内执行 `SET TRANSACTION READ ONLY`。
- 自定义统计使用 `BEGIN TRANSACTION READ ONLY`。输出仅包含计数、状态和已脱敏标识；不记录姓名、电话、合同号、金额、文件名、对象键、连接串或密钥。

## 运行基线

| 项目 | 结果 |
| --- | --- |
| 生产应用 SHA | `ab3c31854c53d73bed417725d29da5a435c48606` |
| API / Nginx / PostgreSQL | `active / active / active` |
| API 健康端点 | `{"status":"ok","service":"jiangkong-api"}` |
| 生产已完成迁移 | 74 |
| 部署 Git 工作树 | 0 个已跟踪改动，3 个未跟踪文件；本阶段未读取文件名、未改动该工作树 |

## 业务兼容性统计

| 事实 | 只读结果 | 改造含义 |
| --- | ---: | --- |
| `ProjectExpenseRequest` | 0 | 新费用/借款可用独立新域；旧项目支出仍必须保留兼容读路径和不写入承诺。 |
| 用户总数 / 上传式签名数 | 11 / 0 | Canvas 签名切换不存在现存签名文件迁移，但未来审批的未设签名阻断规则仍待决。 |
| `wxOpenid` 非空数 | 0 | 微信运行时退役没有现存绑定值需要迁移；运行时/字段删除仍须另行授权。 |
| 合同编号规则 / 启用规则 | 4 / 4 | 统一编号不能假定空白起点，需先决议新合同何时、如何切换分配规则。 |
| 合同版本 / 历史接管 / 系统创建 | 6 / 1 / 5 | 合同主键、接管事实和历史版本必须保留。 |
| 可证明票种与税率 / 必须未确认 | 1 / 5 | 不可由旧数据可靠推断的税务事实不得自动回填。 |
| 合同清单行 / 数量或单价超两位 | 1 / 0 | 现存清单不需追溯舍入，仍按历史精度兼容处理。 |
| 已发布模板 / 数量精度大于两位 / 含 `0%` 选项 | 4 / 4 / 4 | 新写入精度规则必须与旧模板只读兼容分开实施。 |
| 结算草稿 / 在途 / 已完成 | 0 / 0 / 0 | 新结算读写路径不涉及现存正式结算回填，但迁移演练仍需证明零金额改写。 |
| 可放弃合同草稿 | 6 | 全部 A 类：无审批、附件、金额占用或下游事实；阶段 0 不进行任何草稿放弃或清理。 |
| 可放弃零星收货草稿 | 1 | A 类；阶段 0 不进行任何草稿放弃或清理。 |
| 其他可放弃候选（接管、税务修订、结算、付款、项目支出、采购、采购付款、模板、临时） | 0 | 本次不需要做数据退役操作。 |

## 发现与处理

1. 运行 SHA、服务和健康状态与阶段 0 委托基线一致；生产服务健康。
2. 生产部署工作树含 3 个未跟踪文件。由于本阶段禁止读取或改动未知部署文件，已记录为发布前必须定位并清理/纳入的运维风险，不能据此执行发布或覆盖。
3. 服务器 root 的 pnpm 11 与 Node 20.20.2 不兼容，使用 pnpm 启动只读脚本会因 `node:sqlite` 失败；直接使用同一项目 Node 脚本完成了只读盘点。该问题不影响当前 API 健康，但阶段 5 的发布/验收脚本必须固定 Node 与 pnpm 版本，不能依赖 root 的自动解析版本。
4. 本收据不能替代阶段 5 的隔离恢复、迁移 dry-run、真实岗位 UAT 或最终发布验收。

## 可复现检查

在具备生产只读授权的会话中，使用既有 SSH 通道：

```bash
# 只读运行基线：SHA、服务状态、健康端点。
ssh ... 'cd /opt/jiangkong && git rev-parse HEAD && systemctl is-active jiangkong-api nginx postgresql && curl -fsS http://127.0.0.1:3000/health'

# 只读草稿盘点：不打印环境变量，数据库事务显式只读。
ssh ... 'sudo -n env PGOPTIONS="-c default_transaction_read_only=on" bash -lc "set -a; . /etc/jiangkong/api.env; set +a; cd /opt/jiangkong/services/api; node scripts/inspect-draft-lifecycle-readiness.cjs"'
```

生产环境变量、数据库连接串和业务明细不得写入命令历史、仓库、聊天或验收材料。

# 建工智管整站受控改造：统一发布候选

日期：2026-07-24
状态：**生产 `8172b7b7…` 保持已发布且本轮未触碰；本地统一候选代码 SHA `be71795d01a3c4974d23c7bb3b9317e401214188` 已完成自动化、隔离迁移和候选审查，等待新的精确 SHA 推送/部署授权**

本文件是阶段 1 至阶段 5 的候选与发布收据；生产推送、部署和前向迁移已在用户 2026-07-24 的精确授权范围内完成。它不授权生产数据清理、旧能力删除、历史改写或超出测试项目范围的业务写入。

## 1. 候选身份与范围

| 项目 | 事实 |
| --- | --- |
| 唯一实施分支 | `codex/whole-site-phase0` |
| 运行候选 SHA | `be71795d01a3c4974d23c7bb3b9317e401214188` |
| 证据 HEAD | 本文件所在提交仅补充候选收据与 `PROGRESS.md`；发布前必须重新核验其与运行候选在 `apps/`、`services/`、`packages/`、`scripts/` 和 `.github/` 中零差异 |
| 需求基线 | `docs/superpowers/specs/2026-07-23-whole-site-business-ui-and-expense-design.md` |
| 能力矩阵 | `docs/superpowers/plans/2026-07-23-whole-site-capability-matrix.md` |
| 生产动作 | 本候选期间均未执行：未推送、未部署、未连接/读取生产数据库或备份、未执行生产迁移/清理、未写入生产业务数据 |

候选覆盖的阶段目标如下：

1. 阶段 1：统一工作台组件、正式编号、Canvas/二维码签名版本、受控 PDF 预览和 `JgBusinessGrid`；
2. 阶段 2：独立费用报销/借款账本、审批、附件、A5 审批单、冲销/还款及资金办理投影；
3. 阶段 3：项目、合同、历史接管、结算与资金链根单收口；
4. 阶段 4：零星采购、付款、收货与统一工作入口收口；
5. 阶段 5：全仓门禁、三类一次性隔离库演练、候选差异审查和本发布材料。

## 2. 旧能力与数据边界

能力分类、迁移方式、用户影响、数据处理和回退方案以能力矩阵为唯一逐项清单。本候选的执行结论是：

- 所有标为**保留**的认证、审批、文件、审计、合同/结算/付款事实继续使用原服务端不变量；
- 所有标为**迁移**的页面和读取模型仅切换新入口或共享组件，历史 URL、版本、归档、审批和审计继续可读；
- 所有标为**替换**的未来写入（Canvas 签名、合同日流水、费用/借款独立账本、`JgBusinessGrid`）均以前向 Schema/兼容读取落地，未回填、改写或删除旧业务数据；
- 已标为**废弃**的原生微信登录/小程序仍未删除、未关闭入口、未清理 `wxOpenid` 或运行时代码。正式退役必须另出影响、使用量、数据处理和回退清单并获授权。

用户已确认的跨域规则已在候选中实施：合同在首次手动成功保存时分配统一北京日流水；未来新增或修改写入限制为最多两位小数；历史冻结事实和历史精度只读保留，不自动舍入或批量重算。

## 3. 可复现验收证据

### 3.1 全仓候选门禁（运行候选 `be71795d…`）

| 门禁 | 结果 |
| --- | --- |
| `CI=true /Users/leoyang/.local/bin/pnpm test` | shared-domain 12 文件 110/110；Web 116 文件 977/977；API 全量 Jest 通过。输出中的 Fontconfig 缓存与模拟 COS 错误是既有负向用例日志，不是生产连接 |
| `CI=true /Users/leoyang/.local/bin/pnpm typecheck` | 三个 workspace 包通过 |
| `CI=true /Users/leoyang/.local/bin/pnpm lint` | 三个 workspace 包通过 |
| `pnpm --filter @jiangkong/api check:business-errors` | 通过，扫描 353 个生产 TypeScript 文件 |
| `pnpm --filter @jiangkong/web-admin check:ui` | 通过 |
| `pnpm --filter @jiangkong/api build` | 通过 |
| `pnpm --filter @jiangkong/web-admin build` | 通过 |
| `CI=true /Users/leoyang/.local/bin/pnpm --filter @jiangkong/web-admin test:e2e:p0` | Chromium 2 passed、2 条仅在提供真实试运行账号时启用的条件跳过（3.1 秒）；隔离 mock，不连接生产 |
| `CI=true bash scripts/ops/go-live-safety-self-test.sh` | 通过；仅自测脚本的预期失败/重试，不连接 COS 或生产 |
| `git diff --check` | 通过 |

根工作区没有 `build` 脚本，故不能以根命令失败误判候选；上表使用了实际发布包的 API/Web 构建命令。

### 3.2 浏览器与视觉证据

| 范围 | 结果与边界 |
| --- | --- |
| 阶段 1 WebKit 工作台金丝雀 | 用户已执行 `CI=true /Users/leoyang/.local/bin/pnpm --filter @jiangkong/web-admin exec playwright test --config playwright.workbench-canary.config.ts --project=webkit`，2/2，14.3 秒；已解除跨浏览器运行时阻塞 |
| 费用与借款资金 WebKit 闭环（本地候选） | `CI=true /Users/leoyang/.local/bin/pnpm --filter @jiangkong/web-admin exec playwright test --config playwright.expense-claim-workbench.config.ts --project=webkit`：5/5；覆盖公司补付及借款放款、还款、财务主管确认与反向更正，均通过上传测试凭证、二次确认、受控 POST 和账本投影状态断言；隔离 mock，不连接生产 |
| 本轮 WebKit 工作台重跑 | 同一命令 2/2，3.8 秒 |
| 阶段 5 浏览器矩阵 | 隔离 mock 下：搜索 4/4、合同 2/2、结算 2/2、费用 6/6、统一资金 2/2、Canvas/二维码签名 4/4、零采 A4→A5→收货 Chromium 18/18、WebKit 18/18 |
| 最小发布浏览器门禁 | `CI=true pnpm --filter @jiangkong/web-admin test:e2e:p0`：隔离 mock 下 2 passed、2 条仅在提供真实试运行账号时启用的条件跳过（2.6 秒）；不连接本地 API |
| 视口 | 阶段内页面金丝雀覆盖九档/关键六档桌面视口、局部横滚、200% 缩放和键盘流；浏览器报告目录为本地生成物，未纳入提交 |

浏览器均使用隔离 mock/测试账号，不能替代迁移后真实岗位、真实签名、真实乙方签章或生产测试项目 UAT。部分历史 fixture 仍输出本地 `127.0.0.1:3000` 背景 mock 代理告警；断言通过，但该告警不得被表述为“零告警验收”。

2026-07-23 首次生产发布工作流 `30021670169` 在部署前安全停止：构建、单测、类型、Lint、业务错误、发布安全、API/Web 构建、UI 治理均通过；随后将 91 条全站 E2E 错当作“P0”运行，而工作流仅启动静态 Web 预览，79 条通过、10 条因无本地 API 或严格 locator 失败。未启动服务器部署任务，生产运行 SHA、数据库和业务数据均未改变。现已把 `test:e2e:p0` 收敛为专用 mock 烟测，并令工作流直接选择该文件；全站 E2E 仍作为本地候选回归保留，不以静态预览替代后端流程验证。

### 3.3 隔离迁移与关键流程演练

以下命令均强制一次性本地 PostgreSQL 16、回环 API、本地文件存储和临时目录；成功或失败均停止 API、删除容器及临时目录。

| 命令/候选 | 结果 |
| --- | --- |
| `pnpm verify:money-bigint:local` / 本地费用与借款收尾切片 | 86 条迁移（含 `20260724100000_expense_claim_payment_execution_and_repayment_reversal`）、`migrate status`、seed/API、401/403 负向、合同归档→治理结算生效→结算付款→审批→两次实付→入账通过；21/21 BigInt 列、`9007199254740993` 精度及临时 API/数据库清理通过 |
| `pnpm verify:spot-procurement-concurrency:local` / `5c9d14d2…` | 85 条迁移后验证付款单胜、供应商余额/错配、实际付款上限与幂等、凭证唯一、文件绑定竞争、项目资金串行、余额不足零写、收货/PDF/发票账本和原始 P2034 并发保护通过 |
| `pnpm --filter @jiangkong/api verify:draft-lifecycle:local` / `1753e061…` | 85 条迁移与 seed；迁移、约束、索引和正式事实只读核验通过；合成合同、付款、模板生命周期探针完整回滚，前后计数、金额、状态和结束事实一致 |

核心合同/结算演练中的 Canvas 签名和乙方签章件为本地技术材料，仅证明冻结、权限和审计链，不等同于真实人员签字或乙方签署。

### 3.4 候选差异审查与自检

- 独立复核范围：费用/借款收尾 `57451e71` 至运行候选 `be71795d`。复核了新账本的金额上限、凭证唯一性、密码确认、放款/还款/确认/反向更正权限、PDF 归档和前向迁移。
- 发现并修复：`ExpenseClaimPaymentExecution.voucherFileId` 最初漏入应用侧统一文件绑定注册表；全仓 Schema 守卫测试捕获该问题，`be71795d` 已将其补入非收货绑定查询和迁移独占触发器守卫测试。不存在未处理的候选审查发现。
- 自检：`git diff --check` 通过；未暂存/提交浏览器报告生成物；未执行生产命令；未删除旧能力；发布前重新执行 `git diff --quiet <运行候选> <证据HEAD> -- apps services packages scripts .github`。

## 4. 迁移影响与演练策略

1. 本候选含新增前向 Prisma 迁移，既不执行 down migration，也不在生产自动回填、清理、重建或删除旧草稿/历史业务数据。
2. 本地空库已验证当前完整的 85 条迁移可顺序应用并处于最新状态；这不是生产备份恢复，也不证明生产存量数据已完成兼容确认。
3. 正式发布前必须重新取得生产发布时点的只读基线：运行 SHA、迁移清单/状态、备份收据、核心表计数和待迁移事实摘要。
4. 必须从发布前自然/受控备份恢复到隔离库，执行候选迁移、`migrate status`、只读差异核验和与候选一致的关键流程验证；任何数据迁移 preview、批量修复或清理均须单独列明对象、影响、备份和回退，并另获授权。
5. 历史事实不能可靠推断的，继续标识为“历史单据，未按新流程采集”；不得伪造 Canvas 签名、付款主体、税务、附件、审批或归档事实。

## 5. 发布步骤（仅供授权后执行）

1. 固定并由用户确认 40 位运行候选 SHA 与证据 HEAD；核验两者运行时目录零差异。
2. 复跑全仓门禁、关键 WebKit/Chromium 浏览器流和 `git diff --check`，记录输出、时间、环境和 SHA。
3. 在生产进行只读基线核验并取得发布前备份；先将备份恢复到隔离库完成迁移演练，失败即停止。
4. 用户已于 2026-07-24 明确将真实岗位 UAT 改为正式使用后的问题反馈，不再作为本候选的前置门禁；自动化、构建、隔离迁移和跨浏览器验证仍必须逐候选完成。
5. 汇总迁移演练、风险与回滚结论，形成发布候选；没有用户明确的推送、部署和生产迁移授权时，停止在此。
6. 获授权后才推送和部署精确 SHA；迁移结束后核验 SHA、迁移状态、健康、权限拒绝、文件与审计。不得把部署授权扩大解释为数据清理、旧能力删除或历史业务写入授权。

## 6. 回滚与停止条件

| 情形 | 处置 |
| --- | --- |
| 构建、迁移、健康或核心冒烟失败 | 立即停止发布，不继续推送/部署/迁移；保留日志与证据，恢复到已验证运行版本 |
| 迁移已执行、但尚无新格式事实写入 | 保留前向兼容数据库结构，可回退应用到理解该结构的安全 SHA；不手写 down migration |
| 已产生新签名、费用账本、正式编号、文件绑定或其他新事实 | 禁止盲目回退到不理解新结构的旧代码；冻结高风险写入，采用前向修复；如需数据恢复，先隔离恢复并取得新的维护窗口与明确授权 |
| 正式使用反馈暴露问题 | 保留审计和文件事实；不删除、篡改或批量清理，先制定可审核的前向修复或回退方案 |
| 要求删除小程序、旧费用域、旧签名或历史数据 | 视为独立不可逆变更，必须另行提交用户影响、数据处理、回退与授权 |

## 7. 当前 No-Go 项与发布授权请求

真实岗位 UAT 已由用户明确改为正式使用后的持续反馈，故不再作为候选硬门禁。`be71795d…` 已固定并完成完整自动化门禁、代码审查与本地 86 条迁移演练；当前仍为 **No-Go** 的唯一原因是尚未获得将该精确 SHA 推送至共享 `main`、执行候选绑定隔离恢复、生产部署与 85→86 前向迁移的明确授权。不得把既有 `8172b7b7…` 的发布授权扩大到后续提交。

## 8. 2026-07-23 历史生产只读基线与隔离恢复收据

本节按当时用户对“只读基线 + 备份隔离恢复”的授权执行，不包含推送、部署、生产迁移、生产数据清理或旧能力删除。它证明历史 `44b09874…` 候选可由当时生产备份恢复验证，不得替代 `be71795d…` 在本次发布窗口需要完成的全新隔离恢复。

| 项目 | 结果 |
| --- | --- |
| 生产运行 SHA | `ab3c31854c53d73bed417725d29da5a435c48606`；与候选 `44b09874…` 不同 |
| 服务与健康 | API、Nginx、PostgreSQL、Cron 均为 `active`；API 仅监听 `127.0.0.1:3000`，PostgreSQL 仅监听 `127.0.0.1:5432`；公网 `https://jgzg.site/api/health` 为 200 |
| 生产只读配置检查 | 使用 Node 20 兼容的 pnpm 9.15.9 执行 `verify:production-readiness`；环境、seed 停用、COS、文件、转换器和字体项全部通过 |
| 生产迁移 | `74|0|0`，`prisma migrate status` 为最新 |
| 生产测试项目 | `seed-project-jgxm-001` 为 active，7 名项目成员；零星采购试点配置也仅为该项目 |
| 备份输入 | 自然备份 `jiangkong-20260723-030001.dump`，本地 `600 root:root`；checksum 与 `pg_restore --list` 通过，并由独立 COS 收据指定对象重新下载 |
| 隔离恢复 | 仅恢复至新建的 `jiangkong_restore_20260723_44b09874`；候选 checkout 精确为 `44b09874bc05dc7271746386914828b9483de0a7` 且 clean |
| 候选迁移 | 恢复库从 74 条迁移升至 85 条，`prisma migrate status` 最新；恢复前后核心计数为 User 11、Project 2、Contract 6、ContractTakeover 1、Settlement 0、PaymentRequest 0、ProjectExpenseRequest 0、FileObject 30、AuditLog 285 |
| 恢复库只读核验 | `verify-draft-lifecycle.cjs --allow-isolated-restore` 通过：85 条迁移、约束/索引和正式事实一致；验证显式只读事务执行，机器证据摘要 SHA-256 为 `4ddcf0290bf72703d1fbabc9ca89cdd985ee825e717fa32222b5ff7a8726958f` |
| 清理 | 已删除隔离数据库、从 COS 下载的备份副本、临时候选 checkout、兼容 pnpm 包装目录及本地/服务器临时 bundle；正式库、COS 源对象和运行服务未改变 |

演练中发现 root 默认 Corepack 会选 pnpm 11，而生产 Node 20 无 `node:sqlite`；此次仅对隔离命令显式使用服务器缓存 pnpm 9.15.9，未修改服务器全局配置。首次误用旧 `127.0.0.1:5173` 健康端口也已纠正为实际 API `127.0.0.1:3000`，不构成服务故障。

该历史收据消除了当时“生产备份可恢复至候选 Schema”的技术门禁，但不能替代 `be71795d…` 的当前恢复演练。其后用户已授权并完成精确候选 `8172b7b7…` 的推送、部署和生产 74→85 前向迁移，详见下一节；该授权始终不包括数据清理、历史改写或旧能力删除。

## 9. 2026-07-24 历史已授权生产发布收据与 UAT 交接

| 项目 | 结果 |
| --- | --- |
| 精确发布候选 | `8172b7b7a872e9fe0bb6ea84ff5bad39efb9f810`；相对先前候选仅含发布门禁、E2E mock 与证据变更，业务源码、Prisma Schema 与迁移零差异 |
| 发布工作流 | GitHub Actions `30023569545` 成功：完整测试、类型、Lint、业务错误、发布安全、API/Web 构建、UI 治理和生产构建 P0 浏览器烟测均通过；验证 job 10 分 03 秒，服务器部署 job 2 分 10 秒 |
| 部署后只读核验 | 服务器运行 SHA 为 `8172b7b7…`；迁移 `85|0|0`；API、Nginx、PostgreSQL、Cron 均为 active；`127.0.0.1:3000/health` 与 `https://jgzg.site/api/health` 均返回服务正常 |
| 测试项目 | `seed-project-jgxm-001` 的已启用岗位覆盖合同经办 2、财务经办 2、物资主管 1、物资经办 1、项目经理 1；当前真实浏览器会话为董事长，工作台加载正常、可见项目 2、待办/阻塞均为 0 |

未使用或重置任何人的密码，未伪造签名、乙方签章、审批或付款事实。真实链路 UAT 需由合同经办、项目经理、财务经办、物资经办/主管和董事长/总经理分别以本人账号完成；浏览器会话已保留供后续接力。

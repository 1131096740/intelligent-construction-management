# 2026-07-27 合同清单跨版本与结算工作台 V2 启动记录

## 1. 目标

按已确认的
[《建工智管合同清单跨版本与结算工作台 V2 设计》](../design/建工智管_合同清单跨版本与结算工作台V2设计_20260727.md)
推进 P1/P2。

本轮启动不连接生产、不执行生产迁移、不回填历史清单来源、不推送或部署。

## 2. 基线

- 分支：`codex/settlement-workbench-v2`
- 起点：`5a05cdbc303c4620028e6ae832d3a0ce8053154f`
- 主工作区在开工前干净。
- 合同清单、合同条款保存和顶部“最近打开”已经完成并发布，本轮不重复实现。
- 当前结算来源行仍只按同一合同版本统计历史占用。
- 当前最终结算仍保留手填累计金额和五项确认，等待后续切片迁移。

## 3. 实施任务

### P1 核心账本与高频工作台

- [x] Task 1：同一合同只能存在一份进行中的结算。
- [ ] Task 2：合同显式冻结“需要结算/直接付款”事实，替代纯合同类型判断。
- [ ] Task 3：新增清单稳定来源身份，覆盖原始合同、新增行、在线修改、合同变更复制和整表保存写路径。
- [ ] Task 4：结算来源行按合同全生命周期和稳定来源身份累计有效历史结算。
- [ ] Task 5：合同新版清单导入对照、原子预检和待确认阻断。
- [ ] Task 6：结算期间、最终结算新口径和旧五项确认兼容迁移。
- [ ] Task 7：合同清单项、签证/变更项目、金额调整项三类来源和两位精度。
- [ ] Task 8：结算主表迁移到全宽 `JgBusinessGrid`，补搜索、筛选、冻结列、粘贴、错误定位和固定汇总。
- [ ] Task 9：显式保存、修订冲突、离开保护和浏览器临时恢复。
- [ ] Task 10：PDF 对照、页面规范化、文件事实展示和正式证据冻结。
- [ ] Task 11：P1 全链路回归、迁移验证、历史数据只读预检和发布候选。

### P2 复杂变更与资金增强

- [ ] Task 12：清单一对多、多对一映射及历史分配守恒。
- [ ] Task 13：追溯调价补差。
- [ ] Task 14：超结、零金额和负金额结算。
- [ ] Task 15：财务退款/抵扣余额与资金凭证。
- [ ] Task 16：无上限框架合同超量说明。
- [ ] Task 17：合同与结算统一文件事实查看及移动端 PDF 核对增强。

## 4. Task 1 实现事实

共享领域层新增 `SETTLEMENT_IN_PROGRESS_STATUSES`，只包含：

- 草稿；
- 审批中、待审批、审批驳回、撤回；
- 最终文件生成中；
- 审批完成待归档及待归档确认。

它明确排除：

- 已生效；
- 部分付款；
- 已付款；
- 已作废。

因此，上一期结算归档生效但尚未付清时，不会阻止下一期结算。

`SettlementDraftService.create` 和 `copyAbandoned` 在已锁定合同及当前有效版本后，依次检查：

1. 是否存在未提交且未作废的结算草稿；
2. 是否存在仍在办理的正式结算。

存在占用时返回包含结算编号的中文业务错误，不创建第二份草稿。
合同锁使两个并发新建请求串行观察同一占用事实。

## 5. Task 1 验证

- Shared domain：`statuses.test.ts`，6/6。
- API：`settlement-draft.service.spec.ts`，22/22。
- 新增测试先分别以常量缺失、第二草稿未阻断和在途正式结算未阻断形成 RED，再完成 GREEN。
- Shared domain typecheck、lint 通过。
- API typecheck、lint、`check:business-errors` 通过。
- `git diff --check` 通过。

## 6. 数据与发布边界

后续 Schema 可以前向新增稳定来源字段、映射表、索引和约束，但历史来源回填不进入普通迁移自动执行。

历史回填必须另行完成：

1. 生产只读预检；
2. 无歧义和待人工确认清单；
3. 备份及隔离恢复验证；
4. 独立数据变更授权；
5. 执行后数量、金额和结算可用量只读复核。

应用发布授权不能替代历史业务数据回填授权。

# 阶段 B 生产备份与临时恢复库迁移演练（2026-07-12）

## 授权与边界

用户明确授权：允许创建生产数据库备份和临时恢复库迁移演练，不修改正式库，不部署。

本轮实际执行符合该边界：

- 正式库仅用于 `pg_dump` 和显式只读查询。
- 只在独立临时数据库恢复备份并执行候选迁移。
- 未推送、合并、切换生产代码、执行正式库迁移、重启服务或清理正式库权限异常。

## 生产预检

- 生产数据库大小：11,836,439 字节。
- `/srv` 可用空间：50,635,730,944 字节。
- `pg_dump` / `pg_restore` / `psql`：PostgreSQL 16.14。
- 正式库已完成迁移 38，未完成/失败 0，超过 1 分钟事务 0。

## 备份证据

- 文件：`/srv/jiangkong-backups/db/jiangkong-20260712-205711.dump`
- PostgreSQL custom format 清单：333 项，`pg_restore --list` 成功。
- 大小：192,865 字节。
- 权限：`600`，所有者 `ubuntu:ubuntu`。
- SHA-256：`da8123832a89e927e872945cabc3e11c5b9882d4ce606dc66816c74736d18d1e`

## 恢复与迁移演练

- 候选 Prisma schema SHA-256：`25fbb3708dd477622593ff794fda13401578ce89ad75dad078269b4656daf9b6`。
- 候选目录包含 47 个受 Git 管理的迁移；生产备份已有 38 个，待演练 9 个。
- 传输时发现本地存在一个未跟踪的空目录 `20260712170000_layout_template_revision_safety`；该目录无 `migration.sql`、不进入 Git/发布，已从临时演练目录移除。真实版式迁移为 `20260712170500_layout_template_revision_safety`。
- 备份恢复成功；恢复后基线：迁移 38、人员 27、合同版本 6、版式版本 4。
- 使用候选 schema 执行真实 `prisma migrate deploy`，9 个迁移全部成功；`prisma migrate status` 返回 `Database schema is up to date!`。
- 演练后：已完成迁移 47、未完成/失败 0、本轮候选迁移 9/9。
- 关键 schema 证据：全局岗位部分唯一索引 1、合同唯一有效版本索引 1、新治理表 9/9、版式检查修订回填 4/4。
- 业务行数保持：人员 27、合同版本 6、版式版本 4。
- 项目级 `super_admin` 异常在演练库仍为 1，证明迁移没有静默清理权限事实。

## 清理与正式环境复核

- 临时数据库已删除，剩余计数 0。
- 临时迁移目录已删除。
- 新生产备份保留，权限仍为 `600`。
- 正式库仍为 38 个已完成迁移、0 失败；目标部分唯一索引仍为 0，项目级 `super_admin` 异常仍为 1。
- `jiangkong-api` 与 Nginx 均为 active，本机/公网健康检查均返回 200。
- 生产 `/opt/jiangkong` 仍为 `915b86b33e3fc3f387338e440cd1aeb93eae1265`，tracked 修改为 0。

## 下一闸门

本轮已关闭“备份不可用”和“候选迁移未经真实 PostgreSQL 演练”风险。进入发布前仍需用户分别明确授权：

1. 允许推送/合并候选分支、触发生产部署并在正式库执行 9 个迁移。
2. 允许在新版本界面中单独预览并清理 1 条项目级 `super_admin` 异常。

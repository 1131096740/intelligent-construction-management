# 五包阶段 E：PG16 动态数据库门复验收据

日期：2026-08-04  
工作树：`codex/five-package-go-live`  
验证范围：本机 Docker Desktop `postgres:16`，每个 runner 使用 127.0.0.1 随机端口、一次性数据库和自动清理；不连接生产。

## 当前已复验的动态 runner

| Runner | 结果 | 关键证据 |
| --- | --- | --- |
| 付款实付并发 | passed | 完整 116 迁移、二次 deploy 零待办、存量 139000 迁移保留/14 类非法事实失败回滚、并发 1/1 |
| 项目支出实付并发 | passed | 完整 116 迁移、在途写入下迁移立即失败关闭、合法事实保留/13 类非法事实失败回滚、修复重试、并发 1/1 |
| 项目支出财务并发 | passed | 空库与完整 116 迁移、no-op、14 类非法事实与在途 writer 失败回滚、resolve/retry、并发 1/1 |
| 项目支出收货确认并发 | passed | 完整 116 迁移、legacy NULL 保留、6 类非法事实与在途 writer fail closed、并发 1/1 |
| 结算草稿生命周期并发 | passed | 完整 116 迁移、提交/放弃双赢家顺序串行化、并发 1/1 |

## 本轮修复

项目支出实付 runner 的旧库存量跨项目垫资夹具错误写入只在新迁移后才存在的四列；调整为真实 pre-140 结构。当前动态服务又要求已审批垫资具备完整生命周期审批实例，因此在并发测试 fixture 中补入两节点（财务主管、董事长/总经理）已通过的冻结 OR-sign 快照。修复未改生产 API、Schema 或迁移。

## 仍未闭合

当前盘点仍有 54 条动态测试受 `RUN_*` 环境门影响，现有专用 runner 总体只覆盖其中 24 条；本轮是对其中 5 条的当前候选复验，不能把它们写成 54/54。剩余 30 条需统一安全编排并全部执行；还需空库/二次 deploy、自然备份隔离恢复、迁移前后业务/金额/文件/审计守恒、关键 `NOT VALID` 约束检查和失败回滚收据。阶段 F 仍未开始。

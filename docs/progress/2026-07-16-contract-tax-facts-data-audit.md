# 合同税务事实迁移数据审计

- 审计日期：2026-07-17 00:12:52（Asia/Shanghai）
- 执行方式：生产数据库显式只读会话
- 生产应用 SHA：`b857a4269aa907e0550470cece52c846bcbb7623`
- 生产迁移数量：51 个已完成，0 个已知未完成
- 状态：生产只读聚合已完成；未修改数据库、应用代码或业务数据

## 审计边界

本审计只输出聚合数量，不输出合同名称、相对方、文件名、用户信息、数据库连接串或任何密钥。生产执行必须设置：

```bash
PGOPTIONS='-c default_transaction_read_only=on'
```

脚本只使用 Prisma 的 `findMany` 和 `groupBy` 读取以下事实：

- 合同版本中可由现有结构化字段证明的发票类型和税率；
- 历史接管与系统创建合同版本数量；
- 清单行税率、数量和含税单价精度分布；
- 已发布模板数量精度和 `0%` 快捷项分布；
- 结算草稿、在途和已完成数量；
- 可安全回填、必须保持未确认和历史精度兼容分组。

## 聚合结果

| 分组 | 数量 | 说明 |
| --- | ---: | --- |
| 合同版本总数 | 4 | 不输出合同明细 |
| 历史接管版本 | 1 | 迁移后仍保持未确认 |
| 系统创建版本 | 3 | 仅证明充分的记录可回填 |
| 可证明票种和税率 | 0 | 当前没有记录同时满足固定票种和有效数字税率证明 |
| 必须保持未确认 | 4 | 不猜测、不反推 |
| 清单行总数 | 1 | 迁移后统一标记为历史精度兼容 |
| 税率小于或等于 0 的清单行 | 0 | 不自动确认 |
| 数量超过两位小数 | 0 | 不追溯舍入 |
| 含税单价超过两位小数 | 0 | 不追溯舍入 |
| 已发布模板数量精度超过两位 | 4 | 旧模板保持可读 |
| 已发布模板含 `0%` 快捷项 | 4 | 新建/发布规则收口，不覆盖旧快照 |
| 结算草稿/在途/已完成 | 0 / 0 / 0 | 不输出业务编号 |

## 迁移分组原则

1. 只有非历史接管合同，且现有结构化字段中的票种精确为“增值税普通发票”或“增值税专用发票”、税率大于 0 且不超过 100 时，才允许保守回填。
2. 历史接管合同一律保持未确认；即使旧文本中出现类似内容，也必须经过合同员补录、财务主管复核和合同部主管确认。
3. 所有既有清单行均标记为 `legacy`，迁移不得修改其数量、单价、税率或金额。
4. 生产迁移前必须生成最新数据库备份，并在 `jiangkong_restore_*` 隔离数据库中完成恢复、迁移和只读逐项核验。

## 待补证据

- 最新异机备份对象、校验和与 `pg_restore --list` 结果；
- 隔离恢复库迁移前后数量与 Prisma migrate status；
- 既有合同、清单、结算和付款金额未变化的只读断言。

## 本次执行结论

- 生产审计使用既有 SSH 通道在服务器上执行，Prisma 会话通过
  `PGOPTIONS='-c default_transaction_read_only=on'` 强制只读。
- 正式库迁移数量使用同一生产连接在显式只读会话中核验为 51。
- 4 个现有合同版本均不能被迁移自动确认为完整税务事实；新结构迁移必须全部保持 `unconfirmed`。
- 唯一现有清单行数量和含税单价均未超过两位小数，但仍按历史兼容策略标记为 `legacy`，不得重算或覆盖。
- 4 个已发布模板均使用超过两位的数量精度并包含 `0%` 快捷项；治理必须区分“旧模板读取兼容”和“新建、提交、发布规则”，不能机械改写历史模板 JSON。
- 当前生产没有正式结算记录，因此新增结算草稿和税务快照结构不会涉及既有结算数据回填；仍需在隔离恢复库证明迁移零金额改写。
